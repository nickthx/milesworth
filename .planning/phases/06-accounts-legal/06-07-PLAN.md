---
phase: 06-accounts-legal
plan: 07
type: execute
wave: 5
depends_on: ["06-05", "06-06"]
files_modified:
  - .planning/phases/06-accounts-legal/06-VALIDATION.md
autonomous: false
requirements: [ACCT-01, ACCT-02, ACCT-03, ACCT-04]

must_haves:
  truths:
    - "Production serves /privacy (200, static), /account (200 signed out), and /og still as a cookie-free, CDN-cached PNG after the proxy went live"
    - "A real person signs up through the modal with a visible consent checkbox, saves balances, bookmarks a result, records a goal, sees all three persist on a fresh session, and the guest flow works in a private window with no account"
    - "Deleting the account from /account removes every DB row and the Clerk user and lands on / signed out — users rows: 0"
    - "06-VALIDATION.md is signed off: nyquist_compliant: true, every row green or human-verified"
  artifacts:
    - path: ".planning/phases/06-accounts-legal/06-VALIDATION.md"
      provides: "Signed-off validation contract"
      contains: "nyquist_compliant: true"
  key_links:
    - from: "origin/main"
      to: "https://points-unlocked.vercel.app"
      via: "Vercel Git auto-deploy (05-05 precedent)"
      pattern: "points-unlocked.vercel.app"
    - from: "scripts/db-check.ts"
      to: "users / user_balances / bookmarks / travel_goals"
      via: "row counts before and after the human delete"
      pattern: "users rows"
---

<objective>
Ship Phase 6 and prove it against production: run every local gate, deploy by push, probe the routes and the `/og` cache with curl, then hand the human the end-to-end checklist that no test can run (Clerk modal + consent, cross-session persistence, the delete flow) and sign off the validation contract.

Purpose: The three success criteria are user-observable behaviors that involve Clerk's hosted UI and a real browser session. Everything automatable was pinned in plans 06-02..06-06; this plan closes the loop on the rest with evidence.
Output: production deployment of Phase 6, curl evidence, human-verify approval with row-count proof, `06-VALIDATION.md` `nyquist_compliant: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accounts-legal/06-RESEARCH.md
@.planning/phases/06-accounts-legal/06-VALIDATION.md
@.planning/phases/05-credibility-layer/05-05-SUMMARY.md

<interfaces>
Production host: https://points-unlocked.vercel.app (SITE_URL). Deploy-by-push: origin/main is production; Vercel builds within ~30-90s (05-05 precedent). Preview hosts sit behind Deployment Protection (401 for curl) — probe production only.

Phase 5 /og cache gate (must still hold — T-06-08): `curl -sI "https://points-unlocked.vercel.app/og?ur=90000&mr=50000"` twice → `content-type: image/png`, no `set-cookie`, second response `x-vercel-cache: HIT` (Vercel strips s-maxage from the client-facing header; MISS→HIT is the proof).

scripts/db-check.ts prints: `programs rows: N`, `interest_signups rows: N`, `users rows: N`, `user_balances rows: N`, `bookmarks rows: N`, `travel_goals rows: N` (06-02).

Assumptions the human checklist tests: A5 (after modal sign-in the Save button switches without a manual reload — fallback if not: a `SessionRefresh` client component calling `router.refresh()` when `useAuth().isSignedIn` differs from the server prop; would be a gap-closure task), A6 (signOut redirect), A7/A8 (consent UI and Terms requirement), A1 (dev-instance badge acceptable).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Full local gates, deploy by push, probe production with curl</name>
  <files></files>
  <read_first>
    - .planning/phases/05-credibility-layer/05-05-SUMMARY.md (deploy-by-push flow, curl probe set, MISS→HIT proof)
    - .planning/phases/06-accounts-legal/06-VALIDATION.md (the full-suite command and manual table)
    - src/proxy.ts (matcher — what /og exclusion should produce)
    - scripts/db-check.ts (count lines)
  </read_first>
  <action>
    1. Local gates on the merged tree: `npm test && npm run typecheck && npm run lint`, then the env-exported build (`set -a; . ./.env.development.local; set +a; npm run build`) and assert the route table shows `ƒ /`, `ƒ /account`, `ƒ /og`, `○ /methodology`, `○ /privacy`. Record the route table lines in the SUMMARY. Run `npx tsx scripts/db-check.ts` and record the six count lines (baseline before the human test).
    2. Deploy: confirm `git status --porcelain` is empty and `git merge-base --is-ancestor origin/main HEAD` exits 0 (fast-forward safe); then `git push origin HEAD:main`. If HEAD is not a descendant of origin/main, STOP and report — the orchestrator must merge first; never force-push. Poll `curl -s -o /dev/null -w '%{http_code}' https://points-unlocked.vercel.app/privacy` every 15s up to 4 minutes until it returns 200 and the body of `/privacy` contains "Retention and deletion" (proof the new build is live, not the old one).
    3. Production probes (record each header set in the SUMMARY):
       - `curl -sI https://points-unlocked.vercel.app/privacy` → 200; `curl -s …/privacy | grep -c 'mailto:'` ≥ 1
       - `curl -sI https://points-unlocked.vercel.app/account` → 200 (signed-out prompt, no redirect — assert no `location:` header)
       - `curl -s https://points-unlocked.vercel.app/ | grep -c 'Sign in'` ≥ 1 (header affordance rendered for guests)
       - `curl -sI "https://points-unlocked.vercel.app/og?ur=90000&mr=50000"` twice → both `content-type: image/png`, zero `set-cookie` lines, second has `x-vercel-cache: HIT` (T-06-08 / A4). If any `set-cookie` appears on /og, the matcher exclusion failed — stop and report (gap).
       - `curl -sI https://points-unlocked.vercel.app/methodology` → 200
    4. Print the three URLs the human needs (share link `https://points-unlocked.vercel.app/?ur=90000&mr=50000`, `/account`, `/privacy`) at the end of the task output.
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && test "$(curl -s -o /dev/null -w '%{http_code}' https://points-unlocked.vercel.app/privacy)" = "200" && curl -s https://points-unlocked.vercel.app/privacy | grep -q 'Retention and deletion' && test "$(curl -s -o /dev/null -w '%{http_code}' https://points-unlocked.vercel.app/account)" = "200" && test "$(curl -sI 'https://points-unlocked.vercel.app/og?ur=90000&mr=50000' | grep -ci 'set-cookie')" = "0" && curl -sI 'https://points-unlocked.vercel.app/og?ur=90000&mr=50000' | grep -qi 'content-type: image/png'</automated>
  </verify>
  <acceptance_criteria>
    - `npm test`, `npm run typecheck`, `npm run lint`, env-exported `npm run build` all exit 0; route table lines recorded in the SUMMARY
    - `git push origin HEAD:main` succeeded as a fast-forward (or the task reports the non-FF state and stops)
    - Production `/privacy` returns 200 and contains "Retention and deletion" and at least one `mailto:`; `/account` returns 200 with no `location` header; `/methodology` returns 200; `/` HTML contains "Sign in"
    - `/og?ur=90000&mr=50000` returns `image/png` with zero `set-cookie` headers on both fetches and `x-vercel-cache: HIT` on the second; all header sets recorded in the SUMMARY
    - `npx tsx scripts/db-check.ts` baseline (six lines) recorded in the SUMMARY
  </acceptance_criteria>
  <done>Phase 6 is live on production with every automatable route and cache property re-proven by curl.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end account walkthrough on production (sign-up with consent, save, bookmark, goal, persistence, delete, guest flow)</name>
  <files></files>
  <read_first>
    - .planning/phases/06-accounts-legal/06-07-SUMMARY.md draft (Task 1 curl evidence + db-check baseline)
    - .planning/phases/06-accounts-legal/06-RESEARCH.md Assumptions A1, A5, A6, A7, A8 (what the human is confirming)
    - scripts/db-check.ts (post-approval command)
  </read_first>
  <what-built>Phase 6 is deployed to https://points-unlocked.vercel.app: optional Clerk sign-in (modal, development instance — expect a "Development mode" badge), Save my balances beside Copy my link, bookmark toggles on every result card, /account with saved balances + Open my saved results, bookmarks, travel goals (add/remove), and Delete my account (DB cascade → Clerk delete → sign out), plus the /privacy page linked from the footer. Curl evidence for routes and the /og cache is in the SUMMARY draft. Clerk's hosted modal, consent checkbox, cross-session persistence, and the delete flow cannot be exercised by the executor.</what-built>
  <action>Pause for the human steps. Before pausing, print the share link, /account, and /privacy URLs. After the human resumes with "approved", run `npx tsx scripts/db-check.ts` and record the six lines: `users rows` MUST equal the Task 1 baseline (the test account was deleted), and `programs rows: 21` must be unchanged. If the human reports any mismatch (especially step 4 — the Save button not switching after sign-in — or step 10), do not mark the phase complete: record the discrepancy verbatim as a gap for `/gsd:plan-phase 6 --gaps` and stop.</action>
  <how-to-verify>
    1. Guest flow first — open a private/incognito window at https://points-unlocked.vercel.app/?ur=90000&mr=50000. Expected: results render exactly as before, the header shows "Sign in", the results header shows "Copy my link" and "Sign in to save", each card shows "Sign in to save this". No Clerk prompt appears on its own. Enter a different balance — results update. Close the window.
    2. In a normal window open the same share link and click "Sign in" in the header (or "Sign in to save"). Expected: a Clerk modal opens IN PLACE (the URL still contains ur=90000&mr=50000). Choose sign-up with an email you control.
    3. Consent: before you can finish sign-up, the modal shows a checkbox that links to the privacy policy (ACCT-04 consent). Expected: the box is required; the link opens https://points-unlocked.vercel.app/privacy. Complete the email code.
    4. After the modal closes, WITHOUT reloading: the header shows "My account" and the Clerk avatar; "Sign in to save" became "Save my balances"; cards show "Save to my account". If any of these still show the signed-out label until you reload, note "A5 failed" in your reply.
    5. Click "Save my balances". Expected: "Saving" then "Saved" for about two seconds. Click "Save to my account" on the first Bookable-now card and on one Almost-there card. Expected: labels flip to "Saved to my account" immediately.
    6. Open https://points-unlocked.vercel.app/account. Expected: "Saved balances" lists 90,000 Chase Ultimate Rewards and 50,000 Amex Membership Rewards; "Open my saved results" leads to /?ur=90000&mr=50000; "Bookmarks" lists the two titles. Add a goal: text "Business class to Tokyo next spring", destination "Tokyo", a date next year → "Goal saved." and the goal appears; press "Remove" on it → "Goal removed." and it disappears; add it once more and leave it.
    7. Persistence: sign out via the avatar menu (expected: lands on /), then open https://points-unlocked.vercel.app/ (no params) in a NEW private window and sign in with the same account. Expected: after sign-in, the URL becomes /?ur=90000&mr=50000 by itself (account restore on a device with empty storage — ACCT-01) and the two bookmarks still read "Saved to my account". /account still shows the goal.
    8. Clerk Dashboard → Users → your test user: expected a legal-acceptance timestamp on the user (name varies by Dashboard version: "Legal accepted at" / "legalAcceptedAt").
    9. Delete: on /account click "Delete my account" → dialog "Delete your account?" → "Delete everything". Expected: you land on / signed out (header shows "Sign in"). Reloading /account shows the signed-out prompt. In the Clerk Dashboard the user is gone.
    10. Reply "approved" plus notes for steps 4 and 8 (or describe the exact step and what you saw instead).
  </how-to-verify>
  <resume-signal>Type "approved" (with step 4 / step 8 notes) or describe the failing step</resume-signal>
  <verify>
    <automated>npx tsx scripts/db-check.ts | grep -E '^users rows: [0-9]+$' && npx tsx scripts/db-check.ts | grep -qE '^programs rows: 21$'</automated>
    <human-check>Steps 1-9 matched: guest flow untouched, modal in place with required consent, Save/bookmark/goal persist across a fresh private session with account restore into the URL, delete lands on / signed out with the Clerk user gone</human-check>
  </verify>
  <acceptance_criteria>
    - Human resume signal is "approved"; step 4 (A5) and step 8 (consent timestamp) are reported as observed
    - After approval, `npx tsx scripts/db-check.ts` prints `users rows: <baseline>` (the test account's rows are gone) and `programs rows: 21`; all six lines recorded in the SUMMARY
    - Any deviation is recorded verbatim in the SUMMARY under "Gaps" and the phase is NOT marked complete
  </acceptance_criteria>
  <done>The three phase success criteria are confirmed by a human on production, with row counts proving the delete cascade.</done>
</task>

<task type="auto">
  <name>Task 3: Sign off 06-VALIDATION.md</name>
  <files>.planning/phases/06-accounts-legal/06-VALIDATION.md</files>
  <read_first>
    - .planning/phases/06-accounts-legal/06-VALIDATION.md (entire file — every row and checkbox)
    - .planning/phases/06-accounts-legal/06-07-SUMMARY.md draft (curl evidence, human notes, db-check lines)
    - .planning/phases/05-credibility-layer/05-VALIDATION.md (a signed-off example)
  </read_first>
  <action>
    Update `.planning/phases/06-accounts-legal/06-VALIDATION.md`: set every Per-Task row's Status to `✅ green` (automated rows — re-run `npm test` once more and require exit 0) or `✅ human-verified 2026-MM-DD` (manual rows, citing the Task 2 step number); fill the Manual-Only table's "Test Instructions" with the Task 2 step numbers; tick every Validation Sign-Off box; set `nyquist_compliant: true` in the frontmatter and `status: approved`; write `**Approval:** approved <today's date>`. If Task 2 recorded a gap, leave the affected rows `❌ red`, keep `nyquist_compliant: false`, and list the gap under a "Gaps" heading instead.
  </action>
  <verify>
    <automated>npm test && test "$(grep -c '^nyquist_compliant: true' .planning/phases/06-accounts-legal/06-VALIDATION.md)" = "1" && test "$(grep -c '^wave_0_complete: true' .planning/phases/06-accounts-legal/06-VALIDATION.md)" = "1" && test "$(grep -c '⬜ pending' .planning/phases/06-accounts-legal/06-VALIDATION.md)" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - `npm test` exits 0
    - `06-VALIDATION.md` frontmatter reads `nyquist_compliant: true`, `wave_0_complete: true`, `status: approved`; zero `⬜ pending` cells remain; every Sign-Off checkbox is `[x]`; the Approval line carries a date
    - (Gap case) `nyquist_compliant: false` retained, red rows and a "Gaps" section present, and the SUMMARY says the phase is not complete
  </acceptance_criteria>
  <done>The validation contract reflects what was actually proven, automated and human, with nothing left pending.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → origin/main → Vercel production | A push deploys to the public host recruiters will visit |
| Public internet → production routes | Guest, signed-in, crawler (/og), and Clerk handshake traffic |
| Human test account → Neon + Clerk | Real PII (an email) is created and must be fully removed |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-08 | Denial of service / Tampering | /og under the proxy | mitigate | Production curl gate: `image/png`, zero `set-cookie`, `x-vercel-cache: HIT` on second fetch; any cookie on /og is a stop-and-report gap |
| T-06-10 | Repudiation / Privacy | Consent capture | mitigate | Human confirms the required consent checkbox links to /privacy and the Clerk user carries a legal-acceptance timestamp (step 8) |
| T-06-06 | Tampering / Repudiation | Delete cascade end-to-end | mitigate | Human deletes the test account; `db-check` proves `users rows` returned to baseline; Clerk Dashboard shows the user gone |
| T-06-13 | Repudiation / Privacy | Clerk Dashboard admin deletion orphaning rows | accept | Documented: v1 has one user-facing delete path (self-serve OFF); the `user.deleted` webhook backstop is deferred (RESEARCH Open Question 2) |
| T-06-07 | Information disclosure | Secrets during deploy/probe | mitigate | No env values printed; probes use public URLs only; SUMMARY contains headers, never keys |
| T-06-12 | Spoofing | Development-instance limits in production | accept | "Development mode" badge and 100-user cap accepted for the LinkedIn launch (A1); production cut-over listed for Phase 7 |
| T-06-SC | Tampering | npm installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- Production: `/privacy` 200 static content, `/account` 200 signed-out prompt, `/og` PNG cookie-free with CDN HIT, `/` shows "Sign in"
- Human: sign-up with consent → save → bookmark → goal → fresh-session restore → delete → `users rows` back to baseline
- `06-VALIDATION.md` `nyquist_compliant: true`, no pending rows
</verification>

<success_criteria>
- Success criterion 1: sign-up saves balances; guest flow fully works logged out (steps 1-5, 7)
- Success criterion 2: bookmarks and goals persist across sessions (steps 5-7)
- Success criterion 3: privacy policy + consent in place; account and data deletable (steps 3, 8, 9 + db-check)
</success_criteria>

<output>
Create `.planning/phases/06-accounts-legal/06-07-SUMMARY.md` when done. It MUST include the curl header sets, the before/after db-check lines, the human's step 4 / step 8 notes, and any gaps.
</output>
