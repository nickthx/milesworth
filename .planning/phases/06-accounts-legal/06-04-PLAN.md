---
phase: 06-accounts-legal
plan: 04
type: execute
wave: 3
depends_on: ["06-02", "06-03"]
files_modified:
  - src/lib/server/account-data.ts
  - tests/account-data.test.ts
  - src/app/actions/account.ts
  - tests/account-actions.test.ts
  - tests/guest-flow-gate.test.ts
  - .planning/phases/06-accounts-legal/06-VALIDATION.md
autonomous: true
requirements: [ACCT-01, ACCT-02, ACCT-03, ACCT-04]

must_haves:
  truths:
    - "Every account write (save balances, set bookmark, add goal, delete goal, delete account) starts with await auth(), scopes every query by that userId, validates input with the 06-02 schemas, and returns only fixed neutral copy"
    - "Deleting an account removes the users row (cascading balances, bookmarks, goals) BEFORE the Clerk user is deleted; if the DB delete fails, Clerk is never called"
    - "One server-only read module returns a user's saved balances, bookmarked slugs (unknown slugs dropped), and goals; it is the only DB reader and is never imported by a client component"
    - "A source-scan test proves the guest flow is untouched: no @/db, drizzle, or @clerk/nextjs/server under src/components; no auth in /methodology, /privacy, /og; no export const dynamic on /"
  artifacts:
    - path: "src/app/actions/account.ts"
      provides: "saveBalances, setBookmark, addGoal, deleteGoal, deleteAccount Server Actions + ActionState"
      exports: ["ActionState", "saveBalances", "setBookmark", "addGoal", "deleteGoal", "deleteAccount"]
    - path: "src/lib/server/account-data.ts"
      provides: "loadAccountSnapshot + pure filterKnownBookmarks / rowsToBalances"
      exports: ["AccountSnapshot", "TravelGoalRow", "loadAccountSnapshot", "filterKnownBookmarks", "rowsToBalances"]
    - path: "tests/account-actions.test.ts"
      provides: "mocked Clerk + db tests incl. delete call order"
      min_lines: 100
    - path: "tests/guest-flow-gate.test.ts"
      provides: "phase-wide source-scan gate"
      min_lines: 50
  key_links:
    - from: "src/app/actions/account.ts"
      to: "@clerk/nextjs/server auth()"
      via: "first statement of every action"
      pattern: "const \\{ userId \\} = await auth\\(\\)"
    - from: "src/app/actions/account.ts"
      to: "src/lib/account-validation.ts"
      via: "safeParse before any DB call"
      pattern: "Schema\\.safeParse"
    - from: "src/app/actions/account.ts deleteAccount"
      to: "clerkClient().users.deleteUser"
      via: "called only after db.delete(users) resolves"
      pattern: "users\\.deleteUser\\(userId\\)"
    - from: "src/lib/server/account-data.ts"
      to: "@/db"
      via: "three selects filtered by eq(table.userId, userId)"
      pattern: "eq\\(.*userId, userId\\)"
---

<objective>
The server layer for accounts: thin `"use server"` actions that gate on the Clerk session, validate with the 06-02 schemas, write with idempotent batch statements, and answer with neutral copy; a server-only read module for `/` and `/account`; mocked tests for the actions (including the DB-before-Clerk delete order); and the phase-wide guest-flow source-scan gate.

Purpose: Plans 06-05 and 06-06 only import action references and the snapshot type — every security decision (who, what, how much) is made here and pinned by tests that never need a database or a Clerk session.
Output: `src/app/actions/account.ts`, `src/lib/server/account-data.ts`, `tests/account-actions.test.ts`, `tests/account-data.test.ts`, `tests/guest-flow-gate.test.ts`, VALIDATION.md `wave_0_complete: true`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accounts-legal/06-RESEARCH.md
@.planning/phases/06-accounts-legal/06-PATTERNS.md
@.planning/phases/06-accounts-legal/06-02-SUMMARY.md

<interfaces>
From src/lib/account-validation.ts (06-02):
```typescript
export const balancesSchema;   // z.partialRecord(enum of 8 slugs, int positive ≤ MAX_BALANCE) → BalancesInput (assignable to Balances)
export const bookmarkSlugSchema; // z.enum(all @/data redemption slugs)
export const goalSchema;       // { text: string(1..280, trimmed); destination?: string(≤80, trimmed); targetDate?: "" | yyyy-mm-dd }
export const goalIdSchema;     // z.coerce.number().int().positive()
export type BalancesInput; export type GoalInput;
```

From src/db (barrel re-exports schema; `db` is a lazy Proxy — importing never connects):
```typescript
export const users;        // clerkUserId (pk), createdAt
export const userBalances; // id, userId, programSlug, points, updatedAt; uniqueIndex(userId, programSlug)
export const bookmarks;    // id, userId, redemptionSlug, createdAt; uniqueIndex(userId, redemptionSlug)
export const travelGoals;  // id, userId, text, destination|null, targetDate|null (string), createdAt
export const db;           // drizzle(neon(...)) — supports db.batch([...]) (neon-http), insert/delete/select
```

From @clerk/nextjs/server (7.9.x): `auth(): Promise<{ userId: string | null; isAuthenticated: boolean; … }>` (must be awaited); `clerkClient(): Promise<{ users: { deleteUser(userId: string): Promise<unknown> } }>`.

From drizzle-orm: `and, eq, notInArray, sql` from "drizzle-orm"; `type BatchItem` from "drizzle-orm/batch"; `.onConflictDoNothing({ target })`, `.onConflictDoUpdate({ target: [col, col], set: {...} })` (a unique INDEX is a valid conflict target).

From src/app/actions/interest.ts (the analog): `"use server"` line 1; `export type InterestState = { status: "idle" | "ok" | "error"; message: string }`; `(prev, formData: FormData)` signature for `useActionState`; raw FormData values passed straight to `safeParse`; only `issue.path` ever inspected; bare `catch {}` returning "Something went wrong. Try again in a moment."; zero `console.*`.

From src/lib/balance-params.ts: `PARAM_KEY_BY_SLUG` (8 keys), `MAX_BALANCE`. From @/engine: `type Balances`. From @/data: `redemptions` array (`slug`, `title`, …).

Phase 5 grep gate (re-baselined by this plan): `grep -rlE 'from "@/db|drizzle' src/components src/app src/lib` must list exactly `src/app/actions/interest.ts`, `src/app/actions/account.ts`, `src/lib/server/account-data.ts`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: src/lib/server/account-data.ts (loadAccountSnapshot + pure helpers) + tests</name>
  <files>src/lib/server/account-data.ts, tests/account-data.test.ts</files>
  <behavior>
    - `filterKnownBookmarks(["a", "zzz", "a", "b"], [{ slug: "a" }, { slug: "b" }])` → `["a", "b"]` (drops unknown, dedupes, preserves first-seen order); empty input → `[]`
    - `rowsToBalances([{ programSlug: "chase-ur", points: 90000 }, { programSlug: "delta", points: 5 }, { programSlug: "amex-mr", points: 0 }, { programSlug: "bilt", points: 10_000_001 }])` → `{ "chase-ur": 90000 }` (unknown slug, non-positive, and over-ceiling rows dropped); all rows dropped → `null`; `[]` → `null`
    - Source scan: the module has no `"use client"`, contains `import "server-only"` is NOT required (keep Phase-5 style) but must contain `from "@/db"`, and has zero `console.` occurrences outside comments
  </behavior>
  <read_first>
    - src/app/actions/interest.ts (the `@/db` import + neutral-failure discipline)
    - src/db/index.ts (lazy Proxy — why importing is safe in tests and at build)
    - src/db/schema.ts user-table block (column property names: clerkUserId, userId, programSlug, points, redemptionSlug, text, destination, targetDate, createdAt)
    - src/lib/balance-params.ts lines 15-80 (PARAM_KEY_BY_SLUG, MAX_BALANCE, the positive-safe-integer guard to replicate)
    - tests/balance-storage.test.ts lines 1-45 (fake-object idiom, relative import style)
    - .planning/phases/06-accounts-legal/06-PATTERNS.md §`src/lib/server/account-data.ts`
  </read_first>
  <action>
    Write the test first (RED) for the two pure helpers, then the module (GREEN).

    Create `src/lib/server/account-data.ts` (server-only by convention and by the guest-flow gate — it is imported only from `src/app/**/page.tsx`; never from `src/components/**`). Header comment: ACCT-01/02/03 read path; the THIRD and last file permitted to import `@/db` (with `actions/interest.ts` and `actions/account.ts`) — T-06-05; neutral failure (return null, never log — T-06-04); Pitfall 6 read-time filter.
    - Imports: `eq, desc` from "drizzle-orm"; `db, userBalances, bookmarks, travelGoals` from "@/db"; `redemptions` from "@/data"; `type { Balances }` from "@/engine"; `PARAM_KEY_BY_SLUG, MAX_BALANCE` from "@/lib/balance-params".
    - `export type TravelGoalRow = { id: number; text: string; destination: string | null; targetDate: string | null }`.
    - `export type AccountSnapshot = { balances: Balances | null; bookmarkedSlugs: string[]; goals: TravelGoalRow[] }`.
    - `export function filterKnownBookmarks(slugs: readonly string[], known: ReadonlyArray<{ slug: string }>): string[]` — pure; Set of known slugs; keep first occurrence order; dedupe.
    - `export function rowsToBalances(rows: ReadonlyArray<{ programSlug: string; points: number }>): Balances | null` — pure; keep rows whose slug is a key of `PARAM_KEY_BY_SLUG` and whose points is `Number.isSafeInteger`, `> 0`, `<= MAX_BALANCE`; return null when nothing survives (defense in depth even though the write side validates — T-06-03).
    - `export async function loadAccountSnapshot(userId: string): Promise<AccountSnapshot | null>` — `Promise.all` of three selects: `db.select({ programSlug: userBalances.programSlug, points: userBalances.points }).from(userBalances).where(eq(userBalances.userId, userId))`; `db.select({ slug: bookmarks.redemptionSlug }).from(bookmarks).where(eq(bookmarks.userId, userId)).orderBy(desc(bookmarks.createdAt))`; `db.select({ id, text, destination, targetDate }).from(travelGoals).where(eq(travelGoals.userId, userId)).orderBy(desc(travelGoals.createdAt))`. Map through `rowsToBalances` and `filterKnownBookmarks(slugs, redemptions)`. Wrap in try/catch → `null`; bare catch, no console. Never accept a userId that did not come from `auth()` — document that the callers are the two RSC pages.
    - Under 110 lines.

    `tests/account-data.test.ts`: relative import of the two pure helpers (importing the module pulls `@/db` — the lazy Proxy never connects, so no env is needed); one `it` per `<behavior>` row; a source-scan `describe` asserting the file contains `from "@/db"`, no `"use client"`, and `(source.split("\n").filter(l => !l.trim().startsWith("//") && l.includes("console.")).length === 0)`.
  </action>
  <verify>
    <automated>npx vitest run tests/account-data.test.ts && test "$(grep -c 'export async function loadAccountSnapshot' src/lib/server/account-data.ts)" = "1" && test "$(grep -c 'eq(.*\.userId, userId)' src/lib/server/account-data.ts)" = "3" && test "$(grep -v '^\s*//' src/lib/server/account-data.ts | grep -c 'console\.')" = "0" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -cE '^export (async )?function (loadAccountSnapshot|filterKnownBookmarks|rowsToBalances)' src/lib/server/account-data.ts` outputs `3`; `grep -cE '^export type (AccountSnapshot|TravelGoalRow)' src/lib/server/account-data.ts` outputs `2`
    - `grep -c 'eq(.*\.userId, userId)' src/lib/server/account-data.ts` outputs `3` (every select scoped by the session user)
    - `grep -c '"use client"' src/lib/server/account-data.ts` outputs `0`; `grep -v '^\s*//' src/lib/server/account-data.ts | grep -c 'console\.'` outputs `0`
    - `npx vitest run tests/account-data.test.ts` exits 0 with ≥ 6 tests; `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The only DB read path for user data exists, is scoped by userId, degrades to null silently, and its filtering rules are unit-tested.</done>
</task>

<task type="auto">
  <name>Task 2: src/app/actions/account.ts — five Server Actions</name>
  <files>src/app/actions/account.ts</files>
  <read_first>
    - src/app/actions/interest.ts (entire file — the contract to replicate: directive, state type, FormData handling, error discipline)
    - src/lib/account-validation.ts (schemas + types from 06-02)
    - src/db/schema.ts user-table block (column properties and the two uniqueIndex names)
    - scripts/seed.ts lines 48-75 (BatchItem<"pg">[] accumulation + `length > 0` guard idiom)
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Pattern 4 (saveBalances), §Pattern 6 (bookmark), §Pattern 9 (delete order), §Pitfall 10 (empty-array footguns), §Security Domain (IDOR, neutral errors)
  </read_first>
  <action>
    Create `src/app/actions/account.ts` with `"use server";` on line 1. Imports: `auth, clerkClient` from "@clerk/nextjs/server"; `revalidatePath` from "next/cache"; `and, eq, notInArray, sql` from "drizzle-orm"; `type { BatchItem }` from "drizzle-orm/batch"; `db, users, userBalances, bookmarks, travelGoals` from "@/db"; `balancesSchema, bookmarkSlugSchema, goalSchema, goalIdSchema` from "@/lib/account-validation". Header comment: ACCT-01..04 write path; second `@/db` importer under src/app (grep gate re-baselined, T-06-05); T-06-01 every action's first statement is `await auth()` and a null userId returns a neutral error — the userId is NEVER accepted from the client; T-06-02 every UPDATE/DELETE filters by `eq(table.userId, userId)`; T-06-03 Zod before any DB code; T-06-04 zod issue text and driver/Clerk errors are never returned or logged; T-06-06 delete order DB → Clerk.

    - `export type ActionState = { status: "idle" | "ok" | "error"; message: string }`.
    - A module-private `const NEUTRAL_ERROR: ActionState = { status: "error", message: "Something went wrong. Try again in a moment." }` and `const SIGN_IN_FIRST: ActionState = { status: "error", message: "Sign in first." }`.
    - A module-private `ensureUser(userId)` helper returning the batch item `db.insert(users).values({ clerkUserId: userId }).onConflictDoNothing({ target: users.clerkUserId })` — used first in every writing batch so a bookmark or goal before any Save never violates the FK.
    - `export async function saveBalances(input: unknown): Promise<ActionState>` — `const { userId } = await auth(); if (!userId) return SIGN_IN_FIRST;` → `balancesSchema.safeParse(input)`; failure → `{ status: "error", message: "Enter whole-number balances." }`. Build `entries = Object.entries(parsed.data)` (slug, points). Statements: `ensureUser`; if `entries.length > 0`: `db.delete(userBalances).where(and(eq(userBalances.userId, userId), notInArray(userBalances.programSlug, entries.map(([slug]) => slug))))` then `db.insert(userBalances).values(entries.map(([programSlug, points]) => ({ userId, programSlug, points }))).onConflictDoUpdate({ target: [userBalances.userId, userBalances.programSlug], set: { points: sql`excluded.points`, updatedAt: sql`now()` } })`; else (zero balances — Pitfall 10: `notInArray([])` is invalid SQL and `.values([])` throws): a plain `db.delete(userBalances).where(eq(userBalances.userId, userId))` and no insert. `await db.batch(statements as [BatchItem<"pg">, ...BatchItem<"pg">[]])`; `revalidatePath("/")`; `revalidatePath("/account")`; return `{ status: "ok", message: "Balances saved." }`; bare catch → NEUTRAL_ERROR.
    - `export async function setBookmark(slug: unknown, bookmarked: boolean): Promise<ActionState>` — auth gate; `bookmarkSlugSchema.safeParse(slug)`; failure → `{ status: "error", message: "That redemption is no longer available." }`. If `bookmarked === true`: batch [`ensureUser`, `db.insert(bookmarks).values({ userId, redemptionSlug }).onConflictDoNothing({ target: [bookmarks.userId, bookmarks.redemptionSlug] })`]; else `db.delete(bookmarks).where(and(eq(bookmarks.userId, userId), eq(bookmarks.redemptionSlug, redemptionSlug)))`. `revalidatePath("/")`; `revalidatePath("/account")`; ok message `"Saved to your account."` / `"Removed from your account."`. Idempotent under retries.
    - `export async function addGoal(_prev: ActionState, formData: FormData): Promise<ActionState>` — auth gate; `goalSchema.safeParse({ text: formData.get("text"), destination: formData.get("destination") ?? undefined, targetDate: formData.get("targetDate") ?? undefined })` (raw values, like interest.ts); failure → `{ status: "error", message: "Enter a goal of up to 280 characters." }`. Batch [`ensureUser`, `db.insert(travelGoals).values({ userId, text, destination: destination || null, targetDate: targetDate || null })`]. `revalidatePath("/account")`; ok `"Goal saved."`.
    - `export async function deleteGoal(_prev: ActionState, formData: FormData): Promise<ActionState>` — auth gate; `goalIdSchema.safeParse(formData.get("goalId"))`; failure → NEUTRAL_ERROR; `db.delete(travelGoals).where(and(eq(travelGoals.id, id), eq(travelGoals.userId, userId)))` (T-06-02 — the id alone is never trusted); `revalidatePath("/account")`; ok `"Goal removed."`.
    - `export async function deleteAccount(_prev: ActionState, _formData: FormData): Promise<ActionState>` — auth gate; inside try: FIRST `await db.delete(users).where(eq(users.clerkUserId, userId))` (cascades user_balances, bookmarks, travel_goals), THEN `const client = await clerkClient(); await client.users.deleteUser(userId);` then `revalidatePath("/")`; return `{ status: "ok", message: "Your account and data were deleted." }`; bare catch → NEUTRAL_ERROR. Comment the ordering rationale (Pitfall 7: a Clerk failure after the cascade leaves the user signed in to retry; the reverse would orphan rows).
    - No `console.*`, no `throw`, no zod `message` reads; keep the file under 200 lines. Nothing else exported.
  </action>
  <verify>
    <automated>test "$(head -1 src/app/actions/account.ts)" = '"use server";' && test "$(grep -c 'const { userId } = await auth();' src/app/actions/account.ts)" = "5" && test "$(grep -cE '^export async function (saveBalances|setBookmark|addGoal|deleteGoal|deleteAccount)' src/app/actions/account.ts)" = "5" && test "$(grep -v '^\s*//' src/app/actions/account.ts | grep -cE 'console\.|throw ')" = "0" && test "$(grep -c 'users.deleteUser(userId)' src/app/actions/account.ts)" = "1" && test "$(grep -rlE 'from "@/db|drizzle' src/components src/app src/lib | tr -d '\r' | sort | tr '\n' ' ')" = "src/app/actions/account.ts src/app/actions/interest.ts src/lib/server/account-data.ts " && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `head -1 src/app/actions/account.ts` outputs `"use server";`
    - `grep -c 'const { userId } = await auth();' src/app/actions/account.ts` outputs `5` (one per action, first statement)
    - `grep -cE '^export async function (saveBalances|setBookmark|addGoal|deleteGoal|deleteAccount)' src/app/actions/account.ts` outputs `5`; `grep -c '^export type ActionState' src/app/actions/account.ts` outputs `1`
    - `grep -cE '(balancesSchema|bookmarkSlugSchema|goalSchema|goalIdSchema)\.safeParse' src/app/actions/account.ts` outputs `4`
    - `grep -c 'eq(travelGoals.userId, userId)' src/app/actions/account.ts` ≥ 1; `grep -c 'eq(bookmarks.userId, userId)' src/app/actions/account.ts` ≥ 1; `grep -c 'eq(userBalances.userId, userId)' src/app/actions/account.ts` ≥ 2
    - In `deleteAccount`, the line containing `db.delete(users)` precedes the line containing `users.deleteUser(userId)` (`awk '/db.delete\(users\)/{a=NR} /users.deleteUser\(userId\)/{b=NR} END{exit !(a<b)}' src/app/actions/account.ts` exits 0)
    - `grep -v '^\s*//' src/app/actions/account.ts | grep -cE 'console\.|throw |\.message'` outputs `0` (zod issue messages never read)
    - `grep -rlE 'from "@/db|drizzle' src/components src/app src/lib` outputs exactly three paths: `src/app/actions/account.ts`, `src/app/actions/interest.ts`, `src/lib/server/account-data.ts`
    - `npm run typecheck`, `npm run lint` exit 0
  </acceptance_criteria>
  <done>All five account writes exist behind the session gate with validated input, userId-scoped mutations, idempotent statements, cascade-before-Clerk deletion, and neutral-only responses.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: tests/account-actions.test.ts (mocked Clerk + db, delete order) + tests/guest-flow-gate.test.ts + VALIDATION wave_0_complete</name>
  <files>tests/account-actions.test.ts, tests/guest-flow-gate.test.ts, .planning/phases/06-accounts-legal/06-VALIDATION.md</files>
  <behavior>
    - account-actions: with `auth` resolving `{ userId: null }`, every action returns `{ status: "error" }` and `db.batch`/`db.delete`/`db.insert` are never called
    - saveBalances with `{ "chase-ur": 90000 }` → status "ok", `db.batch` called once with 3 items (users upsert, scoped delete, upsert insert); `revalidatePath` called with "/" and "/account"
    - saveBalances with `{}` → `db.batch` called once with 2 items (no insert); saveBalances with `{ "chase-ur": -1 }` → status "error" and no db call
    - setBookmark("not-a-slug", true) → error, no db call; setBookmark(<first @/data slug>, true) → ok and `db.batch` called; setBookmark(<slug>, false) → `db.delete` called
    - addGoal with FormData text "Tokyo" → ok and `db.batch` called; with 281-char text → error and no db call
    - deleteGoal with goalId "7" → `db.delete` called and `where` invoked once; with goalId "abc" → error and no db call
    - deleteAccount: `db.delete` is invoked BEFORE `deleteUser` (compare `mock.invocationCallOrder[0]`); when the DB delete rejects, `deleteUser` is never called and the result is the neutral error; success returns "Your account and data were deleted."
    - guest-flow-gate: every `.tsx`/`.ts` file under `src/components` (recursive) lacks `from "@/db`, `drizzle-orm`, `@clerk/nextjs/server`, `CLERK_SECRET_KEY`; `src/app/page.tsx` lacks `export const dynamic`; `src/app/methodology/page.tsx`, `src/app/privacy/page.tsx`, `src/app/og/route.tsx` each lack `@clerk/nextjs/server` and `from "@/db`; `src/proxy.ts` exists with `export default clerkMiddleware()` and no `auth.protect`; neither `src/middleware.ts` nor `middleware.ts` exists; in `src/db/schema.ts` the text after `pgTable("users"` has exactly 3 `references(() => users.clerkUserId` and zero `references(() => programs` / `references(() => redemptions`; the set of files under src/app, src/components, src/lib containing `from "@/db` equals exactly {actions/interest.ts, actions/account.ts, lib/server/account-data.ts}
  </behavior>
  <read_first>
    - src/app/actions/account.ts (Task 2 — the module under test)
    - tests/og-route.test.ts lines 1-15 (file/describe style for an environment-constrained module)
    - tests/balance-storage.test.ts lines 21-45 and 174-187 (fake objects; purity scan)
    - tests/methodology-page.test.ts lines 150-171 (source-scan describe shape)
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Pitfall 9 (vi.mock shape) and §PATTERNS `tests/account-actions.test.ts`, `tests/guest-flow-gate.test.ts` sections
    - .planning/phases/06-accounts-legal/06-VALIDATION.md (frontmatter + Wave 0 checklist to update)
    - vitest.config.ts (node environment; `@` alias — `vi.mock("@/db", …)` resolves through it)
  </read_first>
  <action>
    1. `tests/account-actions.test.ts` — this is the codebase's first `vi.mock` usage, so get ONE test green before writing the rest (A10). Structure:
       - `const mocks = vi.hoisted(() => ({ auth: vi.fn(), deleteUser: vi.fn(), revalidatePath: vi.fn(), batch: vi.fn(), del: vi.fn(), ins: vi.fn(), where: vi.fn() }))` — `vi.mock` factories are hoisted above imports, so every mock they reference must come from `vi.hoisted`.
       - `vi.mock("@clerk/nextjs/server", () => ({ auth: mocks.auth, clerkClient: vi.fn(async () => ({ users: { deleteUser: mocks.deleteUser } })) }))`; `vi.mock("next/cache", () => ({ revalidatePath: mocks.revalidatePath }))` (it throws outside a request scope); `vi.mock("@/db", async (importOriginal) => ({ ...(await importOriginal<typeof import("@/db")>()), db: fakeDb }))` where `fakeDb` is built from the hoisted fns: `insert` returns `{ values: () => ({ onConflictDoNothing: () => ITEM, onConflictDoUpdate: () => ITEM }) }`, `delete` records the call and returns `{ where: mocks.where }` with `mocks.where` resolving to `undefined` by default (so `await db.delete(...).where(...)` works and a batch item can be the returned promise), `select` unused, `batch: mocks.batch` resolving to `[]`.
       - Import the five actions AFTER the mocks via `../src/app/actions/account`.
       - `beforeEach`: `vi.clearAllMocks()`; default `mocks.auth.mockResolvedValue({ userId: "user_123" })`.
       - One `it` per `<behavior>` row. For the delete-order test use `mocks.del.mock.invocationCallOrder[0] < mocks.deleteUser.mock.invocationCallOrder[0]`; for the rejection test `mocks.where.mockRejectedValueOnce(new Error("boom"))` and assert `mocks.deleteUser` not called and the message equals "Something went wrong. Try again in a moment." (the error text "boom" must not appear in the result).
       - Build FormData with `new FormData(); fd.set("text", "Tokyo")` (Node 18+ global).
    2. `tests/guest-flow-gate.test.ts` — header comment: Phase 4/5 guest-flow gates promoted from plan-time greps into a permanent test (T-06-05, T-06-16, T-06-15). Use `readdirSync(dir, { recursive: true })` from `node:fs` over `join(__dirname, "..", "src", "components")`, filter `.ts`/`.tsx`, read each, and assert with `expect(source, file).not.toContain(...)` per `<behavior>`. The `@/db` importer-set assertion walks `src/app`, `src/components`, `src/lib` and compares a sorted array of relative POSIX paths to the expected three (normalize backslashes to `/` for Windows). The schema assertion slices the file text from `pgTable("users"` to the end.
    3. `.planning/phases/06-accounts-legal/06-VALIDATION.md`: set `wave_0_complete: true`; tick every Wave 0 test-file checkbox (all six files now exist); update the "File Exists" column for those rows to ✅; leave `nyquist_compliant: false` (06-07 flips it after the human gates).
  </action>
  <verify>
    <automated>npx vitest run tests/account-actions.test.ts tests/guest-flow-gate.test.ts && test "$(grep -c '^wave_0_complete: true' .planning/phases/06-accounts-legal/06-VALIDATION.md)" = "1" && test "$(grep -c '^nyquist_compliant: false' .planning/phases/06-accounts-legal/06-VALIDATION.md)" = "1" && npx vitest run && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/account-actions.test.ts` exits 0 with ≥ 14 tests, including one whose name mentions "before" for the delete order and one for the rejected DB delete
    - `grep -c 'vi.hoisted' tests/account-actions.test.ts` ≥ 1; `grep -c 'vi.mock("@clerk/nextjs/server"' tests/account-actions.test.ts` outputs `1`; `grep -c 'vi.mock("next/cache"' tests/account-actions.test.ts` outputs `1`; `grep -c 'vi.mock("@/db"' tests/account-actions.test.ts` outputs `1`
    - `npx vitest run tests/guest-flow-gate.test.ts` exits 0 with ≥ 8 tests; the test file references `src/proxy.ts`, `privacy`, `og`, `methodology`, and `pgTable("users"`
    - Full `npx vitest run` exits 0 (all prior suites + the six Phase 6 files present: account-validation, balance-storage, privacy-page, account-data, account-actions, guest-flow-gate)
    - `06-VALIDATION.md` frontmatter reads `wave_0_complete: true` and `nyquist_compliant: false`; no unchecked Wave 0 test-file boxes remain
    - `npm run typecheck`, `npm run lint` exit 0
  </acceptance_criteria>
  <done>The actions' security behavior — session gate, validation, scoping, and the DB-before-Clerk delete order — is pinned without a database or a Clerk session, and the guest-flow invariants are a permanent test rather than a plan-time grep.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser → Server Action | Attacker-controllable arguments and FormData (balances, slugs, goal fields, ids); no client-supplied identity is trusted |
| Server Action → Neon | All user-table writes; every mutation scoped by the session userId |
| Server Action → Clerk Backend API | Account deletion using CLERK_SECRET_KEY |
| RSC page → account-data module | The only read path into user tables |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Spoofing | All five actions invoked without a session (curl, replay) | mitigate | `const { userId } = await auth()` is the first statement of every action; null → fixed "Sign in first." with no DB call; userId is never a parameter; test: auth → null ⇒ no db mock invoked |
| T-06-02 | Elevation of privilege (IDOR) | deleteGoal(id), setBookmark(slug,false), saveBalances delete | mitigate | Every DELETE/UPDATE `where` includes `eq(table.userId, userId)`; goal id is combined with userId, never used alone; grep gates count the scoped predicates |
| T-06-03 | Tampering / DoS | Forged inputs (1e18 balances, bogus slugs, 10 KB goals) | mitigate | `balancesSchema` / `bookmarkSlugSchema` / `goalSchema` / `goalIdSchema` before any DB code; fixed error copy; tests for negative, oversize, unknown-slug inputs |
| T-06-04 | Information disclosure | Action error paths / logs | mitigate | Zod `issue.message` never read (grep `\.message` = 0 outside comments); bare `catch` → neutral copy; no `console.*`; test asserts the driver error text never reaches the result |
| T-06-05 | Information disclosure | DB driver / secret key in client bundle | mitigate | `@/db` importers are exactly three server files; `src/components/**` scanned for `@/db`, `drizzle-orm`, `@clerk/nextjs/server`, `CLERK_SECRET_KEY` by tests/guest-flow-gate.test.ts |
| T-06-06 | Tampering / Repudiation | Account deletion ordering | mitigate | `db.delete(users)` (cascade) awaited BEFORE `clerkClient().users.deleteUser`; DB failure short-circuits so Clerk is untouched and the user can retry; call order + rejection pinned by tests; Dashboard self-serve delete OFF (06-01) |
| T-06-14 | Tampering (CSRF) | Server Action endpoints | accept | Next.js Server Actions enforce same-origin (Origin/Host check) and Clerk cookies are SameSite=Lax; no additional token layer in v1 |
| T-06-15 | Denial of service | Bookmarks referencing reseeded redemptions | mitigate | No FK into seeded tables; write-time enum; `filterKnownBookmarks` drops unknown slugs at read time; gate test asserts the schema rule |
| T-06-SC | Tampering | npm installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- `npx vitest run` exits 0 with all six Phase 6 test files present
- `grep -rlE 'from "@/db|drizzle' src/components src/app src/lib` → exactly the three server files
- `npm run typecheck && npm run lint` exit 0; VALIDATION.md `wave_0_complete: true`
</verification>

<success_criteria>
- Five actions with session gate, validation, scoping, idempotent writes, neutral copy; delete cascades DB before Clerk
- Read module returns a filtered snapshot and is the only reader
- Guest-flow invariants and action security are permanent tests
</success_criteria>

<output>
Create `.planning/phases/06-accounts-legal/06-04-SUMMARY.md` when done. Note whether the `vi.mock` shape worked first try (A10) and any deviation from the specified fake-db shape.
</output>
