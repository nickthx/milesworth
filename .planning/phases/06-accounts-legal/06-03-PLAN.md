---
phase: 06-accounts-legal
plan: 03
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/proxy.ts
  - src/app/layout.tsx
  - src/components/site-header.tsx
  - src/components/site-footer.tsx
  - src/lib/site.ts
  - src/app/privacy/page.tsx
  - tests/privacy-page.test.ts
autonomous: true
requirements: [ACCT-01, ACCT-04]

must_haves:
  truths:
    - "Every route still works signed out: clerkMiddleware() in src/proxy.ts protects nothing, and /og is excluded from the matcher entirely"
    - "A visitor sees 'Sign in' in the site header on every page; a signed-in user sees 'My account' and the Clerk user button"
    - "/privacy is a statically prerendered page (○ in the build table) stating what is collected by mode, who processes it, cookies, retention/deletion, consent, and a contact address; the footer links to it"
    - "/methodology stays ○ and / stays ƒ after ClerkProvider is added — no dynamic prop, no auth() outside dynamic routes"
  artifacts:
    - path: "src/proxy.ts"
      provides: "Public-by-default Clerk proxy with /og excluded"
      contains: "export default clerkMiddleware()"
    - path: "src/app/layout.tsx"
      provides: "<ClerkProvider> inside <body> wrapping NuqsAdapter; SiteHeader mounted"
      contains: "<ClerkProvider"
    - path: "src/components/site-header.tsx"
      provides: "Server header with Show signed-in/out affordances"
      exports: ["SiteHeader"]
    - path: "src/app/privacy/page.tsx"
      provides: "Static privacy policy (ACCT-04)"
      min_lines: 120
    - path: "src/lib/site.ts"
      provides: "PRIVACY_CONTACT_EMAIL and PRIVACY_LAST_UPDATED constants"
      contains: "PRIVACY_CONTACT_EMAIL"
    - path: "tests/privacy-page.test.ts"
      provides: "SSR render + source-scan assertions for /privacy"
      min_lines: 60
  key_links:
    - from: "src/app/layout.tsx"
      to: "@clerk/nextjs ClerkProvider"
      via: "ClerkProvider as the outermost child of <body>, no dynamic prop"
      pattern: "<ClerkProvider afterSignOutUrl=\"/\">"
    - from: "src/components/site-header.tsx"
      to: "/account"
      via: "Show when=\"signed-in\" link"
      pattern: "href=\"/account\""
    - from: "src/components/site-footer.tsx"
      to: "/privacy"
      via: "next/link"
      pattern: "href=\"/privacy\""
    - from: "src/app/privacy/page.tsx"
      to: "src/lib/site.ts"
      via: "PRIVACY_CONTACT_EMAIL mailto + PRIVACY_LAST_UPDATED"
      pattern: "PRIVACY_CONTACT_EMAIL"
---

<objective>
Bolt Clerk onto the request path and the layout without changing what a guest experiences, and ship the legal surface: `src/proxy.ts` (public by default, `/og` excluded), `<ClerkProvider>` inside `<body>`, a site header with sign-in / account affordances, the static `/privacy` page, and its footer link.

Purpose: `auth()` throws without the proxy, and every later plan's Server Action and page reads it. The privacy page is the consent target configured in the Clerk Dashboard (06-01) and must exist before the deploy in 06-07.
Output: `src/proxy.ts`, modified `layout.tsx`, `site-header.tsx`, `site-footer.tsx` (+Privacy), `src/lib/site.ts` (+contact constants), `src/app/privacy/page.tsx`, `tests/privacy-page.test.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accounts-legal/06-RESEARCH.md
@.planning/phases/06-accounts-legal/06-PATTERNS.md
@.planning/phases/06-accounts-legal/06-01-SUMMARY.md
@.planning/phases/04-core-experience/04-UI-SPEC.md

<interfaces>
From @clerk/nextjs 7.9.x (RESEARCH §Pattern 1/2, Context7-verified):
```typescript
// "@clerk/nextjs/server"
export function clerkMiddleware(): NextMiddleware;           // protects nothing until auth.protect() is called
// "@clerk/nextjs" (client-safe, usable inside server components)
export function ClerkProvider(props: { children; afterSignOutUrl?: string; dynamic?: boolean /* NEVER set */ }): JSX.Element;
export function Show(props: { when: "signed-in" | "signed-out"; children }): JSX.Element;
export function SignInButton(props: { mode?: "modal" | "redirect"; children? }): JSX.Element;
export function UserButton(props?: {}): JSX.Element;
```

From src/app/layout.tsx (current body):
```tsx
<body className="flex min-h-full flex-col">
  <NuqsAdapter>
    {children}
    <SiteFooter />
  </NuqsAdapter>
</body>
```
STATE.md gate: `grep -c NuqsAdapter src/app/layout.tsx` reads 3 (import + open + close) and must still read 3.

From src/lib/site.ts: `export const SITE_URL`, `export const SITE_HOST` (module-scope constants; no React).

From src/app/methodology/page.tsx (the analog for /privacy): `export const metadata: Metadata`, module consts `SECTION_CLASS`, `HEADING_CLASS`, `BODY_CLASS`, `MUTED_CLASS`, `LABEL_CLASS`, `LINK_CLASS`; shell `<main className="bg-cream flex flex-1 flex-col"><article className="mx-auto flex w-full max-w-3xl flex-col gap-8 px-4 py-12 sm:px-6 md:py-16">`; h1 `font-display text-ink text-display font-semibold`; one `<section className={SECTION_CLASS}>` per `<h2 className={HEADING_CLASS}>`; trailing `<Link href="/" className={LINK_CLASS}>Back to your results</Link>`.

From tests/methodology-page.test.ts: `renderToStaticMarkup(createElement(Page)).replace(/&#x27;/g, "'")`, ordered-h2 cursor assertion, source-scan describe (no "use client", searchParams, export const dynamic, new Date, @/db).

Privacy contact: read the line `PRIVACY_CONTACT_EMAIL: <address>` from 06-01-SUMMARY.md. If the SUMMARY also says "terms-required" (A8), additionally create `src/app/terms/page.tsx` as a short static page in the same shape (title "Terms — Points Unlocked"; sections "Educational only", "No warranty", "Contact") and add it to the footer — otherwise do not create it.

Local build needs the Clerk publishable key: `next build` does not load `.env.development.local`, so export the two Clerk vars into the shell for the build command (values never printed): `set -a; . ./.env.development.local; set +a; npm run build`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: src/proxy.ts + ClerkProvider in layout + SiteHeader</name>
  <files>src/proxy.ts, src/app/layout.tsx, src/components/site-header.tsx</files>
  <read_first>
    - src/app/layout.tsx (entire file — import order, the Pitfall 1 NuqsAdapter comment, metadata block that must not change)
    - src/components/site-footer.tsx (entire file — the exact server-component shape and classes the header mirrors)
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Pattern 1 (proxy + matcher, verbatim), §Pattern 2 (ClerkProvider inside body, Show/SignInButton/UserButton), §Pitfall 4, §Pitfall 8
    - .planning/phases/04-core-experience/04-UI-SPEC.md §Color (terracotta reserved uses) and the touch-target rule
  </read_first>
  <action>
    1. Create `src/proxy.ts` (Next 16 convention — a file named `middleware.ts` would not run): `import { clerkMiddleware } from "@clerk/nextjs/server";` then `export default clerkMiddleware();` and `export const config = { matcher: [...] }` using RESEARCH Pattern 1's three matcher entries verbatim — the first entry skips `_next`, static-file extensions, and `og(?:$|\?)` (Pitfall 8: the crawler-fetched, CDN-cached PNG must never see Clerk cookies or a handshake redirect); the second is `"/(api|trpc)(.*)"`; the third is `"/__clerk/(.*)"`. Header comment: ACCT-01 — public by default, nothing calls `auth.protect()`, so every route works signed out; T-06-08 for the `/og` exclusion. No other exports, no `runtime` export (proxy is Node-only).
    2. `src/app/layout.tsx`: add `import { ClerkProvider } from "@clerk/nextjs";` in the framework import group (after the nuqs adapter import) and `import { SiteHeader } from "@/components/site-header";` in the components group (alphabetical, before SiteFooter). Restructure the body to `<body className="flex min-h-full flex-col"><ClerkProvider afterSignOutUrl="/"><NuqsAdapter><SiteHeader />{children}<SiteFooter /></NuqsAdapter></ClerkProvider></body>`. ClerkProvider is the outermost child of `<body>` (Clerk Core 3 requirement) and carries NO `dynamic` prop (it would un-prerender /methodology and /privacy — Pitfall 4). Add a comment saying exactly that, plus that `afterSignOutUrl="/"` makes the UserButton sign-out land on the guest flow. Leave `metadata`, fonts, and the html element untouched. The NuqsAdapter grep gate must still read 3.
    3. Create `src/components/site-header.tsx` — server component, NO "use client" (Show/SignInButton/UserButton are client-safe imports): `import Link from "next/link"; import { Show, SignInButton, UserButton } from "@clerk/nextjs";`. Header comment: ACCT-01 sign-in affordance; mirrors site-footer.tsx; accent discipline ink only (no terracotta); T-06-05 — never import `@clerk/nextjs/server` or `@/db` here. Render `<header className="border-ink/10 border-b">` → `<div className="mx-auto flex w-full max-w-3xl items-center justify-between px-4 py-4 sm:px-6">` → left: `<Link href="/" className="text-ink text-sm font-semibold">Points Unlocked</Link>`; right: `<nav aria-label="Account" className="flex items-center gap-4">` containing `<Show when="signed-out"><SignInButton mode="modal"><button type="button" className="text-ink/70 h-11 text-sm leading-5 underline-offset-4 hover:underline">Sign in</button></SignInButton></Show>` and `<Show when="signed-in"><Link href="/account" className="text-ink/70 text-sm leading-5 underline-offset-4 hover:underline">My account</Link><UserButton /></Show>`. Export `SiteHeader`. Under 50 lines.
    4. Build gate with Clerk env exported into the shell (values never printed): `set -a; . ./.env.development.local; set +a; npm run build`. The route table must show `○ /methodology` and `ƒ /` and `ƒ /og`, and the build must list the proxy (Next 16 prints a `ƒ Proxy` line). If the build fails with a missing publishable key, the env export did not work — fix the shell step, do not hardcode anything.
  </action>
  <verify>
    <automated>test "$(grep -c 'export default clerkMiddleware()' src/proxy.ts)" = "1" && test "$(grep -c 'auth.protect' src/proxy.ts)" = "0" && grep -q 'og(?:\$|' src/proxy.ts && test ! -f src/middleware.ts && test ! -f middleware.ts && test "$(grep -c NuqsAdapter src/app/layout.tsx)" = "3" && test "$(grep -c '<ClerkProvider afterSignOutUrl="/">' src/app/layout.tsx)" = "1" && ! grep -q '<ClerkProvider dynamic' src/app/layout.tsx && test "$(grep -c '"use client"' src/components/site-header.tsx)" = "0" && test "$(grep -c 'href="/account"' src/components/site-header.tsx)" = "1" && ! grep -qE 'terracotta|@clerk/nextjs/server|from "@/db' src/components/site-header.tsx && npm run typecheck && npm run lint && (set -a; . ./.env.development.local; set +a; npm run build 2>&1 | grep -E '○ /methodology|ƒ /og' | wc -l | grep -q '^2$')</automated>
  </verify>
  <acceptance_criteria>
    - `src/proxy.ts` exists; `grep -c 'export default clerkMiddleware()' src/proxy.ts` outputs `1`; `grep -c 'auth.protect' src/proxy.ts` outputs `0`; `grep -c '/(api|trpc)(.*)' src/proxy.ts` outputs `1`; the first matcher entry contains `og(?:$|\?)`; `src/middleware.ts` and `middleware.ts` do not exist
    - `grep -c NuqsAdapter src/app/layout.tsx` outputs `3`; `grep -c '<ClerkProvider afterSignOutUrl="/">' src/app/layout.tsx` outputs `1`; `grep -c '<ClerkProvider dynamic' src/app/layout.tsx` outputs `0`; `grep -c '<SiteHeader />' src/app/layout.tsx` outputs `1` and it appears before `{children}`
    - `grep -c '"use client"' src/components/site-header.tsx` outputs `0`; `grep -c 'Show when="signed-out"' src/components/site-header.tsx` and `'Show when="signed-in"'` each output `1`; `grep -c 'mode="modal"' src/components/site-header.tsx` outputs `1`; `grep -cE 'terracotta|@clerk/nextjs/server|from "@/db' src/components/site-header.tsx` outputs `0`
    - `npm run typecheck`, `npm run lint` exit 0; the env-exported `npm run build` exits 0 and its route table contains `○ /methodology`, `ƒ /`, `ƒ /og`
    - `npx vitest run` exits 0 (no regression)
  </acceptance_criteria>
  <done>Clerk runs on every request without gating anything, the layout provides Clerk context beneath body, and every page carries a sign-in / account affordance in the header.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: /privacy static page + SSR test + footer link + contact constants</name>
  <files>src/lib/site.ts, src/app/privacy/page.tsx, tests/privacy-page.test.ts, src/components/site-footer.tsx</files>
  <behavior>
    - Rendering `PrivacyPage` with `renderToStaticMarkup` yields exactly one `<h1>` containing "Privacy" and exactly seven `<h2>` headings in this order: "What we collect", "Who processes it", "What we never do", "Cookies", "Retention and deletion", "Consent", "Children and changes"
    - The HTML contains "Clerk", "Neon", "Vercel", "pu:balances:v1", "Delete my account", `href="/account"`, `href="/methodology"`, `href="mailto:` followed by the PRIVACY_CONTACT_EMAIL value, and a "Last updated" line containing PRIVACY_LAST_UPDATED; it contains no "terracotta" class and no "!" in prose
    - Source scan of `src/app/privacy/page.tsx`: no `"use client"`, no `searchParams`, no `export const dynamic`, no `new Date`, no `from "@/db`, no `@clerk/nextjs/server`
    - `src/components/site-footer.tsx` contains `href="/privacy"` exactly once
  </behavior>
  <read_first>
    - .planning/phases/06-accounts-legal/06-01-SUMMARY.md (the `PRIVACY_CONTACT_EMAIL:` line and whether "terms-required" was reported)
    - src/app/methodology/page.tsx (entire file — the exact shape to copy: imports, metadata, class consts, shell, sections, back link)
    - tests/methodology-page.test.ts (entire file — render-once, ordered headings, source scan)
    - src/components/site-footer.tsx (the Methodology link block to duplicate)
    - src/lib/site.ts (entire file — constant style)
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Privacy Content items 1-7 (the content checklist) and Assumptions A7/A8
    - src/db/schema.ts header comment (what is stored per table — the page must describe exactly these)
    - src/lib/balance-storage.ts line with STORAGE_KEY (the localStorage key name to cite)
  </read_first>
  <action>
    Write the test first (RED), then the page (GREEN).

    1. `src/lib/site.ts`: append `export const PRIVACY_CONTACT_EMAIL = "<address from 06-01-SUMMARY>";` and `export const PRIVACY_LAST_UPDATED = "2026-09-03";` with a comment that the privacy page renders the date from this constant because the static-route source scan forbids `new Date` (T-05-05 pattern). If no address is recorded in the SUMMARY, use the output of `git config user.email` and flag the assumption in this plan's SUMMARY.
    2. `src/app/privacy/page.tsx` — copy the methodology file shape: `import type { Metadata } from "next"; import Link from "next/link"; import { PRIVACY_CONTACT_EMAIL, PRIVACY_LAST_UPDATED } from "@/lib/site";`. Header comment: ACCT-04 privacy policy; static server component (no client directive, no request input, no clock, no DB, no `@clerk/nextjs/server`) so it prerenders (T-06-16); plain-language, not legal advice (A7); accent discipline ink only. `export const metadata: Metadata = { title: "Privacy — Points Unlocked", description: "What Points Unlocked stores, who processes it, and how to delete your account and data." }`. Reuse the six class constants verbatim. h1: "Privacy". Intro paragraph (MUTED_CLASS): the guest flow needs no account and sends nothing to our server; an optional account stores only what is listed below; then `Last updated {PRIVACY_LAST_UPDATED}` as a LABEL_CLASS line. Seven sections with these exact h2 strings and this content:
       - "What we collect" — three labelled blocks (LABEL_CLASS + BODY_CLASS): Guests: balances you type live in the page URL and in your browser's local storage under the key `pu:balances:v1`; they reach our server only when you open a share link (the URL) or sign in and choose Save. Signed-in: your Clerk account (email address, optional name, sign-in method), the balances you save, the redemptions you bookmark, and the travel goals you write. Waitlist: the email you enter in the advisor "coming soon" form, stored separately from accounts.
       - "Who processes it" — Clerk (authentication; links to https://clerk.com/legal/dpa and https://clerk.com/legal/subprocessors), Neon (Postgres database, AWS us-east-2), Vercel (hosting and request logs). Each as a `<a href target="_blank" rel="noreferrer">` in LINK_CLASS where a URL is given.
       - "What we never do" — no analytics or advertising trackers; no selling or sharing data for marketing; never ask for card numbers, logins, or credentials for any points program (manual entry is the privacy feature); no affiliate links.
       - "Cookies" — only Clerk's strictly necessary session cookies (`__session`, `__client_uat`) when you sign in; no cookie banner because nothing optional is set (A7).
       - "Retention and deletion" — account data is kept until you delete your account: open `<Link href="/account">Your account</Link>` and choose "Delete my account" — immediate and irreversible, removes saved balances, bookmarks, goals, and your sign-in. Waitlist emails are removed on request. Contact: `<a href={`mailto:${PRIVACY_CONTACT_EMAIL}`} className={LINK_CLASS}>{PRIVACY_CONTACT_EMAIL}</a>`.
       - "Consent" — you accept this policy with the checkbox at sign-up (Clerk records the timestamp); using the guest flow requires no account and no acceptance.
       - "Children and changes" — not directed at children under 13; changes are posted here with a new "Last updated" date.
       End with `<Link href="/methodology" className={LINK_CLASS}>How we value your points</Link>` and `<Link href="/" className={LINK_CLASS}>Back to your results</Link>`. Use `&apos;` for apostrophes; no exclamation marks; no `new Date`; under 260 lines.
    3. `tests/privacy-page.test.ts`: copy `tests/methodology-page.test.ts` structure — import `PrivacyPage from "../src/app/privacy/page"` and the two constants from `../src/lib/site`; render once; `SECTION_HEADINGS` tuple with the seven strings; ordered-h2 cursor test; `toHaveLength(7)` on `<h2` matches; content `toContain` assertions from `<behavior>`; `not.toContain("terracotta")`; a `describe("/privacy source scan (T-06-16 static, DB-free, auth-free route)")` block reading the page source and asserting each forbidden token from `<behavior>` is absent; a final `it` that reads `src/components/site-footer.tsx` and asserts `(source.match(/href="\/privacy"/g) ?? []).length === 1`.
    4. `src/components/site-footer.tsx`: duplicate the Methodology `<Link>` block directly after it with `href="/privacy"` and the label `Privacy`; update the header comment to mention ACCT-04.
    5. If 06-01-SUMMARY reports "terms-required": also create `src/app/terms/page.tsx` (same shape; h1 "Terms"; sections "Educational only", "No warranty", "Contact"; metadata title "Terms — Points Unlocked") and a `Terms` footer link; add it to `files_modified` in the SUMMARY. Otherwise skip.
    6. Build gate (env exported as in Task 1): route table must show `○ /privacy` alongside `○ /methodology`.
  </action>
  <verify>
    <automated>npx vitest run tests/privacy-page.test.ts tests/methodology-page.test.ts && test "$(grep -c 'href="/privacy"' src/components/site-footer.tsx)" = "1" && test "$(grep -c '^export const PRIVACY_CONTACT_EMAIL' src/lib/site.ts)" = "1" && ! grep -qE '"use client"|searchParams|export const dynamic|new Date|from "@/db|@clerk/nextjs/server' src/app/privacy/page.tsx && npm run typecheck && npm run lint && (set -a; . ./.env.development.local; set +a; npm run build 2>&1 | grep -E '○ /privacy|○ /methodology' | wc -l | grep -q '^2$')</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/privacy/page.tsx` exists; `grep -c 'export const metadata' src/app/privacy/page.tsx` outputs `1`; `grep -c '<h2' src/app/privacy/page.tsx` outputs `7`
    - `grep -cE '"use client"|searchParams|export const dynamic|new Date|from "@/db|@clerk/nextjs/server' src/app/privacy/page.tsx` outputs `0`
    - `grep -c 'pu:balances:v1' src/app/privacy/page.tsx` ≥ 1; `grep -c 'Delete my account' src/app/privacy/page.tsx` ≥ 1; `grep -c 'PRIVACY_CONTACT_EMAIL' src/app/privacy/page.tsx` ≥ 2 (import + use)
    - `grep -c '^export const PRIVACY_CONTACT_EMAIL = ".\+@.\+";' src/lib/site.ts` outputs `1`; `grep -c '^export const PRIVACY_LAST_UPDATED = "20' src/lib/site.ts` outputs `1`
    - `grep -c 'href="/privacy"' src/components/site-footer.tsx` outputs `1`
    - `npx vitest run tests/privacy-page.test.ts` exits 0 with ≥ 10 tests; the full `npx vitest run` exits 0
    - Env-exported `npm run build` route table contains `○ /privacy` and `○ /methodology`; `npm run typecheck`, `npm run lint` exit 0
  </acceptance_criteria>
  <done>The privacy policy is live as a static page that names every store and processor the app actually has, links to the deletion path, and is reachable from every page via the footer.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Every HTTP request → src/proxy.ts | Clerk verifies session cookies before any route; the matcher decides which paths it touches |
| Server component tree → client bundle | ClerkProvider, Show, SignInButton, UserButton ship client code; the secret key must not |
| Public /privacy page → visitors | Legal disclosure must match what the schema actually stores |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-05 | Information disclosure | Client bundle (header, layout) | mitigate | `site-header.tsx` imports only `@clerk/nextjs` client-safe components; `@clerk/nextjs/server` and `@/db` never appear under `src/components/**` (asserted here by grep, enforced phase-wide by tests/guest-flow-gate.test.ts in 06-04); `CLERK_SECRET_KEY` is read only by Clerk server code |
| T-06-08 | Denial of service / Tampering | /og CDN-cached PNG vs Clerk handshake | mitigate | Matcher excludes `og(?:$|\?)`; production curl gate in 06-07 re-proves `image/png`, no `set-cookie`, `x-vercel-cache: HIT` |
| T-06-16 | Denial of service (availability/cost) | Static routes turning dynamic | mitigate | No `dynamic` prop on ClerkProvider; no `auth()` in `/methodology`, `/privacy`, `/og`; build route table gate `○ /methodology`, `○ /privacy`; privacy source scan forbids `@clerk/nextjs/server` |
| T-06-10 | Repudiation / Privacy | /privacy disclosure accuracy | mitigate | Content enumerates exactly the stores in schema.ts (Clerk account, user_balances, bookmarks, travel_goals, interest_signups, localStorage key) and the three processors; consent and deletion paths named; test pins the section list |
| T-06-01 | Spoofing | Sign-in UI | transfer | Clerk-hosted modal handles credentials, codes, and OAuth; the app never renders a credential form |
| T-06-SC | Tampering | npm installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- `npx vitest run` exits 0 (privacy + methodology + all prior suites)
- Env-exported `npm run build` route table: `○ /methodology`, `○ /privacy`, `ƒ /`, `ƒ /og`
- `grep -rlE '@clerk/nextjs/server|from "@/db' src/components` outputs nothing
</verification>

<success_criteria>
- Guest flow unchanged under Clerk: no route protected, /og outside the proxy, static routes still static
- Header shows Sign in / My account per session state on every page
- /privacy exists, is static, tested, and linked from the footer with a real contact address
</success_criteria>

<output>
Create `.planning/phases/06-accounts-legal/06-03-SUMMARY.md` when done. Record the contact address used and whether /terms was created.
</output>
