---
phase: 06-accounts-legal
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/db/schema.ts
  - package.json
  - package-lock.json
  - .planning/phases/06-accounts-legal/06-VALIDATION.md
autonomous: false
requirements: [ACCT-01, ACCT-04]
user_setup:
  - service: clerk
    why: "Authentication for optional accounts (ACCT-01); consent capture and account deletion policy (ACCT-04)"
    env_vars:
      - name: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard -> Configure -> Developers -> API keys (pk_test_… on the development instance)"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard -> Configure -> Developers -> API keys (sk_test_…)"
    dashboard_config:
      - task: "Enable 'Require express consent to legal documents' and set the Privacy policy URL to https://points-unlocked.vercel.app/privacy"
        location: "Clerk Dashboard -> Configure -> Legal"
      - task: "Turn OFF 'Allow users to delete their accounts'"
        location: "Clerk Dashboard -> Configure -> User & authentication -> User model (Restrictions)"
      - task: "Add both keys to the Vercel project for Production, Preview, and Development"
        location: "Vercel -> points-unlocked -> Settings -> Environment Variables (or the Clerk <-> Vercel Marketplace integration)"

must_haves:
  truths:
    - "npx drizzle-kit push exits 0, and an immediately repeated push prints 'No changes detected' (the Phase 5 deferred blocker is closed)"
    - "@clerk/nextjs 7.9.x is installed and pinned in package.json; no svix, no global clerk CLI"
    - "Clerk keys exist locally in .env.development.local (gitignored) and on Vercel for Production + Preview; nothing secret is printed or committed"
    - "Clerk Dashboard has consent-to-legal-documents ON with the /privacy URL and self-serve account deletion OFF"
    - "06-VALIDATION.md is a filled contract (framework, commands, per-task map, Wave 0 list, manual-only table), not the template"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "Explicitly named transfer_bonuses FK + PG18-ordered transfer_routes composite PK (Pitfall 1 fix)"
      contains: "transfer_bonuses_route_fk"
    - path: "package.json"
      provides: "@clerk/nextjs dependency"
      contains: "@clerk/nextjs"
    - path: ".planning/phases/06-accounts-legal/06-VALIDATION.md"
      provides: "Filled Nyquist validation contract for Phase 6"
      contains: "tests/guest-flow-gate.test.ts"
  key_links:
    - from: "src/db/schema.ts"
      to: "live Neon constraint transfer_bonuses_route_fk"
      via: "drizzle-kit push (TTY, human-run) renames the truncated 63-char FK"
      pattern: "name: \"transfer_bonuses_route_fk\""
    - from: ".env.development.local"
      to: "Vercel env (Production + Preview)"
      via: "vercel env pull after the human adds the Clerk keys"
      pattern: "NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY"
---

<objective>
Wave 0 for Phase 6: unblock the database push that Phase 5 deferred, install the one new runtime package, provision Clerk (keys on Vercel + locally, Dashboard consent and deletion settings), and fill the phase validation contract. Every later plan assumes these four facts.

Purpose: `drizzle-kit push` currently exits 1 on every run (deferred-items.md); the Phase 6 tables cannot be pushed until the constraint churn is gone. `<ClerkProvider>` in the root layout makes every build and request depend on Clerk keys, so the keys must exist on Vercel before plan 06-03 deploys.
Output: fixed `src/db/schema.ts` constraints (live DB renamed, second push no-op), `@clerk/nextjs@^7.9.1` in package.json, Clerk keys in `.env.development.local` + Vercel, Dashboard settings applied, `06-VALIDATION.md` filled (flags still false).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accounts-legal/06-RESEARCH.md
@.planning/phases/06-accounts-legal/06-PATTERNS.md
@.planning/phases/05-credibility-layer/deferred-items.md

<interfaces>
From src/db/schema.ts (current, the two blocks to change):
```typescript
// transferRoutes third argument — line ~71
(t) => [primaryKey({ columns: [t.fromProgramSlug, t.toProgramSlug] })],

// transferBonuses third argument — lines ~87-95
(t) => [
  foreignKey({
    columns: [t.fromProgramSlug, t.toProgramSlug],
    foreignColumns: [transferRoutes.fromProgramSlug, transferRoutes.toProgramSlug],
  }),
],
```

Live Neon facts (RESEARCH Pitfall 1, verified 2026-09-03): PostgreSQL 18.6; stored FK name is the 63-char truncation `transfer_bonuses_from_program_slug_to_program_slug_transfer_rou`; drizzle-kit 0.31.10 introspects the composite PK as `[toProgramSlug, fromProgramSlug]`.

Package Legitimacy Audit (RESEARCH): `@clerk/nextjs` 7.9.1 — Approved [VERIFIED: npm registry, repo github.com/clerk/javascript, slopcheck OK, no postinstall]. No [ASSUMED] or [SUS] packages in this phase; no legitimacy checkpoint required.

Validation template to fill: .planning/phases/06-accounts-legal/06-VALIDATION.md (currently the unfilled template with `nyquist_compliant: false`, `wave_0_complete: false`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix the push-churn constraints, install @clerk/nextjs, fill the validation contract</name>
  <files>src/db/schema.ts, package.json, package-lock.json, .planning/phases/06-accounts-legal/06-VALIDATION.md</files>
  <read_first>
    - src/db/schema.ts (entire file — the `transferRoutes` primaryKey line and the `transferBonuses` foreignKey block are the only edits; header comment style)
    - .planning/phases/05-credibility-layer/deferred-items.md (symptom + root cause of the push failure)
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Pitfall 1, §Package Legitimacy Audit, §Validation Architecture (the test map rows to transcribe)
    - .planning/phases/06-accounts-legal/06-VALIDATION.md (the template being filled)
    - .planning/phases/05-credibility-layer/05-VALIDATION.md (a filled example from the previous phase — copy its shape)
    - package.json (dependency block; exact-version house style for runtime deps added by earlier phases)
  </read_first>
  <action>
    1. `src/db/schema.ts` — two edits and nothing else (the Phase 6 tables are added in plan 06-02, after the push is proven clean):
       - In the `transferRoutes` table's third argument, reorder the composite primary key to `primaryKey({ columns: [t.toProgramSlug, t.fromProgramSlug] })`. Add a one-line comment above it citing RESEARCH Pitfall 1: drizzle-kit 0.31.10 introspects composite-PK columns from an unordered information_schema query and PostgreSQL 18 returns them (to, from); matching that order removes the DROP/ADD churn. Column order of a PK constraint is semantically irrelevant to the app and the seed.
       - In the `transferBonuses` table's `foreignKey({...})` call, add `name: "transfer_bonuses_route_fk"` as the first property, with a comment: the auto-generated name is 100 chars, Postgres truncates identifiers to 63, so every push saw a rename (deferred-items.md).
       - Do not touch `interestSignups`, `programs`, `redemptions`, or the import block.
    2. Install the auth package: `npm install @clerk/nextjs@^7.9.1` (audit disposition: Approved). Confirm `npm ls @clerk/nextjs` resolves 7.9.x and that `npm view @clerk/nextjs@7.9.1 scripts.postinstall` prints nothing. Do NOT install `svix` (verifyWebhook is bundled) or the `clerk` CLI (npx-only, optional, unused in v1).
    3. Fill `.planning/phases/06-accounts-legal/06-VALIDATION.md` (keep both frontmatter flags `false`; plan 06-04 flips `wave_0_complete`, plan 06-07 flips `nyquist_compliant`):
       - Test Infrastructure: Vitest 4.1.11, `environment: "node"` (no jsdom), config `vitest.config.ts` (alias `@` → `src`), quick run `npx vitest run tests/<file>.test.ts`, full suite `npm test && npm run typecheck && npm run lint && npm run build`, runtime ~3s (186 tests green pre-phase).
       - Sampling Rate: after every task commit run the touched test file + `npm run typecheck`; after every wave run the full suite command; before verify-work the full suite must be green plus the second-push no-op evidence and the production curl gate.
       - Per-Task Verification Map: one row per task of plans 06-01 through 06-07 using the task IDs `6-NN-T` (plan NN, task T), the requirement each covers (ACCT-01..04), the threat IDs named in each plan's threat_model, the secure behavior, the test type, and the automated command. Use the RESEARCH §Phase Requirements → Test Map as the source of the commands; mark files that do not exist yet as `❌ W0` and existing ones `✅`.
       - Wave 0 Requirements: `tests/account-validation.test.ts` (06-02), `tests/balance-storage.test.ts` extension (06-02), `tests/privacy-page.test.ts` (06-03), `tests/account-data.test.ts` (06-04), `tests/account-actions.test.ts` (06-04), `tests/guest-flow-gate.test.ts` (06-04), plus the two human gates in this plan (TTY push, Clerk keys/Dashboard). Note: no framework install needed.
       - Manual-Only Verifications: TTY push gate (drizzle-kit prompts on constraint renames); modal sign-up with consent checkbox (Clerk UI cannot run in node); cross-device balance restore; bookmark/goal persistence across sign-out/sign-in; delete flow ending signed out on `/` with `users rows: 0`; Clerk Dashboard settings (consent on, self-serve delete off); `/og` production curl (no set-cookie, x-vercel-cache HIT). Each with the requirement ID and the step list from plan 06-07 Task 2.
       - Leave "Approval: pending".
    4. `npm run typecheck` must pass. Do not run `drizzle-kit push` in this task — the rename needs a TTY (Task 2).
  </action>
  <verify>
    <automated>test "$(grep -c 'name: "transfer_bonuses_route_fk"' src/db/schema.ts)" = "1" && test "$(grep -c 'primaryKey({ columns: \[t.toProgramSlug, t.fromProgramSlug\] })' src/db/schema.ts)" = "1" && test "$(grep -c 'pgTable(' src/db/schema.ts)" = "5" && npm ls @clerk/nextjs 2>/dev/null | grep -E '@clerk/nextjs@7\.9\.[0-9]+' && test "$(grep -c '"svix"' package.json)" = "0" && grep -c 'tests/guest-flow-gate.test.ts' .planning/phases/06-accounts-legal/06-VALIDATION.md | grep -vq '^0$' && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'name: "transfer_bonuses_route_fk"' src/db/schema.ts` outputs `1`
    - `grep -c 'primaryKey({ columns: \[t.toProgramSlug, t.fromProgramSlug\] })' src/db/schema.ts` outputs `1` and `grep -c 'columns: \[t.fromProgramSlug, t.toProgramSlug\] })\]' src/db/schema.ts` outputs `0`
    - `grep -c 'pgTable(' src/db/schema.ts` outputs `5` (no new tables in this plan)
    - `npm ls @clerk/nextjs` resolves `@clerk/nextjs@7.9.x`; `grep -c '"@clerk/nextjs"' package.json` outputs `1`; `grep -c '"svix"' package.json` outputs `0`
    - `06-VALIDATION.md` no longer contains the template placeholder `{pytest 7.x / jest 29.x / vitest / go test / other}`; it names `tests/account-validation.test.ts`, `tests/account-actions.test.ts`, `tests/privacy-page.test.ts`, `tests/guest-flow-gate.test.ts`, `tests/account-data.test.ts`; frontmatter still reads `nyquist_compliant: false` and `wave_0_complete: false`
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Schema constraints carry a short explicit FK name and the PG18 PK column order; @clerk/nextjs 7.9.x is installed; the validation contract is filled with every Phase 6 test file and manual gate named.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: TTY drizzle-kit push gate — rename the constraint once, prove the second push is a no-op</name>
  <files></files>
  <read_first>
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Pitfall 1 steps 1-4 (expected statements, abort rule, manual RENAME CONSTRAINT fallback)
    - .planning/phases/05-credibility-layer/deferred-items.md (the exact stored constraint names)
    - drizzle.config.ts (loads `.env.development.local`)
  </read_first>
  <action>Before pausing: confirm `grep -c '^DATABASE_URL=' .env.development.local` prints `1` (if 0 or the file is missing, run `vercel env pull .env.development.local` first) and `git check-ignore -q .env.development.local` exits 0. Then pause — drizzle-kit refuses constraint renames without a TTY, so the human runs the push. After the human resumes, run `npx drizzle-kit push` yourself once more non-interactively and require the literal "No changes detected" in its output; paste the human's two outputs (with any secrets redacted — the URL is never printed by drizzle-kit, but check) into the SUMMARY under "Push gate evidence".</action>
  <instructions>
    1. In a real terminal at the repo root (PowerShell or Git Bash — not a piped/CI shell) run: `npx drizzle-kit push --verbose`
    2. Read the previewed statements. EXPECTED: `ALTER TABLE "transfer_bonuses" DROP CONSTRAINT "transfer_bonuses_from_program_slug_to_program_slug_transfer_rou"` followed by `ADD CONSTRAINT "transfer_bonuses_route_fk" FOREIGN KEY (...)`, and NOTHING that mentions `"transfer_routes"`. If that is what you see, confirm the prompt (Yes / apply) and let it finish with exit 0.
    3. ABORT (answer No / Ctrl+C) if the preview contains any `ALTER TABLE "transfer_routes"` line (a PK DROP/ADD) — the 2BP01 failure would recur. Fallback in that case: in the Neon SQL editor run `ALTER TABLE transfer_bonuses RENAME CONSTRAINT "transfer_bonuses_from_program_slug_to_program_slug_transfer_rou" TO transfer_bonuses_route_fk;` then re-run step 1. If the PK churn persists after the FK is renamed, stop and paste the preview verbatim when you resume — the executor will swap the PK column order back and retry once, then escalate.
    4. Run `npx drizzle-kit push` a second time. EXPECTED: it prints `No changes detected` (or equivalent "nothing to push") and exits 0.
    5. Copy both terminal outputs (steps 1-2 and step 4) into your reply.
  </instructions>
  <verification>Executor re-runs `npx drizzle-kit push` non-interactively and greps for "No changes detected"; runs `npx tsx scripts/db-check.ts` and confirms `programs rows: 21` and `interest_signups rows: N` still print (seed rows intact).</verification>
  <resume-signal>Reply "push-clean" followed by the two pasted outputs, or "push-blocked" with the preview text</resume-signal>
  <verify>
    <automated>npx drizzle-kit push 2>&1 | grep -q "No changes detected" && npx tsx scripts/db-check.ts | grep -E '^programs rows: [1-9][0-9]*$'</automated>
  </verify>
  <acceptance_criteria>
    - Human resume signal is "push-clean" with two pasted outputs; the first shows the FK rename and no `ALTER TABLE "transfer_routes"` statement
    - Executor's own `npx drizzle-kit push` run prints `No changes detected` and exits 0
    - `npx tsx scripts/db-check.ts` prints `programs rows: 21` (seed rows untouched) and an `interest_signups rows:` line
    - Both outputs are recorded in 06-01-SUMMARY.md under "Push gate evidence"; no connection string appears anywhere in the repo or SUMMARY (`git grep -c 'postgresql://' -- . ':!package-lock.json'` outputs `0`)
  </acceptance_criteria>
  <done>The live database carries `transfer_bonuses_route_fk`, drizzle-kit's diff against schema.ts is empty, and the Phase 5 deferred blocker is closed with pasted evidence.</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Provision Clerk — app + keys on Vercel and locally, consent ON, self-serve delete OFF, privacy contact address</name>
  <files></files>
  <read_first>
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Pitfall 2 (development instance on *.vercel.app — A1), §Pitfall 3 (keys must exist on Vercel for Production AND Preview), §Privacy Content (Dashboard Legal settings, A8), §Pattern 9 (why self-serve delete is turned off), §Open Questions 1 and 3
    - .planning/phases/01-foundation/*-SUMMARY.md (how DATABASE_URL was provisioned: Marketplace injection + `vercel env pull` — same pattern)
  </read_first>
  <action>Before pausing, print the three Dashboard locations and the exact env var names from the instructions so the human can work from one screen. Pause. After the human resumes with the contact address: run `vercel env pull .env.development.local` (overwrites the file with all Vercel Development-scope vars), then verify with grep -c that BOTH `^NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=` and `^CLERK_SECRET_KEY=` lines exist, that the publishable value starts with `pk_test_` (check with `grep -c '^NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="\?pk_test_'`), and that `DATABASE_URL` is still present. NEVER print, echo, or paste a key value; never write keys into any tracked file. Record in the SUMMARY under "Decisions": the Clerk instance type (development, A1 accepted), the consent/legal setting state, the self-serve-delete state, whether a Terms URL was required (A8), and the confirmed privacy contact address as `PRIVACY_CONTACT_EMAIL: <address>` on its own line — plan 06-03 reads it from there.</action>
  <instructions>
    1. Create a Clerk application at https://dashboard.clerk.com (name: Points Unlocked). Enable Email (code) sign-in; enable Google if you want one-click sign-up. This is a DEVELOPMENT instance (`pk_test_`/`sk_test_`) — Clerk production requires a custom domain, and *.vercel.app is not allowed; v1 ships on the dev instance (100-user cap, "Development mode" badge). Production cut-over moves to the Phase 7 launch checklist if you buy a domain.
    2. Add the keys to Vercel — EITHER install the Clerk integration from the Vercel Marketplace on the points-unlocked project (it syncs exactly `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` and `CLERK_SECRET_KEY`), OR copy them manually: Vercel → points-unlocked → Settings → Environment Variables → add both keys for Production, Preview, AND Development environments. Do not paste key values into this chat.
    3. Clerk Dashboard → Configure → Legal (or "Compliance"): turn ON "Require express consent to legal documents"; set Privacy policy URL to `https://points-unlocked.vercel.app/privacy` (the page ships in plan 06-03). If the form refuses to save without a Terms of Service URL, leave consent ON with whatever it requires and tell the executor "terms-required" on resume (A8 — plan 06-03 will then also ship a short /terms page).
    4. Clerk Dashboard → Configure → User & authentication → User model / Restrictions: turn OFF "Allow users to delete their accounts". (The app's own Delete flow cascades the database first; Clerk's built-in delete would orphan rows.)
    5. Decide the contact address for privacy requests (an alias you control, shown publicly on /privacy as a mailto link).
    6. Reply with the resume signal below, including the address.
  </instructions>
  <verification>Executor runs `vercel env pull .env.development.local` and greps for both key names (counts only, values never displayed); confirms `git check-ignore -q .env.development.local` and `git status --porcelain | grep -c '\.env'` = 0.</verification>
  <resume-signal>Reply "clerk-ready contact: <address>" (append "terms-required" if the Dashboard demanded a Terms URL)</resume-signal>
  <verify>
    <automated>test "$(grep -c '^NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=' .env.development.local)" = "1" && test "$(grep -c '^CLERK_SECRET_KEY=' .env.development.local)" = "1" && test "$(grep -c '^NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY="\?pk_test_' .env.development.local)" = "1" && test "$(grep -c '^DATABASE_URL=' .env.development.local)" = "1" && git check-ignore -q .env.development.local && test "$(git status --porcelain | grep -c '\.env')" = "0"</automated>
  </verify>
  <acceptance_criteria>
    - Human resume signal begins with "clerk-ready contact:" and contains an email address
    - `grep -c '^NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY=' .env.development.local` and `grep -c '^CLERK_SECRET_KEY=' .env.development.local` both output `1`; the publishable key line matches `pk_test_`
    - `vercel env ls` (run by the executor) lists both variable names for Production and Preview
    - `git check-ignore .env.development.local` exits 0; `git status --porcelain | grep -c '\.env'` outputs `0`; `git grep -c 'sk_test_\|pk_test_'` outputs `0`
    - 06-01-SUMMARY.md contains a line matching `^PRIVACY_CONTACT_EMAIL: .+@.+$` and records consent ON, self-serve delete OFF, instance = development (A1)
  </acceptance_criteria>
  <done>Clerk exists with keys everywhere the app runs, the Dashboard enforces consent at sign-up and blocks the Clerk-side delete path, and the privacy contact address is recorded for plan 06-03.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local shell → Neon (drizzle-kit push) | `DATABASE_URL` from a gitignored env file; a DDL rename is applied to production data |
| Clerk Dashboard → Vercel env → app | Secret key provisioning; the secret must never enter the repo, logs, or chat |
| npm registry → node_modules | One new runtime dependency crosses the supply-chain boundary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-07 | Information disclosure | Clerk secret key / DATABASE_URL handling | mitigate | Human enters keys in the Clerk/Vercel dashboards, never in chat; executor verifies with `grep -c` counts only; `git check-ignore` + `git status` + `git grep` gates assert nothing secret is tracked (T-05-15 precedent) |
| T-06-11 | Tampering | drizzle-kit push diff against curated tables | mitigate | FK named ≤63 chars + PK column order matched to PG18 introspection; rename applied once under a TTY with the human reading the preview; abort rule on any `transfer_routes` ALTER; second push must print "No changes detected"; `db-check` proves seed rows intact |
| T-06-12 | Spoofing | Clerk development instance (shared OAuth credentials, 100-user cap) | accept | v1 ships on the dev instance because *.vercel.app cannot host a Clerk production instance (A1); production cut-over is a Phase 7 config task if a domain is bought |
| T-06-10 | Repudiation | Consent to the privacy policy | mitigate | Dashboard "Require express consent to legal documents" ON with the /privacy URL — Clerk records `legalAcceptedAt` on the user at sign-up; verified in plan 06-07's human checkpoint |
| T-06-06 | Tampering / Repudiation | Clerk self-serve account deletion bypassing the DB cascade | mitigate | Dashboard "Allow users to delete their accounts" OFF so the only user-facing delete path is the app's `deleteAccount` action (DB cascade before Clerk deletion, plan 06-04) |
| T-06-SC | Tampering | npm install @clerk/nextjs | mitigate | Package audited Approved [VERIFIED] in RESEARCH (official Clerk repo, no postinstall); exact range `^7.9.1` pinned in package.json + lockfile; no svix, no global CLI |
</threat_model>

<verification>
- `npx drizzle-kit push` (non-TTY) prints "No changes detected" and exits 0
- `npm ls @clerk/nextjs` → 7.9.x; `npm run typecheck` exits 0
- `.env.development.local` has both Clerk key names and DATABASE_URL; `git status` shows no `.env` file
- 06-VALIDATION.md is filled; frontmatter flags still `false`
</verification>

<success_criteria>
- The Phase 5 deferred push blocker is closed with pasted evidence (rename applied, second push no-op, seed rows intact)
- Clerk keys are available to local builds, Preview, and Production; Dashboard consent ON, self-serve delete OFF
- Privacy contact address recorded in the SUMMARY for plan 06-03
</success_criteria>

<output>
Create `.planning/phases/06-accounts-legal/06-01-SUMMARY.md` when done. It MUST include a "Push gate evidence" section and a `PRIVACY_CONTACT_EMAIL: <address>` line under Decisions.
</output>
