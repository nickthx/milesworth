---
phase: 06-accounts-legal
plan: 02
type: execute
wave: 2
depends_on: ["06-01"]
files_modified:
  - src/db/schema.ts
  - scripts/db-check.ts
  - src/lib/balance-params.ts
  - src/lib/account-validation.ts
  - tests/account-validation.test.ts
  - src/lib/balance-storage.ts
  - tests/balance-storage.test.ts
autonomous: true
requirements: [ACCT-01, ACCT-02, ACCT-03]

must_haves:
  truths:
    - "The live Neon database has users, user_balances, bookmarks, and travel_goals tables, every child row cascading from users.clerk_user_id, and a repeated drizzle-kit push prints 'No changes detected'"
    - "No user table has a foreign key into programs or redemptions, so npm run db:seed keeps working once a bookmark exists"
    - "balancesSchema, bookmarkSlugSchema, goalSchema, and goalIdSchema reject every hostile shape in the test table and accept every legitimate one, without touching the database"
    - "resolveInitialBalances(url, stored, saved) ranks URL > storage > account > none and existing two-argument callers still pass"
  artifacts:
    - path: "src/db/schema.ts"
      provides: "users, userBalances, bookmarks, travelGoals Drizzle tables with cascade FKs and uniqueIndex (no composite PK / unique constraints)"
      contains: "pgTable(\"travel_goals\""
    - path: "src/lib/account-validation.ts"
      provides: "Zod boundary for balances, bookmark slugs, goals, goal ids"
      exports: ["balancesSchema", "bookmarkSlugSchema", "goalSchema", "goalIdSchema", "BalancesInput", "GoalInput"]
    - path: "src/lib/balance-storage.ts"
      provides: "resolveInitialBalances with the account branch"
      contains: "source: \"account\""
    - path: "scripts/db-check.ts"
      provides: "row counts for the four user tables (post-push proof)"
      contains: "travel_goals rows"
    - path: "tests/account-validation.test.ts"
      provides: "hostile-input table for the four schemas"
      min_lines: 80
  key_links:
    - from: "src/lib/account-validation.ts"
      to: "src/lib/balance-params.ts"
      via: "PARAM_KEY_BY_SLUG keys + MAX_BALANCE (single source of enterable slugs and ceiling)"
      pattern: "PARAM_KEY_BY_SLUG"
    - from: "src/lib/account-validation.ts"
      to: "@/data redemptions"
      via: "z.enum over redemptions.map(r => r.slug)"
      pattern: "redemptions\\.map"
    - from: "src/db/schema.ts user tables"
      to: "users.clerkUserId"
      via: "references(() => users.clerkUserId, { onDelete: \"cascade\" }) on every user_id column"
      pattern: "onDelete: \"cascade\""
---

<objective>
The data layer for accounts: four `users`-anchored tables pushed to Neon with the idempotence gate re-proven, a DB-free Zod boundary module for every account write, and the third precedence branch in `resolveInitialBalances` so a fresh device lands on the user's saved balances.

Purpose: Server Actions (plan 06-04) and the UI (plans 06-05/06-06) build against these contracts. Keeping validation and precedence pure means the product rules are unit-tested in the node environment before any Clerk or DB code exists.
Output: `src/db/schema.ts` (+ live tables), `scripts/db-check.ts` counts, `src/lib/account-validation.ts` + tests, `src/lib/balance-storage.ts` + tests, `MAX_BALANCE` exported from `src/lib/balance-params.ts`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-accounts-legal/06-RESEARCH.md
@.planning/phases/06-accounts-legal/06-PATTERNS.md
@.planning/phases/06-accounts-legal/06-01-SUMMARY.md

<interfaces>
From src/engine/types.ts:
```typescript
export type EnterableProgramSlug = "chase-ur" | "amex-mr" | "capital-one" | "citi-ty" | "bilt" | "world-of-hyatt" | "hilton-honors" | "marriott-bonvoy"; // 8 slugs — derive, never re-list
export type Balances = Partial<Record<EnterableProgramSlug, number>>;
```

From src/lib/balance-params.ts (current):
```typescript
export const PARAM_KEY_BY_SLUG = { "chase-ur": "ur", "amex-mr": "mr", /* … 8 keys */ } as const satisfies Record<EnterableProgramSlug, string>;
const MAX_BALANCE = 10_000_000;   // line 62 — NOT exported yet; this plan exports it
export function paramsToBalances(params: BalanceParams): Balances;
export function balancesToParams(balances: Balances): BalanceParams;
```

From src/lib/balance-storage.ts (current lines 108-138):
```typescript
export type InitialBalancesSource =
  | { source: "url" }
  | { source: "storage"; balances: Balances }
  | { source: "none" };
export function resolveInitialBalances(urlBalances: Balances, storedBalances: Balances | null): InitialBalancesSource;
```

From src/data (barrel): `redemptions: RedemptionSeed[]` where each has `slug: string`, `title`, `cashFareCents`, `verifiedAt`. Name-collision hazard: `src/db` exports TABLES named `programs`/`redemptions` — never import `@/db` in a lib module.

Zod 4.5.4 (installed) — verified in node_modules: `z.partialRecord(keySchema, valueSchema)` (a plain `z.record` with an enum key is EXHAUSTIVE in zod 4 and would reject a two-program balance set), `z.iso.date()` (ISO yyyy-mm-dd), `z.email()` already used in interest-validation.ts.

Drizzle schema house style (src/db/schema.ts `interestSignups`): `serial("id").primaryKey()`, `text(...)`, `timestamp("created_at", { withTimezone: true }).notNull().defaultNow()`, alphabetical `drizzle-orm/pg-core` import block, rule comment on any column with a rule.

Push facts after 06-01: the diff is empty ("No changes detected"); this plan's push must contain ONLY CREATE TABLE / CREATE UNIQUE INDEX / ADD CONSTRAINT … FOREIGN KEY statements (prompt-free, non-TTY safe — 05-04 precedent).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1 [BLOCKING]: users / user_balances / bookmarks / travel_goals schema + drizzle-kit push + no-op gate + db-check counts</name>
  <files>src/db/schema.ts, scripts/db-check.ts</files>
  <read_first>
    - src/db/schema.ts (entire file — import block, header comment, `interestSignups` as the runtime-written analog, the 06-01 constraint fix)
    - scripts/db-check.ts (entire file — env load, dynamic import destructure, count query, T-01-08 error hygiene)
    - scripts/seed.ts lines 1-75 (full delete-then-insert of programs/redemptions — the reason no user table may FK into them)
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Schema block, §Pitfall 1 step 4, §Pitfall 6, §Pitfall 10, Assumption A3
    - .planning/phases/06-accounts-legal/06-01-SUMMARY.md "Push gate evidence" (what a clean push looks like now)
  </read_first>
  <action>
    1. `src/db/schema.ts`: add `uniqueIndex` to the `drizzle-orm/pg-core` import block (alphabetical, after `timestamp`, before `uuid`). Append four tables in this order, each with a comment naming the writer (Server Actions in `src/app/actions/account.ts` only; `scripts/seed.ts` never touches them) and the requirement ID:
       - `users` → `pgTable("users", …)`: `clerkUserId: text("clerk_user_id").primaryKey()` (the Clerk userId from `auth()`; cascade anchor — deleting this row removes every child row), `createdAt: timestamp("created_at", { withTimezone: true }).notNull().defaultNow()`. ACCT-01.
       - `userBalances` → `pgTable("user_balances", …)`: `id: serial("id").primaryKey()`, `userId: text("user_id").notNull().references(() => users.clerkUserId, { onDelete: "cascade" })`, `programSlug: text("program_slug").notNull()` (comment: validated against the 8 enterable slugs by `balancesSchema`; deliberately NO FK into `programs` — Pitfall 6, seed does delete-then-insert), `points: integer("points").notNull()` (comment: positive safe int ≤ MAX_BALANCE, same guard as balance-params), `updatedAt: timestamp("updated_at", { withTimezone: true }).notNull().defaultNow()`; third argument `(t) => [uniqueIndex("user_balances_user_program_uidx").on(t.userId, t.programSlug)]`. ACCT-01.
       - `bookmarks` → `pgTable("bookmarks", …)`: `id serial pk`, `userId` (same cascade FK), `redemptionSlug: text("redemption_slug").notNull()` (comment: validated against `@/data` slugs by `bookmarkSlugSchema`; NO FK into `redemptions` — Pitfall 6; unknown slugs are filtered at read time), `createdAt` timestamptz default now; third argument `(t) => [uniqueIndex("bookmarks_user_redemption_uidx").on(t.userId, t.redemptionSlug)]`. ACCT-02.
       - `travelGoals` → `pgTable("travel_goals", …)`: `id serial pk`, `userId` (same cascade FK), `text: text("text").notNull()` (≤ 280 chars, trimmed by `goalSchema`), `destination: text("destination")` (nullable, ≤ 80), `targetDate: date("target_date")` (nullable ISO date), `createdAt` timestamptz default now. No index. ACCT-03 (stored only; no ranking effect — V2-03 deferred).
       - Use `uniqueIndex`, never `.unique()` column constraints or composite `primaryKey()` on these tables (A3 / Pitfall 1 — composite constraints churn under drizzle-kit 0.31.10 on PG18).
       - Extend the file header comment: name the four user tables, state that they are written only by `src/app/actions/account.ts`, that `scripts/seed.ts` never touches them, and that no user table has a FK into a seeded table (Pitfall 6).
    2. `scripts/db-check.ts`: extend the dynamic-import destructure to `{ db, programs, interestSignups, users, userBalances, bookmarks, travelGoals }`; add four count queries and print, one per line after the existing two, exactly: `users rows: ${n}`, `user_balances rows: ${n}`, `bookmarks rows: ${n}`, `travel_goals rows: ${n}`. Keep the T-01-08 error hygiene (message only, never the URL). Update the header comment to list the user tables as runtime-writable.
    3. `npm run typecheck` must pass before pushing.
    4. [BLOCKING] Push: confirm `grep -c '^DATABASE_URL=' .env.development.local` prints `1` and `git check-ignore -q .env.development.local` exits 0. Run `npx drizzle-kit push`. It must exit 0 with no interactive prompt — a pure CREATE TABLE / CREATE UNIQUE INDEX / ADD CONSTRAINT diff is prompt-free (05-04 precedent). If the output contains ANY `ALTER TABLE "transfer_routes"` or `ALTER TABLE "transfer_bonuses"` statement, or drizzle-kit asks a question, do NOT guess: stop, record the output verbatim in the SUMMARY, and mark the task blocked (the 06-01 gate did not hold — Assumption A2/A3 failed).
    5. Idempotence gate: run `npx drizzle-kit push` a second time; the output must contain `No changes detected`. This proves `uniqueIndex` does not churn on PG18 (A3). Record both outputs in the SUMMARY.
    6. Proof: `npx tsx scripts/db-check.ts` prints the four new lines each ending in `rows: 0`, and `programs rows: 21` still.
  </action>
  <verify>
    <automated>test "$(grep -c 'pgTable(' src/db/schema.ts)" = "9" && test "$(grep -c 'references(() => users.clerkUserId, { onDelete: "cascade" })' src/db/schema.ts)" = "3" && test "$(grep -c 'uniqueIndex(' src/db/schema.ts)" = "2" && npm run typecheck && npx drizzle-kit push 2>&1 | grep -q "No changes detected" && npx tsx scripts/db-check.ts | grep -cE '^(users|user_balances|bookmarks|travel_goals) rows: [0-9]+$' | grep -q '^4$'</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'pgTable(' src/db/schema.ts` outputs `9`; `grep -c 'pgTable("users"' src/db/schema.ts`, `'pgTable("user_balances"'`, `'pgTable("bookmarks"'`, `'pgTable("travel_goals"'` each output `1`
    - `grep -c 'references(() => users.clerkUserId, { onDelete: "cascade" })' src/db/schema.ts` outputs `3`
    - `grep -c 'uniqueIndex(' src/db/schema.ts` outputs `2` (one import line does not count — verify with `grep -c 'uniqueIndex("'` = 2); `grep -c 'primaryKey({' src/db/schema.ts` outputs `1` (only transfer_routes)
    - The text of schema.ts after `pgTable("users"` contains zero occurrences of `references(() => programs` and `references(() => redemptions` (`awk '/pgTable\("users"/{f=1} f' src/db/schema.ts | grep -c 'references(() => \(programs\|redemptions\)'` outputs `0`)
    - `grep -c 'users\|userBalances\|bookmarks\|travelGoals' scripts/seed.ts` outputs `0`
    - First `npx drizzle-kit push` exits 0 with no prompt and no `ALTER TABLE "transfer_` statement; second run prints `No changes detected`; both outputs recorded in the SUMMARY
    - `npx tsx scripts/db-check.ts` prints `users rows: 0`, `user_balances rows: 0`, `bookmarks rows: 0`, `travel_goals rows: 0`, and `programs rows: 21`
    - `git status --porcelain | grep -c '\.env'` outputs `0`; `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>The four user tables exist in Neon with cascade FKs and unique indexes, the push is idempotent, and db-check proves each table by count.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: account-validation.ts Zod boundary + hostile-input tests (export MAX_BALANCE)</name>
  <files>src/lib/balance-params.ts, src/lib/account-validation.ts, tests/account-validation.test.ts</files>
  <behavior>
    - balancesSchema accepts `{}` and `{ "chase-ur": 90000, "world-of-hyatt": 1 }` and an object with all 8 enterable slugs at 10_000_000; parsed data equals input (numbers unchanged)
    - balancesSchema rejects: unknown key `{ "delta": 1 }`, `{ "chase-ur": -1 }`, `{ "chase-ur": 0 }`, `{ "chase-ur": 1.5 }`, `{ "chase-ur": "90000" }`, `{ "chase-ur": 10_000_001 }`, `null`, `[]`, `"x"`
    - bookmarkSlugSchema accepts every `slug` in `redemptions` from `../src/data` (loop with `expect(result.success, slug).toBe(true)`) and rejects `"not-a-redemption"`, `""`, `42`, `null`
    - goalSchema trims text (`"  Tokyo in J  "` → `"Tokyo in J"`), accepts text of exactly 280 chars after trim, accepts `destination: ""` and `targetDate: ""` and absent optionals, accepts `targetDate: "2027-03-01"`
    - goalSchema rejects: empty/whitespace text, 281-char text, 81-char destination, `targetDate: "03/01/2027"`, `targetDate: "2027-13-01"`, `text: 42`, `text: null`
    - goalIdSchema coerces `"12"` → `12` and rejects `"abc"`, `"-1"`, `"0"`, `"1.5"`, `""`, `null`
    - source scan: `src/lib/account-validation.ts` contains no `"use client"`, no `from "@/db`, no `drizzle`, no `@clerk`
  </behavior>
  <read_first>
    - src/lib/interest-validation.ts (entire file — header comment, zod 4 style, `z.infer` export)
    - src/lib/balance-params.ts lines 15-80 (PARAM_KEY_BY_SLUG, MAX_BALANCE at line 62, the positive-safe-integer guard)
    - tests/interest-validation.test.ts (entire file — hostile-input table shape, `if (!result.success) return;` narrowing)
    - tests/seed-data.test.ts lines 25-40 (iterating `redemptions` from `../src/data`)
    - src/data/index.ts (barrel + name-collision comment)
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Code Examples "Zod boundary", Assumption A9
  </read_first>
  <action>
    Write the test file first (RED), then the module (GREEN).

    1. `src/lib/balance-params.ts`: change line 62 to `export const MAX_BALANCE = 10_000_000;` keeping its comment (cite T-05-08: the URL ceiling and the account ceiling must be the same number). No other change; `tests/balance-params.test.ts` must still pass.
    2. `src/lib/account-validation.ts` (DB-free, framework-free, no client directive — importable by the Server Actions and by node tests):
       - Imports: `z` from "zod"; `redemptions` from "@/data"; `PARAM_KEY_BY_SLUG, MAX_BALANCE` from "@/lib/balance-params"; `type { Balances }` from "@/engine".
       - Header comment: ACCT-01/02/03 write boundary; every Server Action input is attacker-controllable (T-06-03), so it is validated here before any DB code; slugs derive from `PARAM_KEY_BY_SLUG` keys and `redemptions` — never re-listed; DB-free on purpose (T-06-05); house style from interest-validation.ts.
       - `const ENTERABLE_SLUGS = Object.keys(PARAM_KEY_BY_SLUG) as [EnterableProgramSlug, ...EnterableProgramSlug[]]` (import the slug type from "@/engine").
       - `export const balancesSchema = z.partialRecord(z.enum(ENTERABLE_SLUGS), z.number().int().positive().max(MAX_BALANCE))` — partialRecord, because zod 4's `z.record` with an enum key is exhaustive (would demand all 8 programs). Comment that.
       - `export type BalancesInput = z.infer<typeof balancesSchema>` and a compile-time assertion that it is assignable to `Balances` (e.g. a `satisfies`-style const or a `type _Check = BalancesInput extends Balances ? true : never`).
       - `export const bookmarkSlugSchema = z.enum(redemptions.map((r) => r.slug) as [string, ...string[]])` with a comment that the enum is the read-time truth for bookmarks (Pitfall 6 — no FK).
       - `export const goalSchema = z.object({ text: z.string().trim().min(1).max(280), destination: z.string().trim().max(80).optional(), targetDate: z.literal("").or(z.iso.date()).optional() })` — FormData values arrive raw (string | null → undefined); the action maps `""` to `null` when inserting. If `z.iso.date` does not typecheck on the installed zod (A9), use `z.string().regex(/^\d{4}-\d{2}-\d{2}$/)` and note it in the SUMMARY.
       - `export const goalIdSchema = z.coerce.number().int().positive()` (FormData hidden input arrives as a string; T-06-02 — the id is only ever combined with the session userId in the action).
       - `export type GoalInput = z.infer<typeof goalSchema>`.
       - No other exports; keep under 80 lines.
    3. `tests/account-validation.test.ts`: header comment (ACCT-01..03, T-06-03), relative imports `../src/lib/account-validation` and `../src/data`, one `describe` per schema named `"<schema> (ACCT-0N …)"`, one `it` per row in `<behavior>`, plus a `describe("purity")` source scan reading the module with `readFileSync(join(__dirname, "..", "src", "lib", "account-validation.ts"), "utf8")`.
  </action>
  <verify>
    <automated>npx vitest run tests/account-validation.test.ts tests/balance-params.test.ts && test "$(grep -c '^export const MAX_BALANCE' src/lib/balance-params.ts)" = "1" && test "$(grep -c 'z.partialRecord(' src/lib/account-validation.ts)" = "1" && ! grep -qE 'from "@/db|drizzle|@clerk|"use client"' src/lib/account-validation.ts && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c '^export const MAX_BALANCE = 10_000_000;' src/lib/balance-params.ts` outputs `1`
    - `grep -cE '^export const (balancesSchema|bookmarkSlugSchema|goalSchema|goalIdSchema)' src/lib/account-validation.ts` outputs `4`; `grep -c 'z.partialRecord(' src/lib/account-validation.ts` outputs `1`
    - `grep -cE 'from "@/db|drizzle|@clerk|"use client"' src/lib/account-validation.ts` outputs `0`
    - `npx vitest run tests/account-validation.test.ts` exits 0 with ≥ 25 tests, including one that loops over every `redemptions` slug
    - `npx vitest run tests/balance-params.test.ts` still exits 0
    - `npm run typecheck` exits 0
  </acceptance_criteria>
  <done>Every account write has a tested, DB-free Zod boundary whose slug sets and ceiling derive from the existing single sources of truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: resolveInitialBalances gains the account branch (URL > storage > account > none)</name>
  <files>src/lib/balance-storage.ts, tests/balance-storage.test.ts</files>
  <behavior>
    - `resolveInitialBalances({}, null, SAVED)` → `{ source: "account", balances: SAVED }`
    - `resolveInitialBalances({}, VALID_BALANCES, SAVED)` → `{ source: "storage", balances: VALID_BALANCES }` (a device's own edits outrank the last explicit save)
    - `resolveInitialBalances({ "chase-ur": 1 }, null, SAVED)` → `{ source: "url" }` (a share link still wins)
    - `resolveInitialBalances({}, null, {})` → `{ source: "none" }` and `resolveInitialBalances({}, null, null)` → `{ source: "none" }`
    - Every existing two-argument case in the file still passes unchanged (third parameter defaults to `null`)
    - Purity scan still passes: the module never contains the string `localStorage`
  </behavior>
  <read_first>
    - src/lib/balance-storage.ts lines 100-138 (the union type, the numbered A1 doc comment, the function)
    - tests/balance-storage.test.ts lines 140-190 (the precedence describe block and the purity scan)
    - .planning/phases/06-accounts-legal/06-RESEARCH.md §Pattern 5 (rationale for storage beating account)
  </read_first>
  <action>
    Extend the tests first (RED), then the module (GREEN).

    1. `src/lib/balance-storage.ts`: add `| { source: "account"; balances: Balances }` to `InitialBalancesSource`. Change the signature to `resolveInitialBalances(urlBalances: Balances, storedBalances: Balances | null, savedBalances: Balances | null = null)`. Insert rule 3 in the doc comment and code: URL empty + storage empty + non-empty `savedBalances` → `{ source: "account", balances: savedBalances }` — the caller hydrates exactly like the storage branch (push into the URL with history "replace") and must NOT write storage (A1: storage is written only after an edit; ACCT-01). Rule 4 becomes `none`. Parameter name must be `savedBalances` — the purity scan forbids the word localStorage anywhere in the file.
    2. `tests/balance-storage.test.ts`: in the existing precedence `describe`, add a `const SAVED: Balances = { "amex-mr": 50_000 }` fixture and one `it` per `<behavior>` row above, in the same one-line `toEqual` style. Update the describe title to mention the account tier.
  </action>
  <verify>
    <automated>npx vitest run tests/balance-storage.test.ts && test "$(grep -c 'source: "account"' src/lib/balance-storage.ts)" = "2" && test "$(grep -c 'localStorage' src/lib/balance-storage.ts)" = "0" && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c 'source: "account"' src/lib/balance-storage.ts` outputs `2` (union member + return)
    - `grep -c 'savedBalances: Balances | null = null' src/lib/balance-storage.ts` outputs `1`
    - `grep -c 'localStorage' src/lib/balance-storage.ts` outputs `0`
    - `npx vitest run tests/balance-storage.test.ts` exits 0 with at least 5 new account-tier cases
    - `npm run typecheck` exits 0 (core-experience.tsx still compiles with the two-argument call until plan 06-05 threads the third)
  </acceptance_criteria>
  <done>The precedence rule that decides what a returning signed-in visitor sees is pure, documented, and pinned by tests.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server Action input → Zod schemas | Attacker-controllable balances, slugs, goal text, ids (this plan builds the gate; 06-04 wires it) |
| Local shell → Neon (drizzle-kit push) | DDL against production data using the gitignored DATABASE_URL |
| Seed script ↔ user tables | Full delete-then-insert of curated tables must not be blocked by, or wipe, user data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-03 | Tampering / DoS | balancesSchema, bookmarkSlugSchema, goalSchema, goalIdSchema | mitigate | Slug enums derived from PARAM_KEY_BY_SLUG and @/data; integer, positive, ≤ MAX_BALANCE ceiling shared with the URL codec; 280/80-char caps; ISO date only; coerced positive int id; hostile-input table in tests/account-validation.test.ts |
| T-06-11 | Tampering | drizzle-kit push diff | mitigate | Only serial PKs + uniqueIndex on new tables (no composite constraints); first push must be prompt-free CREATE-only, second push must print "No changes detected"; any transfer_* ALTER halts the task |
| T-06-15 | Denial of service | npm run db:seed vs user tables | mitigate | No FK from user_balances/bookmarks into programs/redemptions (Pitfall 6); slugs validated at write time, filtered at read time; acceptance grep proves zero `references(() => programs|redemptions` after the users block |
| T-06-09 | Tampering (integrity) | Initial-balances precedence | mitigate | Pure `resolveInitialBalances` ranks URL > storage > account > none; account branch never writes storage; pinned by tests so a share link can never clobber a visitor's own balances |
| T-06-07 | Information disclosure | DATABASE_URL during push / db-check | mitigate | Env from gitignored file; commands never print the URL; `git status` gate on `.env` |
| T-06-SC | Tampering | npm installs | accept | No packages installed in this plan |
</threat_model>

<verification>
- `npx drizzle-kit push` (second run) prints "No changes detected"; `npx tsx scripts/db-check.ts` prints four `… rows: 0` lines
- `npx vitest run tests/account-validation.test.ts tests/balance-storage.test.ts tests/balance-params.test.ts` exits 0
- `npm run typecheck && npm run lint` exit 0
</verification>

<success_criteria>
- Live tables match schema.ts with cascade FKs; push idempotent; seed script untouched and unaffected
- Four exported schemas with a hostile-input test table; MAX_BALANCE shared with the URL codec
- Account precedence branch implemented and tested without any browser or DB dependency
</success_criteria>

<output>
Create `.planning/phases/06-accounts-legal/06-02-SUMMARY.md` when done. Include both push outputs under "Push gate evidence".
</output>
