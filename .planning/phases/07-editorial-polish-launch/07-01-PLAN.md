---
phase: 07-editorial-polish-launch
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - .gitignore
  - config/lighthouserc.cjs
autonomous: false
requirements: [PLAT-02, PLAT-05]
must_haves:
  truths:
    - "Three Wave 0 decisions (custom domain, Clerk social connections, launch dataset size N) are recorded as concrete values that every later plan reads instead of guessing"
    - "The three dev-only packages (@lhci/cli, lighthouse, sharp) are installed only after a human confirmed their registry legitimacy"
    - "npm run lighthouse resolves to a committed LHCI config that targets four production HTML URLs, three runs each, with SEO as a warning until the launch flip"
  artifacts:
    - path: "config/lighthouserc.cjs"
      provides: "LHCI collect/assert/upload config (Pattern 6)"
      contains: "numberOfRuns: 3"
    - path: "package.json"
      provides: "lighthouse + images:optimize scripts; @lhci/cli, lighthouse, sharp devDependencies"
      contains: "lhci autorun --config=config/lighthouserc.cjs"
    - path: ".gitignore"
      provides: "/.lighthouseci/ and /src/images/raw/ excluded from commits"
      contains: "/.lighthouseci/"
  key_links:
    - from: "07-01-SUMMARY.md ## Wave 0 Decisions"
      to: "07-05, 07-06, 07-08, 07-10 task actions"
      via: "D7-01 / D7-02 / D7-03 keys read verbatim by later executors"
      pattern: "D7-0[123]"
    - from: "package.json scripts.lighthouse"
      to: "config/lighthouserc.cjs"
      via: "--config flag (RuFlo: no root config files)"
      pattern: "config/lighthouserc\\.cjs"
---

<objective>
Fix the three human decisions the rest of Phase 7 depends on (custom domain / Clerk production instance, Clerk social-connection state, launch dataset size N), gate the only package installs of the phase behind a legitimacy checkpoint, and land the Lighthouse CI tooling skeleton.

Purpose: Research Open Questions 1–3 cannot be answered by Claude; leaving them open forces every later plan to guess (wrong host in OG cards, a hint rendered for a provider that is not enabled, a CI floor that leads the data). Nailing them first means 07-05, 07-06, 07-08 and 07-10 read concrete values.
Output: 07-01-SUMMARY.md with a `## Wave 0 Decisions` section (D7-01, D7-02, D7-03), three pinned devDependencies, `config/lighthouserc.cjs`, two new npm scripts, two new .gitignore entries.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-editorial-polish-launch/07-RESEARCH.md
@.planning/phases/07-editorial-polish-launch/07-PATTERNS.md
@.planning/phases/07-editorial-polish-launch/07-UI-SPEC.md

<interfaces>
<!-- Facts the executor needs for the decision prompt; measured at plan time (2026-09-16). -->

Dataset truth (src/data via tsx): 36 redemptions, 34 verified 2026-09-01, 2 held drafts
(st-regis-maldives, gritti-palace-venice), 20 unique imageSlugs, 46 routes, 1 dated bonus
(amex-mr → hilton-honors +30%, 2026-09-01 → 2026-10-14). Verified reach per enterable
program: chase-ur 22, amex-mr 24, capital-one 17, citi-ty 13, bilt 17, world-of-hyatt 6,
hilton-honors 4, marriott-bonvoy 5.

Registry versions verified 2026-09-15 (07-RESEARCH): @lhci/cli 0.15.1, lighthouse 13.4.1,
sharp 0.35.4 (already present transitively via next; pinned so scripts/optimize-images.ts
resolves it deterministically).

Production host today: https://milesworth.vercel.app (Vercel project "points-unlocked",
prj_XIvedejHBQr3fQaMgLhJdlAiTSpy). Clerk runs a development instance (06-01).
</interfaces>
</context>

<tasks>

<task type="checkpoint:decision" gate="blocking">
  <name>Task 1: Wave 0 decisions — custom domain, Clerk social connections, launch dataset size</name>
  <files>(none — decisions recorded in 07-01-SUMMARY.md)</files>
  <read_first>.planning/phases/07-editorial-polish-launch/07-RESEARCH.md (Open Questions 1–3, Dataset Launch-Size Gate, Pitfall 1), .planning/phases/07-editorial-polish-launch/07-UI-SPEC.md (Assumption A5), .planning/phases/06-accounts-legal/06-01-SUMMARY.md (Clerk development-instance record)</read_first>
  <action>Pause execution and present the three decisions below to Nick. Do not proceed to Task 2 until all three (plus android_device) have literal answers; write them verbatim into 07-01-SUMMARY.md under `## Wave 0 Decisions` with the keys D7-01, D7-02, D7-03, batch1, batch2, android_device. No file other than the SUMMARY changes in this task.</action>
  <decision>Three launch decisions that later plans consume as literal values: D7-01 host/Clerk instance, D7-02 sign-in providers, D7-03 dataset size N.</decision>
  <context>D7-01: *.vercel.app cannot host a Clerk production instance, so the sign-in modal shows a "Development mode" badge and caps at 100 users; cut-over is config only (domain → Vercel → pk_live/sk_live → NEXT_PUBLIC_SITE_URL → Post Inspector re-scrape) and lands in plan 07-10 if chosen. D7-02: Google OAuth is hard-blocked inside LinkedIn's WebView (403 disallowed_useragent); if any social provider is enabled on the Clerk instance, plan 07-05 renders the UI-SPEC reserved in-app-browser hint under "Sign in to save", otherwise it does not. D7-03: 34 entries are verified today; the roadmap target is 80–120 and STATE.md allows "launch thin (30+)"; each verified entry costs Nick's time. Batch 1 (plan 07-06) drafts min(20, N − 34) entries; batch 2 (plan 07-08) drafts the remainder and does the launch-week re-check of the 8 featured entries, then raises the CI floor to N.</context>
  <options>
    <option id="D7-01-a">
      <name>Buy a custom domain now and cut Clerk over to a production instance in plan 07-10</name>
      <pros>No "Development mode" badge in the modal a recruiter sees; no 100-user cap; permanent share URL</pros>
      <cons>~$10–15/yr; DNS + Clerk production instance setup on Nick's side (~30 min); OG image and share links must be re-scraped</cons>
    </option>
    <option id="D7-01-b">
      <name>Stay on milesworth.vercel.app with the Clerk development instance</name>
      <pros>Zero extra setup; nothing changes for the launch link already in circulation</pros>
      <cons>"Development mode" badge visible in the sign-in modal; 100-user cap</cons>
    </option>
    <option id="D7-02-a">
      <name>Email-code only at launch (disable every social connection in the Clerk Dashboard)</name>
      <pros>Sign-in works in-page inside the LinkedIn WebView; no hint needed</pros>
      <cons>No one-tap Google sign-in for desktop visitors</cons>
    </option>
    <option id="D7-02-b">
      <name>Keep the enabled social connection(s) and render the in-app-browser hint</name>
      <pros>Desktop visitors get one-tap sign-in</pros>
      <cons>Inside LinkedIn's browser the social button dead-ends on Google's 403; the hint mitigates but cannot prevent it</cons>
    </option>
    <option id="D7-03">
      <name>Launch dataset size N (pick a number: 34 = ship as-is, 50, 80, or another integer ≥ 34)</name>
      <pros>Higher N = more programs/destinations covered, closer to DATA-01's 80–120</pros>
      <cons>Each added entry is ~15–30 min of Nick's verification time on the critical path</cons>
    </option>
  </options>
  <acceptance_criteria>
    - 07-01-SUMMARY.md contains a `## Wave 0 Decisions` section with exactly these three keys, each with a literal value: `D7-01: a|b` (plus `domain: <hostname>` when a), `D7-02: a|b` (plus `providers: <comma list or none>` as read from the Clerk Dashboard → Configure → SSO connections), `D7-03: N=<integer ≥ 34>` and the derived `batch1=<min(20, N-34)>`, `batch2=<N-34-batch1>`
    - The summary also records `android_device: yes|no` (Research Open Question 5) for plan 07-07's device pass
  </acceptance_criteria>
  <verify>
    <human-check>07-01-SUMMARY.md `## Wave 0 Decisions` carries D7-01 (a|b + domain), D7-02 (a|b + providers), D7-03 (N, batch1, batch2), android_device — every value literal, none TBD.</human-check>
  </verify>
  <resume-signal>Reply with the three rulings, e.g. "D7-01: b; D7-02: a (providers: none); D7-03: N=50; android: no"</resume-signal>
  <done>The three open questions are closed as grep-able values that 07-05, 07-06, 07-08, and 07-10 read instead of guessing.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Package legitimacy check for @lhci/cli, lighthouse, sharp (slopcheck unavailable — all [ASSUMED])</name>
  <files>(none)</files>
  <read_first>.planning/phases/07-editorial-polish-launch/07-RESEARCH.md (## Package Legitimacy Audit table)</read_first>
  <action>Pause before any install. Present the three npmjs.com checks below; run `npm install` only after the human reply contains "packages verified". If a discrepancy is reported, stop the plan and return it to the orchestrator (a [SUS]/[SLOP] verdict forbids the install).</action>
  <what-built>Nothing installed yet. Research could not run slopcheck, so per protocol every package is [ASSUMED] and the single `npm install -D` step below is gated on a human registry check. Expected registry facts: `@lhci/cli` — publisher GoogleChrome, repo github.com/GoogleChrome/lighthouse-ci, created 2019-09, latest 0.15.1; `lighthouse` — publisher GoogleChrome, repo github.com/GoogleChrome/lighthouse, created 2012-03, latest 13.4.1; `sharp` — publisher lovell, repo github.com/lovell/sharp, created 2013-08, latest 0.35.4, no postinstall script (prebuilt binaries via @img/* optional deps).</what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/@lhci/cli — confirm the repository link is github.com/GoogleChrome/lighthouse-ci and the latest version is 0.15.1 (or a later patch on the 0.15 line).
    2. Open https://www.npmjs.com/package/lighthouse — confirm github.com/GoogleChrome/lighthouse and version 13.4.1 (or later 13.x).
    3. Open https://www.npmjs.com/package/sharp — confirm github.com/lovell/sharp and version 0.35.4 (or later 0.35.x); confirm weekly downloads are in the millions.
    4. If any package's repository, publisher, or version differs from the above, reply with the discrepancy instead of approving.
  </how-to-verify>
  <acceptance_criteria>
    - Human reply contains the literal phrase "packages verified" (or names a discrepancy, in which case Task 3 must not run)
    - No `npm install` has been executed before this reply
  </acceptance_criteria>
  <verify>
    <human-check>Human reply contains "packages verified" for @lhci/cli 0.15.1 (GoogleChrome), lighthouse 13.4.1 (GoogleChrome), sharp 0.35.4 (lovell); no install ran before the reply.</human-check>
  </verify>
  <resume-signal>Type "packages verified" or describe the discrepancy</resume-signal>
  <done>T-07-SC is discharged by a human registry check; Task 3 may install the pinned versions.</done>
</task>

<task type="auto">
  <name>Task 3: Install the three dev packages and land the LHCI config, npm scripts, and gitignore entries</name>
  <files>package.json, package-lock.json, .gitignore, config/lighthouserc.cjs</files>
  <read_first>package.json (scripts block; `"db:seed": "tsx scripts/seed.ts"` is the script precedent), .gitignore (the `# testing` / `# next.js` blocks are the placement precedent), .planning/phases/07-editorial-polish-launch/07-RESEARCH.md (Pattern 6 — the lighthouserc.cjs shape to use verbatim; Pitfall 10 — SEO stays `warn` until the flip), .planning/phases/07-editorial-polish-launch/07-PATTERNS.md (section "config/lighthouserc.cjs + package.json script + .gitignore")</read_first>
  <action>
    Run exactly `npm install -D @lhci/cli@0.15.1 lighthouse@13.4.1 sharp@0.35.4` (pinned, no caret). Do not add any runtime dependency.

    Create `config/lighthouserc.cjs` (CommonJS, `module.exports = { ci: { collect, assert, upload } }`) per RESEARCH Pattern 6: `const BASE = process.env.LHCI_BASE_URL ?? "https://milesworth.vercel.app"`; `collect.url` = the four HTML URLs `${BASE}/`, `${BASE}/?ur=90000&mr=50000`, `${BASE}/methodology`, `${BASE}/privacy` (never `/og` — T-07-07); `collect.numberOfRuns: 3`; `collect.settings.chromeFlags: "--headless=new"`; `assert.assertions` = `categories:performance` `["error", { minScore: 0.85, aggregationMethod: "median" }]`, `categories:accessibility` `["error", { minScore: 0.95 }]`, `categories:best-practices` `["error", { minScore: 0.95 }]`, `categories:seo` `["warn", { minScore: 0.9 }]` (warn, not error — the layout-level noindex fails is-crawlable until plan 07-09 flips it; 07-09 switches this to error); `upload: { target: "filesystem", outputDir: ".lighthouseci" }`. Header comment names PLAT-02, the RuFlo no-root-config rule, and the SEO sequencing note.

    In `package.json` scripts add, beside `db:seed`: `"lighthouse": "lhci autorun --config=config/lighthouserc.cjs"` and `"images:optimize": "tsx scripts/optimize-images.ts"` (the script file itself is created in plan 07-03).

    In `.gitignore` add a `# lighthouse` block with `/.lighthouseci/` and an `# image sources (not committed)` block with `/src/images/raw/` (the gitignored source folder sits beside the committed WebPs under `src/images/` — CLAUDE.md forbids working files at the repo root, so it is never a root-level `raw/`).

    Write the Task 1 rulings into 07-01-SUMMARY.md under `## Wave 0 Decisions` using the exact keys from Task 1's acceptance criteria.
  </action>
  <verify>
    <automated>npx lhci --version && node -e "const c=require('./config/lighthouserc.cjs').ci;if(c.collect.url.length!==4||c.collect.numberOfRuns!==3||c.assert.assertions['categories:seo'][0]!=='warn'||c.collect.url.some(u=>u.includes('/og')))process.exit(1)" && node -e "const p=require('./package.json');if(!p.devDependencies['@lhci/cli']||!p.devDependencies.lighthouse||!p.devDependencies.sharp||p.dependencies['@lhci/cli']||p.dependencies.sharp)process.exit(1);if(p.scripts.lighthouse!=='lhci autorun --config=config/lighthouserc.cjs')process.exit(1)" && grep -c "^/.lighthouseci/$" .gitignore && grep -c "^/src/images/raw/$" .gitignore && npm run typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `npx lhci --version` prints `0.15.1`
    - `package.json` devDependencies contain `"@lhci/cli": "0.15.1"`, `"lighthouse": "13.4.1"`, `"sharp": "0.35.4"` (exact pins) and `dependencies` gained nothing
    - `config/lighthouserc.cjs` loads under `require`, lists exactly 4 URLs none containing `/og`, `numberOfRuns` is 3, `categories:seo` level is `"warn"`, `upload.target` is `"filesystem"`
    - `.gitignore` contains the lines `/.lighthouseci/` and `/src/images/raw/`
    - `git status --porcelain` shows no `.env*` file staged; `npm run typecheck` exits 0
    - 07-01-SUMMARY.md `## Wave 0 Decisions` lists D7-01, D7-02, D7-03 (+ batch1/batch2, android_device) with literal values
  </acceptance_criteria>
  <done>Tooling installed behind the legitimacy gate, `npm run lighthouse` is runnable (it will be executed for the first time in plan 07-07), and the three decisions are recorded for downstream plans.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → node_modules | Third-party code enters the dev toolchain (build + scripts) |
| Human decisions → planning artifacts | D7-01..03 are consumed verbatim by later executors |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering | npm installs (@lhci/cli, lighthouse, sharp) | mitigate | slopcheck unavailable → all [ASSUMED]; blocking human checkpoint (Task 2) verifies publisher/repo/version on npmjs.com before the single pinned `npm install -D`; never auto-approvable |
| T-07-07 | Denial of service | LHCI hitting production `/og` (Satori CPU) | mitigate | `collect.url` lists only 4 HTML routes; verify command fails if any URL contains `/og` |
| T-07-08 | Information disclosure | Vercel bypass / PSI keys | mitigate | `config/lighthouserc.cjs` holds no secrets; `LHCI_BASE_URL` is the only env read; `.lighthouseci/` gitignored so report artifacts never commit |
| T-07-16 | Repudiation | Wave 0 decisions | mitigate | Decisions recorded with fixed keys in 07-01-SUMMARY.md, committed to git — later plans cite `D7-0x` |
</threat_model>

<verification>
- `npx lhci --version` → 0.15.1; `node -e "require('./config/lighthouserc.cjs')"` exits 0
- `npm run typecheck` exits 0; `git status` shows no env files
- 07-01-SUMMARY.md carries D7-01, D7-02, D7-03 with literal values
</verification>

<success_criteria>
- The three open questions are closed with values later plans can grep
- Dev tooling for the Lighthouse gate and the image pipeline is installed and pinned; no runtime dependency added
- Package installs happened only after human legitimacy confirmation
</success_criteria>

<output>
Create `.planning/phases/07-editorial-polish-launch/07-01-SUMMARY.md` when done (must include `## Wave 0 Decisions`)
</output>
