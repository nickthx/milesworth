---
phase: 07-editorial-polish-launch
plan: 07
type: execute
wave: 4
depends_on: ["07-05"]
files_modified:
  - config/lighthouserc.cjs
  - src/components/site-header.tsx
autonomous: false
requirements: [PLAT-02]
must_haves:
  truths:
    - "A Lighthouse mobile baseline (median of 3) exists for /, /?ur=90000&mr=50000, /methodology, /privacy on production, recorded in the SUMMARY, with performance ≥ 0.85 (/) and accessibility/best-practices ≥ 0.95 passing as errors and SEO reported as a warning"
    - "A real iPhone inside the LinkedIn app completed the full flow — share link → balances → results with photos → Copy my link (or the fallback field) → paste in a DM → email-code sign-in → Save → /account → back — and Nick approved it"
  artifacts:
    - path: "config/lighthouserc.cjs"
      provides: "thresholds tuned to baseline − noise (never raised above the measured median)"
      contains: "aggregationMethod: \"median\""
  key_links:
    - from: "origin/main"
      to: "https://milesworth.vercel.app"
      via: "git push origin HEAD:main → Vercel Git auto-deploy (06-07 Task 1 step 2 precedent)"
      pattern: "git push origin HEAD:main"
    - from: "npm run lighthouse"
      to: "https://milesworth.vercel.app (production)"
      via: "config/lighthouserc.cjs collect.url"
      pattern: "milesworth\\.vercel\\.app"
---

<objective>
Run the repeatable Lighthouse mobile gate against production for the first time, fix only the cheap blockers it names, cross-check with PageSpeed Insights, and then run the mandatory real-device pass inside LinkedIn's in-app browser — the one thing no emulator can do.

Purpose: Roadmap Success Criteria 2 and 3 (Lighthouse half). LinkedIn's production WebView cannot be remote-debugged (RESEARCH Pitfall 2), so the phone in Nick's hand is the only ground truth for PLAT-02.
Output: baseline scores + tuned thresholds in the SUMMARY, PSI cross-check, device-pass approval or a findings list.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-editorial-polish-launch/07-RESEARCH.md
@.planning/phases/07-editorial-polish-launch/07-VALIDATION.md
@.planning/phases/07-editorial-polish-launch/07-01-SUMMARY.md
@.planning/phases/06-accounts-legal/06-07-PLAN.md
@config/lighthouserc.cjs

<interfaces>
<!-- Gate mechanics. -->

`npm run lighthouse` = `lhci autorun --config=config/lighthouserc.cjs` (07-01). Filesystem upload writes
`.lighthouseci/manifest.json` (gitignored) with per-run `summary` scores; median per URL is what the assertions read.
Env override: `LHCI_BASE_URL`. Chrome: `C:\Program Files\Google\Chrome\Application\chrome.exe` (set `CHROME_PATH` if lhci cannot find it).

Deploy-by-push (06-07 Task 1 step 2 precedent): origin/main is production; nothing in waves 1–5 pushes, so this plan is the first push of Phase 7.
`git status --porcelain` empty → `git merge-base --is-ancestor origin/main HEAD` exits 0 → `git push origin HEAD:main` (never force-push) → Vercel builds within ~30–90s.
Deployment readiness probes (proof that 07-04/07-05 are live on production — poll the first one every 15s, up to 4 minutes, after the push):
`curl -s https://milesworth.vercel.app/ | grep -c "Photos via "` ≥ 1 (07-04 footer credit; wording reconciled against the manifest in 07-05) and
`curl -s https://milesworth.vercel.app/ | grep -c "viewport-fit=cover"` ≥ 1 (07-05 viewport).

PSI second opinion: `curl -s "https://www.googleapis.com/pagespeedonline/v5/runPagespeed?url=https://milesworth.vercel.app/&strategy=mobile"` →
`.lighthouseResult.categories.{performance,accessibility,best-practices,seo}.score` (no key needed at this volume).

Permitted remediation files if an assertion fails (anything else is a gap for /gsd:verify-work): `site-header.tsx` (defer `UserButton` via `next/dynamic` with `ssr: false` ONLY if TBT is the failing metric — Pitfall 11), `config/lighthouserc.cjs` (lower a threshold to measured-median − 0.03, never below 0.80 perf / 0.90 a11y-bp).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Deploy by push, confirm production carries 07-04/07-05, run the LHCI baseline, tune thresholds to baseline − noise, PSI cross-check</name>
  <files>config/lighthouserc.cjs, src/components/site-header.tsx</files>
  <read_first>config/lighthouserc.cjs, .planning/phases/07-editorial-polish-launch/07-RESEARCH.md (## Lighthouse Mobile Gate; Pitfalls 8, 9, 10, 11; Assumption A2), src/components/site-header.tsx (only if TBT remediation is needed), .gitignore (`.lighthouseci/` ignored), .planning/phases/06-accounts-legal/06-07-PLAN.md (Task 1 step 2 — the deploy-by-push sequence this task copies)</read_first>
  <action>
    Deploy (06-07 Task 1 step 2 sequence): confirm `git status --porcelain` is empty and `git merge-base --is-ancestor origin/main HEAD` exits 0 (fast-forward safe); then `git push origin HEAD:main`. If HEAD is not a descendant of origin/main, STOP and report — the orchestrator must merge first; never force-push. Never print env values during the deploy or the probes (T-07-28). Then poll `curl -s https://milesworth.vercel.app/ | grep -c "viewport-fit=cover"` every 15s up to 4 minutes until it returns ≥ 1 (proof the new build is live, not the old one), then run the second probe (`Photos via `) once — it must also be ≥ 1. Record the push output (SHA range) and both probe counts in the SUMMARY.

    If either probe is still 0 after 4 minutes, check `vercel ls --prod` for the latest deployment state (ERROR → read the build log with `vercel inspect --logs <url>` and report the failing line as a gap); do not run Lighthouse against a preview URL (Pitfall 5, previews are behind Vercel Authentication) and do not proceed until both probes pass on production.

    Run `npm run lighthouse` (≈4 min). Read `.lighthouseci/manifest.json` and compute, per URL, the median of the four category scores across the 3 runs; write them into the SUMMARY as a table `URL | perf | a11y | bp | seo` plus the LCP element and TBT for `/` and `/?ur=90000&mr=50000` (from the run JSON). Expected: SEO fails `is-crawlable` on every URL (layout noindex — by design until 07-09; it is a `warn`).

    If an `error`-level assertion fails: (a) if the failing audit is `tap-targets` or `color-contrast`, report the exact element — 07-04/07-02 should have closed these; fix only if the offending element is in `site-header.tsx`; (b) if perf < 0.85 on `/` and the LCP element is an `<img>`, that is a Pitfall 8 regression — report it as a gap (07-05 placed images below the form; do not add `preload`); (c) if perf < 0.85 and TBT is the dominant opportunity with `@clerk/clerk-js` chunks named, defer `UserButton` in `site-header.tsx` via `next/dynamic(() => import("@clerk/nextjs").then(m => m.UserButton), { ssr: false })` keeping the `<Show>` structure, then re-run; (d) otherwise lower the threshold in `config/lighthouserc.cjs` to `median − 0.03` (floor 0.80 for performance, 0.90 for accessibility / best-practices) with a comment `// baseline <date>: <median>` — thresholds are baseline − noise, not aspiration. Re-run `npm run lighthouse` until it exits 0.

    PSI cross-check: the curl from `<interfaces>` for `/` and `/?ur=90000&mr=50000`; record the four scores beside the LHCI medians in the SUMMARY (differences > 0.10 get a note; PSI is advisory).
  </action>
  <verify>
    <automated>git merge-base --is-ancestor origin/main HEAD && curl -s https://milesworth.vercel.app/ | grep -q "viewport-fit=cover" && curl -s https://milesworth.vercel.app/ | grep -q "Photos via " && npm run lighthouse && node -e "const c=require('./config/lighthouserc.cjs').ci.assert.assertions;if(c['categories:performance'][1].minScore<0.8||c['categories:accessibility'][1].minScore<0.9||c['categories:best-practices'][1].minScore<0.9||c['categories:seo'][0]!=='warn')process.exit(1)"</automated>
  </verify>
  <acceptance_criteria>
    - `git push origin HEAD:main` succeeded as a fast-forward (or the task stopped and reported the non-FF state); afterwards `git rev-parse origin/main` equals `git rev-parse HEAD` and the working tree is clean (pre-push condition); any threshold or TBT tuning made after the push is committed and pushed again with `git push origin HEAD:main` before the SUMMARY is written
    - Both readiness probes return ≥ 1 on production before Lighthouse runs (polled every 15s, ≤ 4 minutes; counts recorded in the SUMMARY); no env value appears in any task output
    - `npm run lighthouse` exits 0 (all `error` assertions pass; SEO warnings allowed)
    - SUMMARY contains the per-URL median table, the LCP element for `/` (must be a text node / `<h1>`, not `<img>`), TBT for `/`, the PSI scores, and every threshold change with its baseline value
    - `config/lighthouserc.cjs` still has SEO at `warn`, performance minScore ≥ 0.80, a11y and best-practices ≥ 0.90, 4 URLs, none `/og`
    - `site-header.tsx` changed only if TBT remediation was applied (state which in the SUMMARY); `npm test && npm run typecheck && npm run lint` green if it was
  </acceptance_criteria>
  <done>Lighthouse is a passing, repeatable gate with recorded baselines; 07-10 re-runs it post-flip with SEO as an error.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Real-device pass inside the LinkedIn in-app browser (iOS mandatory, Android per D7-01 android_device)</name>
  <files>(none — findings recorded in 07-07-SUMMARY.md)</files>
  <read_first>.planning/phases/07-editorial-polish-launch/07-VALIDATION.md (## Manual-Only Verifications row 1), .planning/phases/07-editorial-polish-launch/07-RESEARCH.md (Pitfalls 1–7), .planning/phases/07-editorial-polish-launch/07-01-SUMMARY.md (D7-02 — whether a social button is expected; android_device)</read_first>
  <action>Pause execution and present the ten-step checklist below. Record the reply verbatim in 07-07-SUMMARY.md under `## Device-pass findings` (including the step-5 path: copied vs fallback). If steps fail, do not improvise fixes in this plan — the findings feed /gsd:verify-work 7 gap closure. Approval or an enumerated findings list are both valid completions of this task.</action>
  <what-built>Production at https://milesworth.vercel.app now has the editorial pass (tokens, warm neutrals, Fraunces masthead, 44px links, safe-area footer, destination photos on bookable-now cards and the teaser), the canonical share link with a visible clipboard fallback, the viewport-fit=cover export, and passes the Lighthouse mobile gate (scores in the SUMMARY). LinkedIn's WebView cannot be attached to an inspector, so this pass is observational: what you see is the evidence.</what-built>
  <how-to-verify>
    1. On your iPhone, open the LinkedIn app and DM yourself `https://milesworth.vercel.app/?ur=90000&mr=50000`. Confirm the DM shows the OG unfurl (cream card, big terracotta number). Tap the link so it opens in LinkedIn's in-app browser (not Safari).
    2. Landing: no horizontal scroll; masthead "Milesworth" in Fraunces; the h1 is the first thing you see (no image above the form); inputs show the pre-filled 90,000 / 50,000; tapping an input does NOT zoom the page.
    3. Scroll to results: bookable-now cards lead with a photo that blurs in then sharpens; titles are 28px serif; the delta is terracotta; "Almost there" cards have no photo.
    4. Change one balance (e.g. Hyatt 40,000) — results update live, URL updates.
    5. Tap "Copy my link". EITHER the label swaps to "Link copied" and pasting into a LinkedIn DM yields exactly `https://milesworth.vercel.app/?ur=90000&mr=50000&hyatt=40000` (no extra params), OR a "Your link" field appears under the buttons holding that exact URL, already focused/selected, with the "Couldn't reach the clipboard" line — press-and-hold copies it. Report which path occurred.
    6. Tap "Sign in to save" → the Clerk modal opens in-page. Use the email code (expect a "Development mode" badge unless D7-01 = a). If a social button is visible (D7-02 = b), note whether the hint line renders under the button; do NOT tap the social button. After sign-in you are still on `/` with balances intact; tap "Save my balances" → "Saved".
    7. Tap "My account" in the header → /account shows the saved balances in 28px serif figures; the loading line "Loading your account…" may flash. Tap "Open my saved results" → back on `/` with the same numbers.
    8. Scroll to the footer on every route (`/`, `/account`, `/methodology`, `/privacy`): links are easy to tap, the "Photos via …" credit line is present, and the footer clears the home-indicator area (nothing hidden under it). Open `/privacy`'s Clerk DPA link — it navigates in the same tab (no dead tap).
    9. Visit `https://milesworth.vercel.app/nowhere` → the "Page not found" page in the same shell.
    10. Android (only if android_device = yes): repeat steps 1, 2, 5, 6 in the LinkedIn Android app and note any difference.
    Reply with "approved" plus the step-5 path (copied / fallback), or list the failing step numbers with what you saw.
  </how-to-verify>
  <acceptance_criteria>
    - Human reply contains "approved" with the step-5 outcome, or an enumerated findings list; the executor records either verbatim in 07-07-SUMMARY.md under `## Device-pass findings` (findings become gap-closure input for /gsd:verify-work 7 — the executor does not improvise fixes here)
    - Step 5's pasted URL contains only keys from the eight short keys (no `__clerk_db_jwt`, no `utm_`)
  </acceptance_criteria>
  <verify>
    <human-check>Nick replied "approved — copied" or "approved — fallback" (or listed failing steps); the step-5 pasted URL contains only the eight short keys; the reply is recorded in the SUMMARY.</human-check>
  </verify>
  <resume-signal>Type "approved — copied" / "approved — fallback", or list failing steps</resume-signal>
  <done>PLAT-02 has real-device evidence from inside the LinkedIn app on iOS (and Android if available), recorded for verification.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → origin/main → Vercel production | A push deploys waves 1–5 to the public host recruiters will visit |
| Local Chrome / Google PSI → production | Automated load against the public site |
| Nick's phone → LinkedIn WebView → production | The real user path under test |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-07 | Denial of service | `/og` under Lighthouse/PSI | mitigate | Only 4 HTML URLs × 3 runs; `/og` never in `collect.url`; PSI called twice |
| T-07-08 | Information disclosure | Lighthouse artifacts | mitigate | `.lighthouseci/` gitignored; SUMMARY records scores only, never full HTML reports |
| T-07-02 | Information disclosure | Share link in a LinkedIn DM | mitigate | Step 5 checks the pasted URL for foreign params — the real-world proof of 07-05's canonical URL |
| T-07-01 | Spoofing (Google 403 dead-end) | Social sign-in in WebView | accept / observe | Per D7-02; step 6 records whether a social button/hint is visible without exercising it |
| T-07-28 | Information disclosure | Secrets during deploy/probe | mitigate | No env values printed; `git push` output and curl probes touch public URLs only; SUMMARY contains SHAs, scores, and headers, never keys (mirrors T-06-07) |
| T-07-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `git push origin HEAD:main` fast-forwarded; production probes (`viewport-fit=cover`, `Photos via `) ≥ 1
- `npm run lighthouse` exits 0 against production with recorded medians
- Device pass approved or findings enumerated in the SUMMARY
</verification>

<success_criteria>
- Lighthouse mobile scores are launch-worthy on the 4 URLs (thresholds = baseline − noise, SEO deferred to the flip)
- The full flow works inside LinkedIn's in-app browser on a real phone, verified by Nick
</success_criteria>

<output>
Create `.planning/phases/07-editorial-polish-launch/07-07-SUMMARY.md` when done (baseline table + device-pass findings)
</output>
