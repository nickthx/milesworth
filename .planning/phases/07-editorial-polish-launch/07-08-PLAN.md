---
phase: 07-editorial-polish-launch
plan: 08
type: execute
wave: 4
depends_on: ["07-06"]
files_modified:
  - src/data/redemptions.ts
  - src/data/redemptions-hotels.ts
  - src/data/redemptions-hotels-2.ts
  - src/data/redemptions-flights.ts
  - src/data/redemptions-flights-europe.ts
  - src/data/redemptions-flights-americas.ts
  - src/data/transfers.ts
  - src/images/destinations.ts
  - src/images/destinations/*.webp
  - src/components/site-footer.tsx
  - tests/seed-data.test.ts
  - .planning/REQUIREMENTS.md
  - .planning/ROADMAP.md
  - .planning/STATE.md
autonomous: false
requirements: [DATA-01, DATA-04, PLAT-05]
must_haves:
  truths:
    - "The verified count equals or exceeds N (D7-03) and the CI floor equals N — every entry a visitor can see was confirmed by Nick"
    - "The 8 featured entries and the dated Amex→Hilton bonus were re-checked in launch week; expired or changed items are corrected"
    - "Neon holds exactly the repo's verified set; stale planning bookkeeping (DATA-0x boxes, Phase 2/3 roadmap boxes, the STATE deferred row) reflects reality"
  artifacts:
    - path: "tests/seed-data.test.ts"
      provides: "floor = N"
      contains: "toBeGreaterThanOrEqual("
    - path: ".planning/REQUIREMENTS.md"
      provides: "DATA-02/03/04 ticked; DATA-01 ticked iff N ≥ 80 else annotated"
      contains: "[x] **DATA-04**"
  key_links:
    - from: "Nick (human checkpoint)"
      to: "src/data/*.ts"
      via: "batch-2 rulings + launch-week re-check applied as a git event"
    - from: "seed files"
      to: "Neon"
      via: "npm run db:seed ×2 + DB count = N"
---

<objective>
Close the dataset launch-size gate: draft and verify the remainder of N (batch 2, possibly zero entries), re-verify the eight featured entries and the one dated transfer bonus in launch week, set the CI floor to N, reseed Neon, and correct the stale Phase 2/3 bookkeeping research flagged (Pitfall 13).

Purpose: Roadmap Success Criterion 3 (dataset half) becomes true and CI-enforced at exactly the number Nick chose; the featured entries are the ones the OG card and the teaser show a recruiter first.
Output: verified count ≥ N, floor = N, Neon reseeded, REQUIREMENTS/ROADMAP/STATE updated.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/07-editorial-polish-launch/07-01-SUMMARY.md
@.planning/phases/07-editorial-polish-launch/07-06-SUMMARY.md
@.planning/phases/07-editorial-polish-launch/07-RESEARCH.md
@.planning/phases/02-redemption-database/02-05-corrections.md
@src/data/transfers.ts
@tests/seed-data.test.ts

<interfaces>
<!-- Same dataset contract as 07-06 (see its interfaces block). Additional facts: -->

Featured entries (8, `featured: true`): ana-business-tokyo-roundtrip, ana-first-tokyo-via-virgin, jal-business-tokyo-via-alaska,
singapore-first-777-frankfurt, emirates-first-dubai, park-hyatt-tokyo, conrad-maldives, st-regis-bora-bora.
The teaser on `/` is the first featured + verified entry in seed order; `/og` shows the top-ranked result for the shared balances.

Dated bonus (src/data/transfers.ts `bonuses`): amex-mr → hilton-honors, +30%, 2026-09-01 → 2026-10-14, sourceNote "Verified 2026-09-01 …".
The engine handles expiry by date; the badge copy "+30% transfer bonus through …" disappears after endDate. If the promo was
extended/replaced, the row is edited in place (DATA-03: manual override, no schema change).

Bookkeeping targets (Pitfall 13): REQUIREMENTS.md lines 29–32 (DATA-01..04 unchecked); ROADMAP.md lines 15–16 (Phase 2, 3 unchecked),
lines 189–190 (Progress table rows "0/TBD Not started"); STATE.md ## Deferred Items row "Plan 02-05 DATA-04 gate…" (stale — says all 36 null).
Phase 2 completed 2026-09-01 (02-05-SUMMARY); Phase 3 completion date: read `.planning/phases/03-*/03-04-SUMMARY.md`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft batch 2 (batch2 from D7-03, may be 0), assemble the launch-week re-check dossier for the featured 8 and the dated bonus</name>
  <files>src/data/redemptions-hotels-2.ts, src/data/redemptions-flights-americas.ts, src/data/redemptions.ts, src/images/destinations.ts, src/images/destinations/*.webp</files>
  <read_first>.planning/phases/07-editorial-polish-launch/07-01-SUMMARY.md (N, batch2), .planning/phases/07-editorial-polish-launch/07-06-SUMMARY.md (V1, which programs are still thin after batch 1, files created), the seed files batch 1 created, src/data/transfers.ts (bonuses array), src/data/redemptions-flights.ts / redemptions-hotels.ts (the 8 featured entries' current sourceNotes and figures), .planning/phases/07-editorial-polish-launch/07-06-PLAN.md (Task 1 drafting rules — apply identically)</read_first>
  <action>
    Draft `batch2 = N − 34 − batch1` entries (0 allowed) following 07-06 Task 1 rules exactly (thin programs first using the post-batch-1 reach counts from 07-06-SUMMARY; drafts `verifiedAt: null` + `CLAUDE DRAFT — check …`; images via the 07-03 pipeline for new destinations, Unsplash-first, any source new to the manifest listed under `## New photo sources` for Task 3; files < 500 lines; barrel updated). Also re-draft any batch-1 entry Nick asked to revisit rather than drop.

    Launch-week dossier (always, even when batch2 = 0), written into the SUMMARY draft for Task 2: for each of the 8 featured slugs list the current pointsMin/pointsMax, cashFareCents, taxesFeesCents, partner, and verifiedAt with a one-line "what to re-check" (award chart page / search date range / cash benchmark); for the Amex→Hilton bonus list its dates and ask whether it is still live, extended, replaced, or ended (if ended before launch, the row stays as history — the engine ignores expired windows — unless Nick prefers deletion); ask Nick for a re-verification date to stamp on any featured entry he re-confirms (the "Verified <date>" badge is what a recruiter sees first).

    Run `npx vitest run tests/seed-data.test.ts tests/image-manifest.test.ts` (floor still V1; drafts do not count; image-manifest coverage green); the full suite and build run once, in Task 3.
  </action>
  <verify>
    <automated>npx vitest run tests/seed-data.test.ts tests/image-manifest.test.ts && npm run typecheck && npm run lint && npx tsx -e "import('./src/data/index.ts').then(d=>{const r=d.redemptions;const v=r.filter(x=>x.verifiedAt!==null).length;console.log(r.length+' total, '+v+' verified');const f=r.filter(x=>x.featured);console.log('featured '+f.length);if(f.length<8)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Total redemptions = 36 + batch1 + batch2 (minus any batch-1 drops); new entries all `verifiedAt: null` with `CLAUDE DRAFT — check` sourceNotes
    - `npx vitest run tests/seed-data.test.ts tests/image-manifest.test.ts` exits 0; every split file < 500 lines
    - SUMMARY draft contains the 8-row featured dossier and the bonus question with current dates
    - If batch2 = 0 the SUMMARY states "no new drafts per D7-03" and the dossier alone goes to Task 2
  </acceptance_criteria>
  <done>Batch 2 drafts (if any) and the launch-week re-check list are ready for Nick.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Nick verifies batch 2, re-checks the featured 8 and the dated bonus in launch week</name>
  <files>(none — human review; edits land in Task 3)</files>
  <read_first>The seed files Task 1 touched, src/data/transfers.ts (bonus row), the 07-08-SUMMARY draft (featured dossier)</read_first>
  <action>Pause execution and present the request below. Do not modify seed files or the bonus row, mark anything verified, or proceed to Task 3 until Nick has ruled on every batch-2 slug, each featured entry, and the bonus, and has confirmed the final count reaches N (or amended N explicitly).</action>
  <what-built>Batch 2 drafts (count per D7-03 — possibly none) and the launch-week dossier: the 8 featured entries that front the teaser card and the OG image, plus the Amex→Hilton +30% bonus that ends 2026-10-14. After this pass the CI floor is raised to N and nothing else changes in the dataset before launch.</what-built>
  <how-to-verify>
    1. Batch 2 drafts (if any): same procedure as batch 1 — confirm/correct numbers against live sources, give a verifiedAt date per confirmed slug, or say keep-as-draft / drop.
    2. Featured 8: for each slug, quickly re-check the award price range and cash benchmark against today's sources. Reply "unchanged" or give corrected figures, and give the date to stamp as the new verifiedAt (this is the "Verified <date>" a recruiter sees on the teaser and the first cards).
    3. Amex→Hilton bonus: reply live / extended to <date> / replaced by <program → partner, %, dates, source> / ended.
    4. Confirm the final number: after your rulings the verified count must be ≥ N (= D7-03). If it will fall short, either confirm more drafts now or reply with a revised N (recorded as D7-03 amended).
  </how-to-verify>
  <acceptance_criteria>
    - Every batch-2 slug has a ruling; every featured slug has "unchanged" or corrections plus a re-verification date; the bonus has one of the four rulings
    - The resulting verified count (existing + newly confirmed) is ≥ N, or a revised N is stated explicitly
  </acceptance_criteria>
  <verify>
    <human-check>Rulings exist for every batch-2 slug, all 8 featured slugs (unchanged/corrected + re-verification date), and the bonus (live/extended/replaced/ended); the resulting verified count is ≥ N or an amended N is stated.</human-check>
  </verify>
  <resume-signal>Provide rulings per slug, the featured re-check dates, the bonus ruling, and type "batch 2 verified"</resume-signal>
  <done>Launch-week verification rulings are captured — Task 3 applies them and sets the floor to N.</done>
</task>

<task type="auto">
  <name>Task 3: Apply rulings, set the floor to N, reseed Neon, fix Phase 2/3 bookkeeping, re-check the footer photo credit</name>
  <files>src/data/redemptions-hotels.ts, src/data/redemptions-hotels-2.ts, src/data/redemptions-flights.ts, src/data/redemptions-flights-europe.ts, src/data/redemptions-flights-americas.ts, src/data/redemptions.ts, src/data/transfers.ts, src/images/destinations.ts, src/components/site-footer.tsx, tests/seed-data.test.ts, .planning/REQUIREMENTS.md, .planning/ROADMAP.md, .planning/STATE.md</files>
  <read_first>Task 2's checkpoint response, all seed files in their current state, src/data/transfers.ts (bonus row), tests/seed-data.test.ts (floor line + comment), src/components/site-footer.tsx (credit span), src/images/destinations.ts (`source:` values — read as text), 07-06-SUMMARY.md and this plan's SUMMARY draft (`## New photo sources`), .planning/REQUIREMENTS.md (lines 27–32), .planning/ROADMAP.md (lines 14–20 phase list; 186–194 progress table), .planning/STATE.md (## Deferred Items), .planning/phases/03-valuation-ranking-engine/03-04-SUMMARY.md or the Phase 3 directory's last SUMMARY (completion date), .planning/phases/02-redemption-database/02-05-corrections.md</read_first>
  <action>
    Apply batch-2 rulings and featured re-checks exactly as ruled (verifiedAt only on confirmed slugs; featured entries re-confirmed get the new date and a sourceNote beginning `Verified <new date> — …`; corrections applied verbatim). Apply the bonus ruling to `src/data/transfers.ts` (edit dates/percent/sourceNote in place, add a replacement row, or leave as history). Append `## Phase 7 batch 2 + launch-week re-check (<date>)` to `02-05-corrections.md`.

    Compute the verified count V2 with the tsx one-liner; it must be ≥ N (or the amended N). Set `tests/seed-data.test.ts` floor to `toBeGreaterThanOrEqual(N)` with a comment `// launch floor N=<N> per D7-03, verified <date>`. Update seed-file header comments with the new verified counts/dates.

    Reseed: `npm run db:seed` twice (identical counts, verified suffix = V2), DB verified count one-liner = V2, full local gate.

    Footer credit re-check (UI-SPEC A7, T-07-14): with every batch image now in the manifest, run `grep -o 'source: "[A-Za-z]*"' src/images/destinations.ts | sort | uniq -c` and make the credit span in `src/components/site-footer.tsx` name exactly that set — "Photos via Unsplash", "Photos via Unsplash and Pexels", or "Photos via Pexels" (the 07-05 Task 3 rule); usually a no-op. Record the counts and the final wording in the SUMMARY.

    Bookkeeping (Pitfall 13): in `.planning/REQUIREMENTS.md` tick `[x]` DATA-02, DATA-03, DATA-04; tick DATA-01 only if N ≥ 80, otherwise leave `[ ]` and append ` — launch size N=<N> per D7-03; 80–120 target carried forward` to its line. In `.planning/ROADMAP.md` mark Phase 2 `[x]` with `(completed 2026-09-01)` and Phase 3 `[x]` with its SUMMARY date; update the Progress table rows for Phase 2 (`5/5 | Complete | 2026-09-01`) and Phase 3 (`4/4 | Complete | <date>`). In `.planning/STATE.md` change the Deferred Items row's Status to `resolved (07-08: N=<N> verified)`. Commit data + tests separately from the planning-doc edits (two commits) so the verification diff stays clean.
  </action>
  <verify>
    <automated>npm run db:seed && npm run db:seed && npx tsx -e "process.loadEnvFile('.env.development.local');Promise.all([import('./src/db/index.ts'),import('drizzle-orm'),import('./src/data/index.ts')]).then(async([m,o,d])=>{const want=d.redemptions.filter(r=>r.verifiedAt!==null).length;const[{n}]=await m.db.select({n:o.count()}).from(m.redemptions).where(o.isNotNull(m.redemptions.verifiedAt));console.log(n+' verified in DB, '+want+' in seed');if(Number(n)!==want)process.exit(1)})" && npm test && npm run typecheck && npm run lint && npm run build && grep -c "\[x\] \*\*DATA-04\*\*" .planning/REQUIREMENTS.md</automated>
  </verify>
  <acceptance_criteria>
    - `tests/seed-data.test.ts` floor number equals N (grep) and `npm test` passes; seed verified count V2 ≥ N
    - DB verified count = V2; both seed runs identical; no connection-string fragment in output
    - Every featured entry's `verifiedAt` is the re-check date Nick gave (or unchanged if he did not re-confirm — state which in the SUMMARY)
    - `transfers.ts` bonus row reflects the ruling; `npx vitest run tests/transfers.test.ts tests/seed-data.test.ts` green
    - `site-footer.tsx` credit text matches the set of `source:` values in `src/images/destinations.ts` (record both in the SUMMARY); the design-system gate's credit regex still passes
    - REQUIREMENTS.md shows `[x]` for DATA-02, DATA-03, DATA-04 (and DATA-01 iff N ≥ 80, else the annotation); ROADMAP.md Phase 2 and 3 lines start with `- [x]`; STATE.md deferred row says `resolved`
  </acceptance_criteria>
  <done>The dataset is at its launch size, Nick-verified, CI-floored at N, live in Neon, and the planning ledgers say so.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verification → seed files | Nick's rulings anchor data truth (featured entries front the product) |
| Seed files → Neon | Reseed with DATABASE_URL in scope |
| Planning docs | Bookkeeping must match the repo, not aspiration |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Spoofing (false verification) | verifiedAt / featured re-stamp | mitigate | Dates only from Nick's reply; provenance test; corrections log appended |
| T-07-11 | Information disclosure | reseed / DB count | mitigate | Counts-only output, message-only errors |
| T-07-22 | Tampering (CI red) | floor = N | mitigate | Floor set in the same commit as the data that satisfies it; V2 ≥ N asserted before the edit |
| T-07-23 | Repudiation (stale badge) | "Verified <date>" on featured cards + bonus badge | mitigate | Launch-week re-check dossier; bonus row edited per DATA-03 manual override |
| T-07-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
- Floor = N, `npm test` green, DB count = seed count ≥ N
- Featured 8 re-stamped; bonus ruling applied
- REQUIREMENTS/ROADMAP/STATE corrected
</verification>

<success_criteria>
- Roadmap SC3 (dataset half) true and CI-enforced at N
- Recruiter-facing entries carry launch-week verification dates
</success_criteria>

<output>
Create `.planning/phases/07-editorial-polish-launch/07-08-SUMMARY.md` when done (V2, N, featured re-check table, bonus ruling)
</output>
