---
phase: 07-editorial-polish-launch
plan: 06
type: execute
wave: 3
depends_on: ["07-01", "07-03"]
files_modified:
  - src/data/redemptions.ts
  - src/data/redemptions-hotels.ts
  - src/data/redemptions-flights.ts
  - src/data/redemptions-flights-europe.ts
  - src/data/redemptions-hotels-2.ts
  - src/data/redemptions-flights-americas.ts
  - src/images/destinations.ts
  - src/images/destinations/*.webp
  - tests/seed-data.test.ts
autonomous: false
requirements: [DATA-01, DATA-04, PLAT-05]
must_haves:
  truths:
    - "Batch 1 (min(20, N−34) entries per D7-03) is drafted with provenance notes, verified by Nick, and only Nick-confirmed entries carry a verifiedAt date"
    - "The two held drafts (st-regis-maldives, gritti-palace-venice) have a ruling — verified with numbers or explicitly kept as drafts"
    - "Every new destination has a WebP + manifest entry before its entry is drafted, so the image-manifest test stays green"
    - "The CI floor equals the verified count after this batch (never above the data), and Neon mirrors the seed files"
  artifacts:
    - path: "src/data/redemptions.ts"
      provides: "barrel spreading every split file"
      contains: "satisfies RedemptionSeed[]"
    - path: "tests/seed-data.test.ts"
      provides: "verified floor raised to the post-batch count"
      contains: "toBeGreaterThanOrEqual("
  key_links:
    - from: "Nick (human checkpoint)"
      to: "src/data/redemptions-*.ts"
      via: "corrections + verifiedAt dates applied as a diffable git event (02-05 pattern)"
    - from: "src/data/redemptions-*.ts imageSlug"
      to: "src/images/destinations/<slug>.webp"
      via: "tests/image-manifest.test.ts"
      pattern: "imageSlug"
---

<objective>
Grow the verified dataset toward the launch number N fixed in 07-01 (D7-03): draft batch 1 targeting the thinnest programs, source any new destination photos through the 07-03 pipeline, run Nick's verification checkpoint in the 02-05 shape, apply his rulings, raise the CI floor to the achieved count, and reseed Neon.

Purpose: Roadmap Success Criterion 3 — "the dataset has reached its launch size with all entries Nick-verified". Nick's time is the long pole; batching keeps each review ~20 entries.
Output: batch-1 entries verified in seed files, floor raised, Neon reseeded, held-draft rulings recorded.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-editorial-polish-launch/07-01-SUMMARY.md
@.planning/phases/07-editorial-polish-launch/07-03-SUMMARY.md
@.planning/phases/07-editorial-polish-launch/07-RESEARCH.md
@.planning/phases/02-redemption-database/02-05-PLAN.md
@.planning/phases/02-redemption-database/02-05-corrections.md
@src/data/types.ts
@src/data/redemptions-hotels.ts
@tests/seed-data.test.ts

<interfaces>
<!-- Dataset contract and measured state (2026-09-16). -->

RedemptionSeed (src/data/types.ts lines 64–91): slug, partnerProgramSlug, title, category "flight"|"hotel"|"other",
origin|null, destination|null, cabin|null, pointsMin (int>0), pointsMax (int>0|null, ≥ pointsMin), taxesFeesCents (≥0),
cashFareCents (>0), availabilityRating "wide_open"|"plan_ahead"|"hard_to_find", bookingHint, methodologyNote|null,
sourceNote (non-empty), verifiedAt (ISO date | null = draft, never ranked), imageSlug|null, featured, notes|null.

Draft convention (tests/seed-data.test.ts 180–192): drafts have `verifiedAt: null` and `sourceNote: "CLAUDE DRAFT — check …"`;
verified entries' sourceNote must start with "Verified ". Floor assertion at line ~161: `expect(verified.length).toBeGreaterThanOrEqual(30)`.
Barrel (src/data/redemptions.ts): spread of flightRedemptions, flightRedemptionsEurope, hotelRedemptions; src/data/index.ts must
NOT re-export split files. Each split file < 500 lines (redemptions-hotels.ts is 378 today — new hotels go in a new file).

Measured verified reach per enterable program: chase-ur 22, amex-mr 24, capital-one 17, citi-ty 13, bilt 17,
world-of-hyatt 6, hilton-honors 4, marriott-bonvoy 5. Partner distribution of the 34 verified: world-of-hyatt 6,
hilton-honors 4, marriott-bonvoy 3, virgin-atlantic 4, then 1–2 each across 12 airline partners. Held drafts: st-regis-maldives,
gritti-palace-venice (Nick: manual marriott.com check needed). One dated bonus: amex-mr→hilton-honors +30% through 2026-10-14.

Image pipeline (07-03): drop `raw/<slug>.jpg`, run `npm run images:optimize -- <slug>`, add the manifest import + entry
(alt 4–10 words, credit). tests/image-manifest.test.ts fails on any imageSlug without a file/manifest entry.

Reseed proof (02-05 Task 3): `npm run db:seed` twice with identical counts, then the read-only DB count one-liner.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Draft batch 1 (size batch1 from D7-03) targeting thin programs; source images for any new destination</name>
  <files>src/data/redemptions-hotels-2.ts, src/data/redemptions-flights-americas.ts, src/data/redemptions.ts, src/images/destinations.ts, src/images/destinations/*.webp</files>
  <read_first>.planning/phases/07-editorial-polish-launch/07-01-SUMMARY.md (D7-03: N, batch1), src/data/redemptions-hotels.ts (entry shape lines 14–37, file header lines 3–10, section comments), src/data/redemptions-flights.ts (header lines 3–13 — fare-benchmark convention), src/data/programs.ts (partner program slugs available for partnerProgramSlug — only slugs present here validate), src/data/transfers.ts (which enterable programs reach which partners — a draft is only useful if some enterable program reaches its partner), src/data/types.ts (schema + validateDataset lines 147–213), .planning/phases/02-redemption-database/02-05-corrections.md (Nick's prior rulings and sources), .planning/phases/07-editorial-polish-launch/07-03-SUMMARY.md (per-slug credit table, pipeline commands)</read_first>
  <action>
    Read `batch1` from 07-01-SUMMARY (`min(20, N − 34)`). If `batch1 = 0`, skip drafting: this task only assembles the held-draft dossier (below) and the SUMMARY states "no new drafts per D7-03".

    Otherwise draft exactly `batch1` new entries, each with `verifiedAt: null`, `sourceNote: "CLAUDE DRAFT — check <what: award chart/search page, cash-fare benchmark, taxes>"`, `featured: false`, realistic 2026 figures with pointsMin/pointsMax ranges where pricing is dynamic, a 2–4 line `bookingHint`, a `methodologyNote` naming the fare convention (discounted realistic retail for economy/business, undiscounted retail for First — flights header), and an `imageSlug`. Allocation (measured reach above; thin first): ≈40% hotel entries used directly via world-of-hyatt / hilton-honors / marriott-bonvoy (new `src/data/redemptions-hotels-2.ts`, header in the hotels-file style), ≈60% flights whose partner is reachable from citi-ty and capital-one/bilt via active routes in transfers.ts (new `src/data/redemptions-flights-americas.ts` for Americas/Caribbean/domestic and any region not yet covered; put Asia/Europe additions in the existing regional files only if they stay < 500 lines). Every `partnerProgramSlug` must exist in programs.ts; every `slug` unique; `pointsMax >= pointsMin`. Spread the new files into `src/data/redemptions.ts` (keep `satisfies RedemptionSeed[]`); do not touch `src/data/index.ts`.

    Prefer existing imageSlugs (20 available) where the destination matches. For each genuinely new destination: source the photo per 07-03 Task 1 method into `raw/<slug>.jpg`, run `npm run images:optimize -- <slug>`, add the import + manifest entry (alt, credit) in `src/images/destinations.ts`. New slugs must match `^[a-z0-9]+(-[a-z0-9]+)*$`.

    Held-draft dossier: for `st-regis-maldives` and `gritti-palace-venice`, list in the SUMMARY what Nick must check on marriott.com (standard award night points range for a representative high-season date, cash rate incl. taxes) so Task 2 can rule on them.

    Run `npm test` — the floor stays at its current value (drafts never count), `validateDataset` and Zod must pass, and `tests/image-manifest.test.ts` must pass (every new imageSlug has a file + manifest entry).
  </action>
  <verify>
    <automated>npm test && npm run typecheck && npm run lint && npx tsx -e "import('./src/data/index.ts').then(d=>{const r=d.redemptions;const v=r.filter(x=>x.verifiedAt!==null).length;const dr=r.filter(x=>x.verifiedAt===null).length;console.log(r.length+' total, '+v+' verified, '+dr+' drafts');if(v<34)process.exit(1)})"</automated>
  </verify>
  <acceptance_criteria>
    - Total redemptions = 36 + batch1; verified count still 34; every new entry has `verifiedAt: null` and a `sourceNote` starting with `CLAUDE DRAFT — check`
    - `npm test` exits 0 (schema, cross-refs, floor at its pre-plan value, image-manifest coverage)
    - `wc -l` of every `src/data/redemptions-*.ts` < 500; `src/data/index.ts` unchanged
    - Every new `partnerProgramSlug` is reachable from at least one enterable program via an active route or direct use (`npx tsx` check over routes — include the one-liner output in the SUMMARY)
    - SUMMARY lists each draft slug with its partner and target program, plus the held-draft dossier
  </acceptance_criteria>
  <done>Batch 1 is on disk as drafts with imagery, structurally valid, invisible to the engine until verified.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Nick verifies batch 1 and rules on the two held drafts against live 2026 sources</name>
  <files>(none — human review; edits land in Task 3)</files>
  <read_first>src/data/redemptions-hotels-2.ts, src/data/redemptions-flights-americas.ts (and any other file Task 1 touched), src/data/redemptions-hotels.ts (the two held entries), the 07-06-SUMMARY draft (draft list + held-draft dossier)</read_first>
  <action>Pause execution and present the verification request below (02-05 Task 1 pattern). Do not modify any seed file, mark any entry verified, or proceed to Task 3 until Nick has ruled on every batch-1 slug and both held drafts. Data truth (2026 award prices, cash benchmarks) is exactly what Claude gets wrong (Phase 2 Pitfall 1).</action>
  <what-built>Batch 1 — `batch1` new redemption drafts (all `verifiedAt: null`, each sourceNote naming what to check) plus the dossier for the two held Marriott drafts. Nothing new is ranked or shown to visitors until you confirm it; the engine's A5 filter and the CI provenance test make that structural.</what-built>
  <how-to-verify>
    1. Open the new seed files listed above. For each draft, check against live sources (current award charts / award search, Google Flights or the hotel's own site for the cash benchmark) that pointsMin/pointsMax, cashFareCents, taxesFeesCents, partnerProgramSlug, and bookingHint are accurate as of 2026. Expect stale numbers — that is what this pass is for.
    2. For each entry you confirm, reply with corrected numbers (if any) and the verification date (YYYY-MM-DD) for `verifiedAt`. Entries you do not review stay drafts.
    3. Rule on `st-regis-maldives` and `gritti-palace-venice`: either supply the marriott.com figures + date, or say "keep as drafts".
    4. Optional: name any draft you want dropped entirely (the executor deletes it and its image if no other entry uses the slug).
  </how-to-verify>
  <acceptance_criteria>
    - Nick's reply covers every batch-1 slug with one of: confirmed (+date, ± corrections), keep as draft, or drop
    - Explicit ruling on each of the two held drafts
  </acceptance_criteria>
  <verify>
    <human-check>Nick supplied a ruling (confirm + date, with corrections / keep as draft / drop) for every batch-1 slug and an explicit ruling for st-regis-maldives and gritti-palace-venice.</human-check>
  </verify>
  <resume-signal>Provide the corrections + verifiedAt dates per slug and the held-draft rulings; type "batch 1 verified" when complete, or describe blockers</resume-signal>
  <done>Nick's rulings for batch 1 are captured in the checkpoint response — the raw input Task 3 materializes into the seed files.</done>
</task>

<task type="auto">
  <name>Task 3: Apply rulings, raise the CI floor to the achieved count, reseed Neon, prove the DB count</name>
  <files>src/data/redemptions-hotels.ts, src/data/redemptions-hotels-2.ts, src/data/redemptions-flights-americas.ts, src/data/redemptions-flights.ts, src/data/redemptions-flights-europe.ts, src/data/redemptions.ts, src/images/destinations.ts, tests/seed-data.test.ts</files>
  <read_first>All seed files Task 1 touched (current state), Task 2's checkpoint response, tests/seed-data.test.ts (lines 155–195 — floor and provenance tests), scripts/seed.ts, scripts/db-check.ts, .planning/phases/02-redemption-database/02-05-PLAN.md (Task 2/3 wording and the DB one-liner)</read_first>
  <action>
    Apply every correction exactly as ruled. Set `verifiedAt: "YYYY-MM-DD"` ONLY on slugs Nick confirmed; rewrite each confirmed entry's `sourceNote` to start with `Verified <date> — <source/finding>`; entries not reviewed keep `verifiedAt: null` and their `CLAUDE DRAFT` note; delete dropped entries (and their manifest entry + WebP only if no remaining entry uses the slug — the image-manifest test's orphan check will tell you). Apply the held-draft rulings to `redemptions-hotels.ts` and update its header comment (verified count / held slugs). Append the rulings to `.planning/phases/02-redemption-database/02-05-corrections.md` under a `## Phase 7 batch 1 (<date>)` heading so the ruling log stays in one place.

    Compute the new verified count V1 (`npx tsx` one-liner). In `tests/seed-data.test.ts` change `toBeGreaterThanOrEqual(30)` to `toBeGreaterThanOrEqual(V1)` and update the comment to name the batch and date (Pitfall 12: floor follows the data — V1 ≤ N).

    Run `npm run db:seed` twice — identical counts, verified suffix = V1 — then the read-only DB count one-liner (02-05 Task 3 shape: `process.loadEnvFile('.env.development.local')`, `db.select({n: count()}).from(redemptions).where(isNotNull(redemptions.verifiedAt))`), asserting ≥ V1. Full local gate. Commit; the wave merge deploys.
  </action>
  <verify>
    <automated>npm run db:seed && npm run db:seed && npx tsx -e "process.loadEnvFile('.env.development.local');Promise.all([import('./src/db/index.ts'),import('drizzle-orm'),import('./src/data/index.ts')]).then(async([m,o,d])=>{const want=d.redemptions.filter(r=>r.verifiedAt!==null).length;const[{n}]=await m.db.select({n:o.count()}).from(m.redemptions).where(o.isNotNull(m.redemptions.verifiedAt));console.log(n+' verified in DB, '+want+' in seed');if(Number(n)!==want)process.exit(1)})" && npm test && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `tests/seed-data.test.ts` floor equals the seed-file verified count V1 exactly (grep the number) and `npm test` passes
    - Zero entries carry `verifiedAt` without appearing in Nick's confirmation list; every verified entry's sourceNote starts with `Verified `
    - Both seed runs exit 0 with identical counts; the DB verified count equals V1
    - No command output contains a connection-string fragment
    - `02-05-corrections.md` has the `## Phase 7 batch 1` section; `redemptions-hotels.ts` header reflects the held-draft ruling
  </acceptance_criteria>
  <done>Batch 1 is a diffable verification event, the floor tracks the data, and Neon matches the repo.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Human verification → seed files | Nick's rulings are the data-truth anchor |
| Seed files → Neon | DATABASE_URL in scope for the reseed |
| New photos → repo | Same pipeline/threats as 07-03 |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-10 | Spoofing (false verification) | verifiedAt | mitigate | Dates set only from Nick's list; provenance test rejects `CLAUDE DRAFT` on verified rows; git diff is the audit record |
| T-07-11 | Information disclosure | reseed + DB count | mitigate | Counts-only output; message-only errors; acceptance forbids connection-string fragments |
| T-07-22 | Tampering (CI red) | seed floor | mitigate | Floor raised only to the achieved verified count in the same commit as the data (Pitfall 12) |
| T-07-05 / T-07-09 | Info disclosure / path traversal | new images | mitigate | Same sharp pipeline and slug regex as 07-03 |
| T-07-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `npm test` green with the raised floor; DB verified count = seed verified count
- Every new imageSlug covered by tests/image-manifest.test.ts
- Held drafts have an explicit ruling in 02-05-corrections.md
</verification>

<success_criteria>
- Verified count moved from 34 toward N by Nick's rulings only; floor and Neon follow
- No unverified entry is rankable; new destinations have editorial photos
</success_criteria>

<output>
Create `.planning/phases/07-editorial-polish-launch/07-06-SUMMARY.md` when done (record V1 and the per-slug rulings table)
</output>
