---
phase: 07-editorial-polish-launch
plan: 03
type: execute
wave: 2
depends_on: ["07-01"]
files_modified:
  - scripts/optimize-images.ts
  - src/images/destinations.ts
  - src/images/destinations/tokyo.webp
  - src/images/destinations/singapore.webp
  - src/images/destinations/hong-kong.webp
  - src/images/destinations/hawaii.webp
  - src/images/destinations/london.webp
  - src/images/destinations/paris.webp
  - src/images/destinations/frankfurt.webp
  - src/images/destinations/dubai.webp
  - src/images/destinations/istanbul.webp
  - src/images/destinations/doha.webp
  - src/images/destinations/amsterdam.webp
  - src/images/destinations/big-sur.webp
  - src/images/destinations/kauai.webp
  - src/images/destinations/austin.webp
  - src/images/destinations/cancun.webp
  - src/images/destinations/maldives.webp
  - src/images/destinations/bora-bora.webp
  - src/images/destinations/maui.webp
  - src/images/destinations/kyoto.webp
  - src/images/destinations/venice.webp
  - tests/image-manifest.test.ts
autonomous: false
requirements: [PLAT-05]
must_haves:
  truths:
    - "Every one of the 20 imageSlugs in the seed data has a committed 1600×1067 WebP under 200 KB and a typed manifest entry with alt text and a photographer credit"
    - "Images are sourced once, re-encoded with metadata stripped, and never referenced by URL or filesystem path at runtime"
    - "A DB-free test fails CI if a seed imageSlug lacks a file or manifest entry, or a file lacks a manifest entry"
    - "Nick has looked at all 20 photos and accepted them against the UI-SPEC selection rules"
  artifacts:
    - path: "scripts/optimize-images.ts"
      provides: "sharp pipeline src/images/raw/<slug>.{jpg,png} → src/images/destinations/<slug>.webp (3:2, 1600w, q75, ≤200 KB gate)"
      contains: "position: \"attention\""
    - path: "src/images/destinations.ts"
      provides: "DESTINATION_IMAGES typed map, ImageSlug type, getDestinationImage(slug) lookup"
      exports: ["DESTINATION_IMAGES", "getDestinationImage", "ImageSlug", "DestinationImage"]
    - path: "tests/image-manifest.test.ts"
      provides: "seed imageSlug ⟷ webp file ⟷ manifest source-text coverage"
      contains: "src/images/destinations"
  key_links:
    - from: "src/data/redemptions-*.ts imageSlug"
      to: "src/images/destinations/<slug>.webp"
      via: "manifest static imports keyed by slug"
      pattern: "import \\w+ from \"\\./destinations/[a-z-]+\\.webp\""
    - from: "tests/image-manifest.test.ts"
      to: "src/images/destinations.ts (source text) + directory listing"
      via: "readFileSync/readdirSync — never imports the manifest (vitest cannot load .webp)"
      pattern: "readdirSync"
---

<objective>
Build the destination-imagery pipeline the CLAUDE.md contract prescribes — source once, pre-crop 3:2, WebP ~1600w, commit, expose through a typed manifest keyed by `imageSlug` — for the 20 destinations the dataset uses today, with a coverage test and a human photo review.

Purpose: PLAT-05 names "destination imagery" and the codebase has none. The manifest is the only thing 07-05 (card render) and 07-06/07-08 (new destinations) need; getting it right here keeps those plans to a one-line lookup.
Output: 20 WebPs, `src/images/destinations.ts`, `scripts/optimize-images.ts`, `tests/image-manifest.test.ts`, Nick's photo approval.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-editorial-polish-launch/07-UI-SPEC.md
@.planning/phases/07-editorial-polish-launch/07-PATTERNS.md
@.planning/phases/07-editorial-polish-launch/07-RESEARCH.md
@scripts/seed.ts
@src/lib/balance-params.ts
@tests/seed-data.test.ts

<interfaces>
<!-- Contract this plan creates; result-card.tsx / core-experience.tsx (07-05) and the dataset plans consume it. -->

From src/images/destinations.ts (NEW):
```typescript
import type { StaticImageData } from "next/image";

export interface DestinationImage {
  image: StaticImageData;            // width/height/blurDataURL supplied by the static import
  alt: string;                       // 4–10 words, the scene — never the redemption title
  credit: { photographer: string; source: "Unsplash" | "Pexels" };
}
export const DESTINATION_IMAGES: Record<string, DestinationImage>; // `as const satisfies Record<string, DestinationImage>`
export type ImageSlug = keyof typeof DESTINATION_IMAGES;
export function getDestinationImage(slug: string | null): DestinationImage | null; // `slug in DESTINATION_IMAGES` guard
```

Seed field consumed: `RedemptionSeed.imageSlug: string | null` (src/data/types.ts line 85).
The 20 slugs in use (measured 2026-09-16): tokyo, singapore, hong-kong, hawaii, london, paris,
frankfurt, dubai, istanbul, doha, amsterdam, big-sur, kauai, austin, cancun, maldives, bora-bora,
maui, kyoto, venice.

Script precedent (scripts/seed.ts): header comment → `async function main()` → `process.exit(0)`;
`main().catch` prints only `err.message` and exits 1. Run via the `images:optimize` npm script
added in 07-01 (`tsx scripts/optimize-images.ts`).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Write scripts/optimize-images.ts and source the 20 raw photos into src/images/raw/</name>
  <files>scripts/optimize-images.ts</files>
  <read_first>scripts/seed.ts (script skeleton, exit codes, secret-free output), .planning/phases/07-editorial-polish-launch/07-UI-SPEC.md (## Imagery Contract — Asset row, Photo selection row, Alt text row), .planning/phases/07-editorial-polish-launch/07-RESEARCH.md (Pattern 2 sharp call and the Unsplash license note; Security Domain V5 — slugs never come from user input), .gitignore (`/raw/` must already be present from 07-01)</read_first>
  <action>
    Create `scripts/optimize-images.ts` in the seed.ts skeleton (header names PLAT-05, the CLAUDE.md pipeline contract, T-07-05 metadata stripping, T-07-09 slug validation; no env load — no DB). Behavior: read the `src/images/raw/` directory listing (exit 1 with the message `src/images/raw/ is missing — drop source photos there as <slug>.jpg` if absent); accept only entries matching `/^([a-z0-9]+(?:-[a-z0-9]+)*)\.(jpe?g|png|webp)$/` and ignore everything else (the slug is the capture group — never argv, never a path segment from input); optional argv slugs restrict the run (`npm run images:optimize -- tokyo kyoto`), each validated against the same regex. For each slug call `sharp(input).resize({ width: 1600, height: 1067, fit: "cover", position: "attention" }).webp({ quality: 75, effort: 6 }).toFile("src/images/destinations/<slug>.webp")` (do not call `withMetadata` — EXIF/GPS is dropped on re-encode). After writing, read the output's `metadata()` and byte size; print one line per file `slug 1600x1067 <bytes> B`; after the loop exit 1 if any output exceeds 204800 bytes or is not exactly 1600×1067, naming the offenders. Create `src/images/destinations/` if it does not exist.

    Then source one photo per slug into `src/images/raw/<slug>.jpg` (gitignored). Selection rules (UI-SPEC Photo selection): daylight or golden hour, light and warm, landscape/cityscape/architecture of the destination; no cabins, lounges, hotel logos, people as subjects, heavy HDR or night shots; landscape orientation ≥ 1600 px wide. Method, in order: (1) Unsplash — find a photo id via WebFetch on `https://unsplash.com/s/photos/<destination>`, then `curl -L -o src/images/raw/<slug>.jpg "https://unsplash.com/photos/<id>/download?force=true&w=2000"`; (2) Pexels, only when Unsplash has no photo meeting the selection rules — find a photo page via WebFetch on `https://www.pexels.com/search/<destination>/`, then `curl -L -o src/images/raw/<slug>.jpg "https://images.pexels.com/photos/<id>/pexels-photo-<id>.jpeg?w=2000"`. Unsplash-first keeps the footer credit line that 07-04 writes in parallel ("Photos via Unsplash") accurate; 07-05 Task 3 reconciles that line against the manifest's `source:` values, so any Pexels fallback must be visible in the SUMMARY credit table. This is a one-time download under the Unsplash License / Pexels License (not API hotlinking). For every slug record photographer name, source (`"Unsplash"` | `"Pexels"`), and the photo page URL in a scratch list for Task 2's manifest (the URL goes in a manifest comment only — never rendered, never fetched at runtime). Suggested searches: tokyo "tokyo skyline sunset", singapore "marina bay singapore", hong-kong "victoria harbour hong kong", hawaii "waikiki diamond head", london "westminster thames", paris "paris rooftops eiffel", frankfurt "frankfurt skyline main river", dubai "dubai skyline morning", istanbul "bosphorus istanbul skyline", doha "doha corniche", amsterdam "amsterdam canal houses", big-sur "bixby bridge big sur", kauai "na pali coast", austin "austin skyline lady bird lake", cancun "cancun beach turquoise", maldives "maldives overwater villas", bora-bora "bora bora otemanu lagoon", maui "maui coast golden hour", kyoto "arashiyama bamboo kyoto", venice "grand canal gondolas venice".

    If any slug cannot be downloaded automatically after both sources, do not fabricate or copy another slug's file — leave it missing and list it in the SUMMARY under `## Missing raw photos`; Task 3's checkpoint asks Nick to drop those files into `src/images/raw/` before the rest of the plan completes.
  </action>
  <verify>
    <automated>npx tsc --noEmit -p tsconfig.json && node -e "const fs=require('fs');const n=fs.existsSync('src/images/raw')?fs.readdirSync('src/images/raw').filter(f=>/^[a-z0-9-]+\.(jpe?g|png|webp)$/.test(f)).length:0;console.log(n+' raw photos');if(n<1)process.exit(1)" && git check-ignore src/images/raw/x.jpg</automated>
  </verify>
  <acceptance_criteria>
    - `scripts/optimize-images.ts` contains `position: "attention"`, `quality: 75`, `effort: 6`, `width: 1600`, `height: 1067`, the slug regex, the 204800-byte gate, and no `withMetadata`
    - The script never builds a path from argv or listing text without the regex passing (source assertion: every `join(` that includes a slug is preceded by the regex test)
    - `src/images/raw/` contains ≥ 1 valid source file; `git check-ignore src/images/raw/x.jpg` exits 0 (directory is ignored)
    - `npm run typecheck` exits 0
    - SUMMARY lists, per slug, photographer + source + page URL, and any slugs left under `## Missing raw photos`; the table makes any Pexels fallback obvious (07-05 Task 3 reads it)
  </acceptance_criteria>
  <done>The pipeline script exists and the raw sources are on disk (gitignored), ready to encode.</done>
</task>

<task type="auto">
  <name>Task 2: Encode the 20 WebPs, write the typed manifest, and add the coverage test</name>
  <files>src/images/destinations.ts, src/images/destinations/*.webp, tests/image-manifest.test.ts</files>
  <read_first>src/lib/balance-params.ts (lines 14–32 — the `as const satisfies Record<…>` typed-map pattern and derived union type), src/components/balance-form.tsx (lines 27–30 — the `in` membership guard), tests/seed-data.test.ts (lines 1–16 — DB-free relative-import shape), .planning/phases/07-editorial-polish-launch/07-RESEARCH.md (Pattern 2 manifest code; Code Examples → Image-manifest coverage test), .planning/phases/07-editorial-polish-launch/07-PATTERNS.md (sections `src/images/destinations.ts`, `src/images/destinations/*.webp`, `tests/image-manifest.test.ts`)</read_first>
  <action>
    Run `npm run images:optimize` — it must exit 0 and produce `src/images/destinations/<slug>.webp` for every slug present in `src/images/raw/`.

    Create `src/images/destinations.ts` per RESEARCH Pattern 2 and the balance-params typed-map convention: header comment stating the CLAUDE.md contract (sourced once under the Unsplash/Pexels licenses, committed, statically imported, never hotlinked, never a filesystem path at runtime; credits live here; the footer credit line reads "Photos via Unsplash" and 07-05 Task 3 extends it to "… and Pexels" if any entry's source is Pexels — UI-SPEC A7), one `import <camelSlug> from "./destinations/<slug>.webp"` per slug, the `DestinationImage` interface, `DESTINATION_IMAGES` as `as const satisfies Record<string, DestinationImage>`, `export type ImageSlug = keyof typeof DESTINATION_IMAGES`, and `export function getDestinationImage(slug: string | null): DestinationImage | null` returning `DESTINATION_IMAGES[slug as ImageSlug]` only when `slug !== null && slug in DESTINATION_IMAGES`. Use these alt strings verbatim (UI-SPEC alt rule — the scene, 4–10 words): tokyo "Tokyo skyline at golden hour"; singapore "Marina Bay waterfront, Singapore, at dusk"; hong-kong "Victoria Harbour skyline, Hong Kong"; hawaii "Waikiki shoreline with Diamond Head, Oahu"; london "Thames riverside and Westminster, London"; paris "Paris rooftops with the Eiffel Tower"; frankfurt "Frankfurt skyline over the Main river"; dubai "Dubai skyline in warm morning light"; istanbul "Bosphorus and Istanbul's old-city skyline"; doha "Doha corniche and West Bay towers"; amsterdam "Canal houses along an Amsterdam canal"; big-sur "Bixby Bridge on the Big Sur coast"; kauai "Na Pali Coast cliffs, Kauai"; austin "Austin skyline across Lady Bird Lake"; cancun "Turquoise water and white sand, Cancun"; maldives "Overwater villas at dusk, Maldives"; bora-bora "Mount Otemanu across the Bora Bora lagoon"; maui "Maui coastline at golden hour"; kyoto "Bamboo grove path in Arashiyama, Kyoto"; venice "Gondolas on the Grand Canal, Venice". If the photo actually sourced shows a different scene, adjust the alt to describe it (keep 4–10 words). Each entry's `credit` carries the photographer and source from Task 1; put the photo page URL in a trailing `//` comment on the entry line.

    Confirm the module type-checks under Next's image typing: the repo's `next-env.d.ts` declares `*.webp` imports as `StaticImageData` — if `tsc` complains, add nothing to the manifest; instead note it, because the fix belongs in tsconfig `include`, which already lists next-env.d.ts.

    Create `tests/image-manifest.test.ts` (header names PLAT-05, T-07-04, and the "never import the manifest — vitest cannot load .webp" rule): read `src/images/destinations` with `readdirSync` → set of slugs (strip `.webp`, ignore non-webp); read the manifest with `readFileSync` as text. Assertions: (a) every non-null `redemptions[].imageSlug` (from `../src/data`) is in the directory set — failure message `missing images: <list>`; (b) for every slug in the directory set the manifest text contains `from "./destinations/<slug>.webp"` and a line matching `^\s*"?<slug>"?:` (both directions — no orphan file, no orphan entry); (c) every file's `statSync().size` ≤ 204800; (d) the manifest text contains no `http://` or `https://` outside `//` comment lines, and contains `getDestinationImage`; (e) every manifest entry line group contains `source: "Unsplash"` or `source: "Pexels"` (count of `source:` equals the directory set size).
  </action>
  <verify>
    <automated>npm run images:optimize && npx vitest run tests/image-manifest.test.ts && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `ls src/images/destinations/*.webp | wc -l` prints 20 (or 20 minus the `## Missing raw photos` count, which Task 3 then closes)
    - Every WebP is exactly 1600×1067 and ≤ 204800 bytes (the script's own gate exits 0)
    - `src/images/destinations.ts` exports `DESTINATION_IMAGES`, `ImageSlug`, `DestinationImage`, `getDestinationImage`; contains one `.webp` import per file; contains `satisfies Record<string, DestinationImage>`; contains no `https://` outside comment lines
    - `npx vitest run tests/image-manifest.test.ts` exits 0; deleting one WebP (then restoring it) makes assertion (a) fail with `missing images:` naming the slug — perform and revert; record in SUMMARY
    - `npm run build` exits 0 (Next resolves the static imports)
  </acceptance_criteria>
  <done>The manifest, assets, and coverage gate exist; result cards can call `getDestinationImage(redemption.imageSlug)` in 07-05.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Nick reviews the 20 photos against the editorial selection rules (and supplies any missing raw files)</name>
  <files>(none — approval; replacements re-run Task 1/2 commands)</files>
  <read_first>.planning/phases/07-editorial-polish-launch/07-UI-SPEC.md (## Imagery Contract — Photo selection and Treatment rows), the plan's 07-03-SUMMARY draft (`## Missing raw photos` and the per-slug credit list)</read_first>
  <action>Pause and present the review request below. Handle replace requests in a loop: re-source `src/images/raw/<slug>.jpg` (Task 1 method, or the URL Nick supplies), run `npm run images:optimize -- <slug>`, update the manifest alt/credit, re-run `npx vitest run tests/image-manifest.test.ts`, and re-present. For slugs listed under `## Missing raw photos`, wait for Nick to drop `src/images/raw/<slug>.jpg`, then run the optimize + manifest steps. Finish only on "photos approved".</action>
  <what-built>Twenty destination WebPs under `src/images/destinations/` (3:2, 1600×1067, ≤ 200 KB each, metadata stripped), a typed manifest with alt text and photographer credits, and a CI test that fails if any seed destination lacks a photo. The photos have not yet been rendered on cards (plan 07-05 does that); this review is of the files themselves. If the SUMMARY lists slugs under `## Missing raw photos`, those could not be downloaded automatically.</what-built>
  <how-to-verify>
    1. Open the folder `src/images/destinations/` in Explorer (large-icon view previews WebP) — or run `npm run dev` after 07-05 lands if you prefer to see them on cards.
    2. For each of the 20 images check: daylight/golden hour, light and warm (not night, not heavy HDR), the destination itself (skyline, coast, architecture), no airplane cabins / lounge seats / hotel logos / people as the subject, nothing that would read as stock-tropical clip art.
    3. For any image you reject, reply with the slug and, optionally, a preferred photo URL (Unsplash or Pexels page) — the executor re-downloads it to `src/images/raw/<slug>.jpg`, re-runs `npm run images:optimize -- <slug>`, and updates the manifest credit + alt.
    4. If the SUMMARY lists missing slugs, download a suitable JPEG for each yourself and save it as `src/images/raw/<slug>.jpg` (the folder is gitignored), then reply so the executor runs `npm run images:optimize` and completes the manifest for those slugs.
  </how-to-verify>
  <acceptance_criteria>
    - Human reply contains "photos approved" (possibly after one or more replace rounds), or names slugs to replace which the executor then re-sources until approval
    - After approval: `ls src/images/destinations/*.webp | wc -l` = 20; `npx vitest run tests/image-manifest.test.ts` exits 0; `## Missing raw photos` in the SUMMARY is empty
  </acceptance_criteria>
  <verify>
    <human-check>Nick replied "photos approved"; all 20 WebPs exist; `npx vitest run tests/image-manifest.test.ts` exits 0; `## Missing raw photos` is empty.</human-check>
  </verify>
  <resume-signal>Type "photos approved", or list slugs to replace (optionally with a preferred photo URL each)</resume-signal>
  <done>All 20 destination photos meet the editorial selection rules per Nick and are covered by the manifest test.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet photo sources → src/images/raw/ → repo | Third-party binaries enter the repository (one-time download, re-encoded) |
| src/images/raw/ directory listing → script file paths | Filenames become output paths |
| Seed `imageSlug` string → manifest lookup | Data-layer string selects an asset |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering / Availability | Runtime image source | mitigate | Static imports only; manifest test forbids `https://` outside comments; no `remotePatterns` (07-02 gate) |
| T-07-05 | Information disclosure | EXIF/GPS in committed photos | mitigate | sharp re-encode without `withMetadata` strips metadata; acceptance criterion forbids `withMetadata` in the script |
| T-07-09 | Tampering (path traversal) | scripts/optimize-images.ts | mitigate | Slugs accepted only via `^[a-z0-9]+(-[a-z0-9]+)*$` from the `src/images/raw/` listing or argv; output path is a fixed directory + validated slug |
| T-07-14 | Repudiation | Photo licensing | mitigate | Photographer + source per manifest entry, page URL in a comment; downloaded (not API) use is license-compliant; footer credit line lands in 07-04 |
| T-07-18 | Spoofing | `getDestinationImage` on an unknown slug | mitigate | `in` guard returns null; a null renders the card without an image (UI-SPEC Missing image row) — never a broken `<img>` |
| T-07-SC | Tampering | package installs | accept | This plan installs nothing (sharp pinned in 07-01) |
</threat_model>

<verification>
- `npm run images:optimize` exits 0 with 20 lines `slug 1600x1067 <bytes> B`, all ≤ 204800
- `npx vitest run tests/image-manifest.test.ts` green; `npm run build` green
- Nick's "photos approved" recorded in the SUMMARY
</verification>

<success_criteria>
- 20 committed WebPs + typed manifest + coverage test; no runtime URL, no metadata, no path lookup
- Photos meet the editorial selection rules per Nick's review
</success_criteria>

<output>
Create `.planning/phases/07-editorial-polish-launch/07-03-SUMMARY.md` when done (include the per-slug credit table)
</output>
