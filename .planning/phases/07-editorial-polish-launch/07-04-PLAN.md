---
phase: 07-editorial-polish-launch
plan: 04
type: execute
wave: 2
depends_on: ["07-02"]
files_modified:
  - src/app/account/page.tsx
  - src/app/privacy/page.tsx
  - src/app/methodology/page.tsx
  - src/components/account/bookmark-list.tsx
  - src/components/account/delete-account-dialog.tsx
  - src/components/core-experience.tsx
  - src/components/result-card.tsx
  - src/components/almost-there.tsx
  - src/components/advisor-tease.tsx
  - src/components/site-header.tsx
  - src/components/site-footer.tsx
  - src/app/not-found.tsx
  - src/app/account/loading.tsx
  - tests/design-system-gate.test.ts
autonomous: true
requirements: [PLAT-05, PLAT-02]
must_haves:
  truths:
    - "Every heading, card title, dialog title, masthead, and account figure uses the text-heading token — no arbitrary text-[1.75rem] and no undeclared text-lg size remains anywhere under src"
    - "Header and footer links are 44px-tall targets, the footer respects the iPhone safe-area inset, and the footer carries the Unsplash credit line"
    - "The /privacy page has no target=_blank links (WebView-safe same-tab navigation)"
    - "A not-found route and an /account loading state render in the same editorial shell with the locked copy"
  artifacts:
    - path: "src/app/not-found.tsx"
      provides: "Designed 404 in the error.tsx shell"
      contains: "Page not found"
    - path: "src/app/account/loading.tsx"
      provides: "/account navigation loading state"
      contains: "Loading your account"
    - path: "src/components/site-footer.tsx"
      provides: "pb-safe, min-h-11 links, Photos via Unsplash credit"
      contains: "Photos via Unsplash"
    - path: "tests/design-system-gate.test.ts"
      provides: "type-sweep pins (text-[1.75rem]=0, text-lg=0, target=_blank=0, min-h-11 present)"
      contains: "text-[1.75rem]"
  key_links:
    - from: "src/components/site-footer.tsx"
      to: "src/app/globals.css @utility pb-safe"
      via: "pb-safe class on the footer container"
      pattern: "pb-safe"
    - from: "every heading class string"
      to: "globals.css --text-heading"
      via: "text-heading utility"
      pattern: "text-heading"
---

<objective>
Apply the tokens from 07-02 across every route and component: retire the nine `text-[1.75rem] leading-tight` copies and the two `text-lg` sizes, re-type the masthead and dialog, grow header/footer links to 44px with safe-area padding and the credit line, drop `target="_blank"` on /privacy, and add the two missing designed states (not-found, /account loading).

Purpose: This is the "applied consistently across all pages" half of PLAT-05 and the tap-target / WebView-chrome half of PLAT-02 — mechanical but recruiter-visible.
Output: 11 modified files, 2 new route files, extended design-system gate.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/07-editorial-polish-launch/07-UI-SPEC.md
@.planning/phases/07-editorial-polish-launch/07-PATTERNS.md
@src/app/error.tsx
@src/components/site-header.tsx
@src/components/site-footer.tsx
@tests/design-system-gate.test.ts

<interfaces>
<!-- Exact current strings to replace (grep 2026-09-16). -->

`text-[1.75rem] leading-tight` occurrences (9): src/app/account/page.tsx:38 (HEADING_CLASS),
src/app/methodology/page.tsx:93 (HEADING_CLASS), src/app/privacy/page.tsx:29 (HEADING_CLASS),
src/components/advisor-tease.tsx:58 (h2), src/components/almost-there.tsx:40 (h2) and :65 (CardTitle),
src/components/core-experience.tsx:104 (SECTION_HEADING_CLASS) and :346 (teaser CardTitle),
src/components/result-card.tsx:78 (CardTitle).

`text-lg` occurrences (2): src/app/account/page.tsx:140 `font-heading text-ink text-lg font-semibold`
(saved-balance dd), src/components/account/bookmark-list.tsx:34 same string (bookmark title p).

`target="_blank"` + `rel="noreferrer"`: src/app/privacy/page.tsx lines 99–100 and 108–109.

Class-constant convention (account/page.tsx 36–43): HEADING_CLASS / BODY_CLASS / MUTED_CLASS /
LABEL_CLASS / LINK_CLASS. Nav-link addition this phase: `inline-flex min-h-11 items-center`.

error.tsx shell (copy for not-found.tsx): `<main className="bg-cream flex flex-1 flex-col">` →
`<div className="mx-auto flex w-full max-w-3xl flex-col gap-6 px-4 py-12 sm:px-6 md:py-16">` →
`<h1 className="font-display text-ink text-display font-semibold">` → `<p className="text-ink/70 text-base leading-6">` →
`<Link href="/" className="text-ink/70 text-sm leading-5 underline-offset-4 hover:underline">Back to your results</Link>`.

account/page.tsx Shell (copy for loading.tsx): `<main className="bg-cream flex flex-1 flex-col">` →
`<article className="mx-auto flex w-full max-w-3xl flex-col gap-8 px-4 py-12 sm:px-6 md:py-16">` →
`<header className="flex flex-col gap-6"><h1 className="font-display text-ink text-display font-semibold">Your account</h1></header>`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Token sweep on the pages and account components; remove /privacy target=_blank</name>
  <files>src/app/account/page.tsx, src/app/privacy/page.tsx, src/app/methodology/page.tsx, src/components/account/bookmark-list.tsx, src/components/account/delete-account-dialog.tsx</files>
  <read_first>src/app/account/page.tsx (lines 36–43 constants, line 140 dd), src/app/privacy/page.tsx (lines 27–34 constants, 96–113 links), src/app/methodology/page.tsx (lines 90–95), src/components/account/bookmark-list.tsx (line 34), src/components/account/delete-account-dialog.tsx (lines 57–62 DialogTitle/DialogDescription), .planning/phases/07-editorial-polish-launch/07-UI-SPEC.md (## Typography binding-changes table; A11, A14), tests/privacy-page.test.ts and tests/methodology-page.test.ts (what they pin — re-run after)</read_first>
  <action>
    In `account/page.tsx`, `privacy/page.tsx`, `methodology/page.tsx`: change `HEADING_CLASS` from `"font-heading text-ink text-[1.75rem] leading-tight font-semibold"` to `"font-heading text-ink text-heading font-semibold"`. In `account/page.tsx` line 140 change the saved-balance `dd` class `font-heading text-ink text-lg font-semibold` to `font-display text-ink text-heading font-semibold` (UI-SPEC A14 — these are the account page's big numbers). In `bookmark-list.tsx` line 34 change `font-heading text-ink text-lg font-semibold` to `font-display text-ink text-heading font-semibold`. In `delete-account-dialog.tsx` give `<DialogTitle>` `className="font-display text-ink text-heading font-semibold"` and `<DialogDescription>` `className="text-ink/70 text-base leading-6"` (UI-SPEC Typography table, dialog row).

    In `privacy/page.tsx` remove `target="_blank"` and `rel="noreferrer"` from both `<a>` elements (Clerk DPA at line ~99, subprocessors at line ~108); keep `href` and `className={LINK_CLASS}` (UI-SPEC A11 / RESEARCH Pitfall 6). Do not touch the `mailto:` link. Run `npx prettier --write` on the five files.
  </action>
  <verify>
    <automated>! grep -rn "text-\[1.75rem\]\|text-lg\|target=\"_blank\"\|rel=\"noreferrer\"" src/app/account/page.tsx src/app/privacy/page.tsx src/app/methodology/page.tsx src/components/account/bookmark-list.tsx src/components/account/delete-account-dialog.tsx && npx vitest run tests/privacy-page.test.ts tests/methodology-page.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - The five files contain zero occurrences of `text-[1.75rem]`, `text-lg`, `target="_blank"`, `rel="noreferrer"`
    - `account/page.tsx` contains `font-display text-ink text-heading font-semibold` (the dd) and `HEADING_CLASS` contains `text-heading`
    - `delete-account-dialog.tsx` DialogTitle className contains `text-heading` and DialogDescription className contains `text-ink/70 text-base leading-6`
    - `npx vitest run tests/privacy-page.test.ts tests/methodology-page.test.ts` exits 0 (section lists unchanged)
  </acceptance_criteria>
  <done>Pages and account components use only the declared type scale; /privacy is WebView-safe.</done>
</task>

<task type="auto">
  <name>Task 2: Token sweep on the island components; masthead, 44px nav links, safe-area footer with the credit line</name>
  <files>src/components/core-experience.tsx, src/components/result-card.tsx, src/components/almost-there.tsx, src/components/advisor-tease.tsx, src/components/site-header.tsx, src/components/site-footer.tsx</files>
  <read_first>src/components/core-experience.tsx (lines 103–104 SECTION_HEADING_CLASS, 343–349 teaser CardTitle), src/components/result-card.tsx (lines 75–81), src/components/almost-there.tsx (lines 39–41, 64–67), src/components/advisor-tease.tsx (lines 56–60), src/components/site-header.tsx (entire — keep the 20-line header comment; lines 30–32 wordmark, 36–41 button, 45–50 link), src/components/site-footer.tsx (entire), .planning/phases/07-editorial-polish-launch/07-UI-SPEC.md (Header and Footer rows in ## Interaction, Layout & WebView Contract; Spacing exceptions; A6, A7), .planning/phases/07-editorial-polish-launch/07-PATTERNS.md (section `site-header.tsx and site-footer.tsx`)</read_first>
  <action>
    Replace `text-[1.75rem] leading-tight` with `text-heading` in: core-experience.tsx `SECTION_HEADING_CLASS` (→ `"font-heading text-ink text-heading font-semibold"`) and the teaser `CardTitle` (→ `"text-ink text-heading font-semibold"`); result-card.tsx `CardTitle` (same); almost-there.tsx `h2` (→ `"font-heading text-ink text-heading font-semibold"`) and `CardTitle`; advisor-tease.tsx `h2`. No other change in those four files (the images and share link arrive in 07-05).

    `site-header.tsx`: wordmark `Link` className → `font-display text-ink text-heading font-semibold` (UI-SPEC A6 masthead); "Sign in" button className → `text-ink/70 inline-flex h-11 items-center text-sm leading-5 underline-offset-4 hover:underline`; "My account" Link className → `text-ink/70 inline-flex min-h-11 items-center text-sm leading-5 underline-offset-4 hover:underline`; header element `border-ink/10 border-b` → `border-border border-b` (warm token from 07-02). Keep `"use client"`, the header comment, `<Show>`, `<UserButton />` untouched.

    `site-footer.tsx`: container className `... gap-2 px-4 py-8 sm:flex-row ...` → `... gap-3 px-4 pt-8 pb-safe sm:flex-row ...`; footer element `border-ink/10` → `border-border`; wordmark `<p>` className → `font-display text-ink text-base font-semibold`; nav className `flex flex-col gap-2 sm:flex-row sm:items-center sm:gap-4` → `flex flex-col gap-3 sm:flex-row sm:items-center sm:gap-4`; both `Link`s get `inline-flex min-h-11 items-center` prepended to their existing classes; insert a new `<span className="text-ink/70 text-sm leading-5">Photos via Unsplash</span>` (or "Photos via Unsplash and Pexels" if 07-03's manifest uses both sources — read the manifest's `source:` values) between the Privacy link and the disclaimer span; plain text, no link (UI-SPEC A7). Footer stays a server component (no hooks, no client directive). Add a one-line comment above the credit span citing PLAT-05 / UI-SPEC A7 and T-07-14. Run prettier on the six files.
  </action>
  <verify>
    <automated>! grep -rn "text-\[1.75rem\]" src && grep -c "text-heading" src/components/site-header.tsx && grep -c "pb-safe" src/components/site-footer.tsx && grep -c "Photos via Unsplash" src/components/site-footer.tsx && [ "$(grep -c 'min-h-11' src/components/site-footer.tsx)" -ge 2 ] && grep -c "min-h-11" src/components/site-header.tsx && ! grep -q "border-ink/10" src/components/site-header.tsx src/components/site-footer.tsx && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "text-\[1.75rem\]" src` returns nothing
    - `site-header.tsx` wordmark class contains `font-display` and `text-heading`; "My account" link class contains `min-h-11`; the "Sign in" button retains `h-11`
    - `site-footer.tsx` contains `pb-safe`, `gap-3`, `Photos via Unsplash`, exactly two `min-h-11` Link classes, `font-display` on the wordmark, no `py-8`
    - Neither header nor footer contains `border-ink/10`; both contain `border-border`
    - `npm run build` exits 0 and `/methodology`, `/privacy` still show `○` (static) in the build route table (the footer stays a server component)
  </acceptance_criteria>
  <done>Island headings are tokenized; header/footer meet the tap-target, safe-area, masthead, and credit contract.</done>
</task>

<task type="auto">
  <name>Task 3: Add not-found.tsx and account/loading.tsx; extend the design-system gate with the sweep pins</name>
  <files>src/app/not-found.tsx, src/app/account/loading.tsx, tests/design-system-gate.test.ts</files>
  <read_first>src/app/error.tsx (entire — the shell and copy voice to mirror; not-found is a SERVER component: no "use client", no reset, no Button), src/app/account/page.tsx (lines 58–71 Shell, line 40 MUTED_CLASS — do not import Shell, it is not exported and page.tsx imports @clerk/nextjs/server), tests/design-system-gate.test.ts (append after the `extended in 07-04` comment), tests/guest-flow-gate.test.ts (lines 54–74 static-route loop shape), .planning/phases/07-editorial-polish-launch/07-UI-SPEC.md (## Copywriting Contract — Not found and Account loading rows; A9)</read_first>
  <action>
    Create `src/app/not-found.tsx`: server component (no client directive), header comment mirroring error.tsx lines 7–17 (route convention, T-04-12 neutral copy, ink-only accent discipline, PLAT-05). Markup = the error.tsx shell with h1 `Page not found`, body `That address doesn't go anywhere. Head back to see what your points are worth.` (use `&apos;` in JSX), and the `Link href="/"` "Back to your results" with the LINK class. No Button, no imports beyond `next/link`.

    Create `src/app/account/loading.tsx`: server component rendering the account Shell markup inline (main → article → header/h1 "Your account") plus one `<p className="text-ink/70 text-base leading-6">Loading your account…</p>` (Unicode ellipsis, UI-SPEC copy). No skeletons, no spinner, no imports from `@clerk/nextjs/server` or `@/db`, no `page.tsx` import. Header comment names A9 and T-06-16 (must stay free of server-only imports).

    Append to `tests/design-system-gate.test.ts` a `describe("PLAT-05 type sweep + chrome (07-04)")`: (1) no `.ts/.tsx` under `src` contains `text-[1.75rem]`; (2) none contains `text-lg` as a whole class token (regex `/\btext-lg\b/`); (3) none contains `target="_blank"` or `rel="noreferrer"`; (4) `src/components/site-header.tsx` contains `text-heading` and `min-h-11`; `src/components/site-footer.tsx` contains `pb-safe`, `Photos via Unsplash`, and at least two `min-h-11`; (5) `src/app/not-found.tsx` exists, first line is not `"use client";`, contains `Page not found` and `Back to your results`, does not contain `@clerk/nextjs/server` or `from "@/db`; (6) `src/app/account/loading.tsx` exists, contains `Loading your account`, does not contain `@clerk/nextjs/server`, `from "@/db`, or `from "./page"`; (7) `src/components/account/delete-account-dialog.tsx` contains `text-heading`. Each assertion's expect message names the file.
  </action>
  <verify>
    <automated>npx vitest run tests/design-system-gate.test.ts && npm test && npm run typecheck && npm run lint && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/app/not-found.tsx` exists, contains `Page not found`, contains no `"use client"`, imports only from `next/link`
    - `src/app/account/loading.tsx` exists, contains `Loading your account…`, imports nothing from `@clerk/nextjs/server`, `@/db`, or `./page`
    - `npx vitest run tests/design-system-gate.test.ts` exits 0 with the new describe block (≥ 7 new tests); temporarily reintroducing `text-lg` in bookmark-list.tsx fails it (perform and revert; note in SUMMARY)
    - `npm test` (full suite) exits 0; `npm run build` lists `/_not-found` and keeps `/methodology`, `/privacy` static
  </acceptance_criteria>
  <done>Every route, including 404 and the account wait, renders in the same editorial shell; the sweep is CI-pinned.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Route files → client bundle / static render | not-found.tsx and loading.tsx must not pull server-only or DB code |
| /privacy outbound links → WebView | Same-tab navigation to third-party legal pages |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-19 | Information disclosure | not-found.tsx / loading.tsx | mitigate | Gate asserts neither imports `@clerk/nextjs/server` or `@/db`; not-found renders fixed neutral copy only (T-04-12 precedent) |
| T-07-20 | Tampering (reverse-tabnabbing class) | /privacy external links | mitigate | `target="_blank"` removed entirely (same-tab), so `rel="noreferrer"` is no longer needed; gate forbids both tokens under src |
| T-07-14 | Repudiation | Photo credits | mitigate | Footer credit line "Photos via Unsplash" (plain text, no third-party link in the demo path) |
| T-07-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
- Full local gate green: `npm test && npm run typecheck && npm run lint && npm run build`
- `grep -rn "text-\[1.75rem\]\|\btext-lg\b\|target=\"_blank\"" src` empty
- Static routes remain `○` in the build table
</verification>

<success_criteria>
- One heading size token everywhere; masthead and dialog re-typed; 44px nav targets; safe-area footer with credit; no new-window links
- 404 and /account loading are designed states in the shared shell
</success_criteria>

<output>
Create `.planning/phases/07-editorial-polish-launch/07-04-SUMMARY.md` when done
</output>
