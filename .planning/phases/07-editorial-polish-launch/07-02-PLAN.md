---
phase: 07-editorial-polish-launch
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/brand.ts
  - src/app/globals.css
  - src/components/ui/button.tsx
  - src/components/ui/card.tsx
  - src/components/ui/dialog.tsx
  - src/components/ui/input.tsx
  - src/components/ui/label.tsx
  - src/app/og/route.tsx
  - tests/design-system-gate.test.ts
autonomous: true
requirements: [PLAT-05]
must_haves:
  truths:
    - "The three brand colors exist once in TypeScript (brand.ts) and the CSS token block carries the identical hex strings, enforced by a test"
    - "A 28px heading token (text-heading) exists so the seven arbitrary text-[1.75rem] copies can be retired in plan 07-04"
    - "Every shadcn neutral (border, input, ring, primary, muted, secondary, accent) is derived from ink — no achromatic oklch gray remains, and the inert .dark block is gone"
    - "Vendored primitives use only weights 400/600 and never shrink inputs below 16px on desktop"
    - "The /og card reads its colors from brand.ts and renders the AA-compliant terracotta #b25429"
  artifacts:
    - path: "src/lib/brand.ts"
      provides: "CREAM, INK, TERRACOTTA literal exports"
      contains: "export const TERRACOTTA = \"#b25429\""
    - path: "src/app/globals.css"
      provides: "@theme tokens (text-heading, re-tuned terracotta), warm :root neutrals, pb-safe utility, color-scheme light"
      contains: "--text-heading: 1.75rem"
    - path: "tests/design-system-gate.test.ts"
      provides: "PLAT-05 source-scan pins (extended by 07-04 and 07-05)"
      min_lines: 60
  key_links:
    - from: "tests/design-system-gate.test.ts"
      to: "src/lib/brand.ts + src/app/globals.css"
      via: "imports brand constants and asserts the CSS contains the same hex strings"
      pattern: "from \"../src/lib/brand\""
    - from: "src/app/og/route.tsx"
      to: "src/lib/brand.ts"
      via: "import { CREAM, INK, TERRACOTTA }"
      pattern: "from \"@/lib/brand\""
---

<objective>
Lay the design-system foundation every later polish touch depends on: brand constants, the missing heading token, the accent re-tune, warm neutrals replacing shadcn's cool grays, the safe-area utility, weight/size fixes in the five vendored primitives, and the /og route reading brand.ts — all pinned by a new source-scan test.

Purpose: PLAT-05 fails today on tokens, not on layout: seven files copy an arbitrary 28px size, inputs/borders/rings are achromatic gray under a warm palette, and the accent misses WCAG AA. Fixing the source of those values first lets 07-04's sweep be pure one-line class swaps.
Output: brand.ts, rewritten token/neutral blocks in globals.css, five vendored edits, og/route.tsx on brand constants, tests/design-system-gate.test.ts (first describe blocks).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-editorial-polish-launch/07-UI-SPEC.md
@.planning/phases/07-editorial-polish-launch/07-PATTERNS.md
@.planning/phases/07-editorial-polish-launch/07-RESEARCH.md
@src/app/globals.css
@src/lib/site.ts
@tests/guest-flow-gate.test.ts

<interfaces>
<!-- Contracts this plan creates; 07-04/07-05/07-09 import them. -->

From src/lib/brand.ts (NEW — literal-only, client-safe, no env, no node: imports):
```typescript
export const CREAM = "#faf7f2";
export const INK = "#262119";
export const TERRACOTTA = "#b25429";   // UI-SPEC A3 re-tune from #c05f33 (AA contrast)
```

From src/app/globals.css @theme (NEW tokens; existing display tokens unchanged):
```css
--text-heading: 1.75rem;
--text-heading--line-height: 1.2;
--color-terracotta: #b25429;
```
Utility class produced: `text-heading` (28px / 1.2), `pb-safe`.

Current og/route.tsx lines to replace: line 51 `const CREAM = "#faf7f2";`, line 52 `const INK = "#262119";`, line 151 `color: "#c05f33",`.
Vendored grep hits (current): ui/button.tsx:8 `font-medium`; ui/card.tsx:41 `font-medium`; ui/dialog.tsx:133 `font-medium`; ui/input.tsx:11 `file:font-medium` and `md:text-sm`; ui/label.tsx:16 `font-medium`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create brand.ts and rewrite the globals.css token, neutral, and base blocks</name>
  <files>src/lib/brand.ts, src/app/globals.css</files>
  <read_first>src/app/globals.css (entire file — the `@theme inline` map lines 8–50, `@theme` block 52–64, `:root` 66–99, `.dark` 101–133, `@layer base` 135–145), src/lib/site.ts (header-comment + JSDoc + `export const` shape to copy), .planning/phases/07-editorial-polish-launch/07-UI-SPEC.md (## Color — warm neutrals table and the accent re-tune rationale; ## Spacing Scale — pb-safe definition), .planning/phases/07-editorial-polish-launch/07-PATTERNS.md (sections `src/lib/brand.ts` and `src/app/globals.css`)</read_first>
  <action>
    Create `src/lib/brand.ts` in the `site.ts` shape (header `//` comment naming PLAT-05, that CSS cannot import TS so the design-system gate asserts equality, and that the module ships to the client bundle so it holds literals only): `export const CREAM = "#faf7f2"`, `export const INK = "#262119"`, `export const TERRACOTTA = "#b25429"`, each with a one-line JSDoc. No env override, no other exports.

    Edit `src/app/globals.css`:
    1. `@theme` block: change `--color-terracotta` to `#b25429`; add `--text-heading: 1.75rem;` and `--text-heading--line-height: 1.2;` before `--text-display` (same companion-variable convention). Update the block's leading comment to drop "starting values — refined in Phase 7".
    2. `@theme inline` map: delete the twelve `--color-sidebar-*` and `--color-chart-*` lines. Keep every other line.
    3. `:root`: keep `--background: #faf7f2`, `--foreground: #262119`, `--destructive: oklch(0.577 0.245 27.325)`, `--radius: 0.625rem`. Replace every other value with the UI-SPEC warm set: `--card: #ffffff`, `--card-foreground: #262119`, `--popover: #ffffff`, `--popover-foreground: #262119`, `--primary: #262119`, `--primary-foreground: #faf7f2`, `--secondary: rgb(38 33 25 / 0.05)`, `--secondary-foreground: #262119`, `--muted: rgb(38 33 25 / 0.05)`, `--muted-foreground: rgb(38 33 25 / 0.7)`, `--accent: rgb(38 33 25 / 0.05)`, `--accent-foreground: #262119`, `--border: rgb(38 33 25 / 0.12)`, `--input: rgb(38 33 25 / 0.24)`, `--ring: #262119`. Delete all `--chart-*` and `--sidebar-*` declarations. No `color-mix(` anywhere in hand-written CSS (UI-SPEC A4).
    4. Delete the entire `.dark { … }` block. Keep line 5 `@custom-variant dark (&:is(.dark *));` (UI-SPEC A10 — removing it would let the vendored `dark:` classes activate on dark-mode phones).
    5. After the `@theme` block add `@utility pb-safe { padding-bottom: max(--spacing(8), env(safe-area-inset-bottom)); }` with a comment naming PLAT-02 / RESEARCH Pattern 3.
    6. In `@layer base`, extend the existing `html { @apply font-sans; }` rule with `color-scheme: light;`.
    Run `npx prettier --write src/app/globals.css src/lib/brand.ts` afterwards so the CSS formatting matches the repo.
  </action>
  <verify>
    <automated>grep -c -- "--text-heading: 1.75rem" src/app/globals.css && grep -c -- "--color-terracotta: #b25429" src/app/globals.css && grep -c "@utility pb-safe" src/app/globals.css && grep -c "color-scheme: light" src/app/globals.css && ! grep -q "^\.dark" src/app/globals.css && ! grep -q "sidebar\|chart-\|color-mix(" src/app/globals.css && grep -c "@custom-variant dark" src/app/globals.css && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - `src/lib/brand.ts` exports exactly CREAM `"#faf7f2"`, INK `"#262119"`, TERRACOTTA `"#b25429"`; the file contains no `process.env` and no `node:` import
    - `globals.css` contains `--text-heading: 1.75rem`, `--text-heading--line-height: 1.2`, `--color-terracotta: #b25429`, `@utility pb-safe`, `color-scheme: light`, `@custom-variant dark`
    - `globals.css` contains zero occurrences of `.dark {`, `--sidebar`, `--chart-`, `color-mix(`, and zero `oklch(` values other than the single `--destructive` line
    - `--border: rgb(38 33 25 / 0.12)`, `--input: rgb(38 33 25 / 0.24)`, `--ring: #262119`, `--primary: #262119`, `--primary-foreground: #faf7f2` are present verbatim
    - `npm run build` exits 0 (Tailwind v4 accepts `@utility` with `--spacing(8)` and the `text-heading` utility compiles)
  </acceptance_criteria>
  <done>Tokens, neutrals, and the safe-area utility exist; the build proves Tailwind accepts them.</done>
</task>

<task type="auto">
  <name>Task 2: Fix the five vendored primitives (weights 600 only, 16px inputs) and move /og onto brand.ts</name>
  <files>src/components/ui/button.tsx, src/components/ui/card.tsx, src/components/ui/dialog.tsx, src/components/ui/input.tsx, src/components/ui/label.tsx, src/app/og/route.tsx</files>
  <read_first>src/components/ui/button.tsx (line 8), src/components/ui/card.tsx (line 41), src/components/ui/dialog.tsx (line 133), src/components/ui/input.tsx (line 11), src/components/ui/label.tsx (line 16), src/app/og/route.tsx (lines 1–60 imports and the local CREAM/INK constants at 51–52; line 151 `color: "#c05f33"`), tests/og-route.test.ts (what it asserts — status/headers only)</read_first>
  <action>
    In each vendored file change the base class string only (UI-SPEC A12 — edit in place, keep every other class including the `dark:` ones, which stay inert): `ui/button.tsx` line 8 `font-medium` → `font-semibold`; `ui/card.tsx` line 41 (CardTitle) `font-medium` → `font-semibold`; `ui/dialog.tsx` line 133 (DialogTitle) `font-medium` → `font-semibold`; `ui/input.tsx` line 11 `file:font-medium` → `file:font-semibold` AND delete the `md:text-sm` token (inputs stay `text-base` at every width — iOS focus-zoom guard and UI-SPEC body rule); `ui/label.tsx` line 16 `font-medium` → `font-semibold`.

    In `src/app/og/route.tsx`: add `import { CREAM, INK, TERRACOTTA } from "@/lib/brand";` in the `@/` import group (alphabetical: after `@/lib/balance-params`, before `@/lib/share-content`); delete the two local `const CREAM` / `const INK` lines; replace the literal `"#c05f33"` at line 151 with `TERRACOTTA`. Search the file for any other `#`-hex literal and replace it with the matching brand constant — after this task the route contains no six-digit hex literal. Update the header comment's color sentence (line ~123 mentions terracotta) only if it names the old hex.
  </action>
  <verify>
    <automated>! grep -rq "font-medium" src/components/ui && ! grep -q "md:text-sm" src/components/ui/input.tsx && grep -c 'from "@/lib/brand"' src/app/og/route.tsx && ! grep -Eq '#[0-9a-fA-F]{6}' src/app/og/route.tsx && npx vitest run tests/og-route.test.ts && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `grep -r "font-medium" src/components/ui` returns no lines; `grep "md:text-sm" src/components/ui/input.tsx` returns no lines
    - `src/app/og/route.tsx` imports `{ CREAM, INK, TERRACOTTA }` from `@/lib/brand`, contains no `#c05f33`, and contains no regex match for `#[0-9a-fA-F]{6}`
    - `npx vitest run tests/og-route.test.ts` passes (3 real PNG renders)
    - `npm run typecheck` and `npm run lint` exit 0
  </acceptance_criteria>
  <done>Vendored primitives obey the two-weight / 16px-input contract and the social card renders the AA terracotta from the single source.</done>
</task>

<task type="auto">
  <name>Task 3: Create tests/design-system-gate.test.ts pinning everything this plan landed</name>
  <files>tests/design-system-gate.test.ts</files>
  <read_first>tests/guest-flow-gate.test.ts (helpers `sourceFiles`, `read`, forbidden-token loop, `expect(value, file)` messages — copy this shape exactly), .planning/phases/07-editorial-polish-launch/07-PATTERNS.md (section "tests/design-system-gate.test.ts and tests/launch-gate.test.ts" — the assertion list), .planning/phases/07-editorial-polish-launch/07-RESEARCH.md (Code Examples → Design-system gate), src/lib/brand.ts, src/app/globals.css</read_first>
  <action>
    Create `tests/design-system-gate.test.ts` (node env, relative `../src/…` imports, header comment naming PLAT-05 / PLAT-02 and threat IDs T-07-04, T-07-12). Extend the copied `sourceFiles` helper to match `/\.(ts|tsx|css)$/`. Import `{ CREAM, INK, TERRACOTTA }` from `../src/lib/brand` (literal-only, node-safe). Describe blocks and assertions (each `it` names the offending file in its expect message):

    `describe("PLAT-05 tokens (07-02)")`:
    - globals.css matches `/--text-heading:\s*1\.75rem/` and `/--text-heading--line-height:\s*1\.2/`
    - globals.css contains `--color-cream: ${CREAM}`, `--color-ink: ${INK}`, `--color-terracotta: ${TERRACOTTA}` (template strings from the imports — the CSS/TS equality gate)
    - globals.css contains `@utility pb-safe`, `color-scheme: light`, `@custom-variant dark`
    - globals.css does not match `/^\.dark\s*\{/m`; does not contain `color-mix(`, `--sidebar`, `--chart-`
    - globals.css: the number of `oklch(` occurrences equals 1 (the destructive token)

    `describe("vendored primitives obey the two-weight / 16px contract (UI-SPEC A12)")`:
    - no file under `src/components/ui` contains `font-medium`
    - `src/components/ui/input.tsx` does not contain `md:text-sm`

    `describe("brand colors have one source (T-07-04 hotlink / drift)")`:
    - `src/app/og/route.tsx` contains `from "@/lib/brand"` and does not match `/#[0-9a-fA-F]{6}/`
    - `src/lib/brand.ts` does not contain `process.env`
    - `next.config.ts` does not contain `remotePatterns` and does not contain `images.unsplash.com`

    `describe("PLAT-02 viewport-height units never lie inside WebViews")`:
    - no `.ts/.tsx/.css` file under `src` matches `/\b(h-screen|min-h-screen|100vh|dvh)\b/` (true already; pins the rule)

    `describe("accent budget (UI-SPEC: terracotta only on the hero, the CTA, the bonus badge)")`:
    - for every `.tsx` under `src/components`, collect non-comment lines (trim, skip lines starting with `//`, `/*`, `*`, or `{/*`) containing `terracotta`; the set of files with at least one such line must be a subset of `["core-experience.tsx", "result-card.tsx", "share-link.tsx"]` (share-link.tsx arrives in 07-05; core-experience.tsx is dropped from the set there)

    Leave a `// extended in 07-04 (type sweep) and 07-05 (WebView pins)` comment at the end of the file so later plans append rather than rewrite.
  </action>
  <verify>
    <automated>npx vitest run tests/design-system-gate.test.ts && npm test && npm run typecheck && npm run lint</automated>
  </verify>
  <acceptance_criteria>
    - `npx vitest run tests/design-system-gate.test.ts` exits 0 with at least 12 passing tests
    - The test file imports `../src/lib/brand` and contains the template assertions for `--color-terracotta: ${TERRACOTTA}` (so changing either side alone fails)
    - Temporarily reverting `--color-terracotta` to `#c05f33` in globals.css makes the test fail (executor performs and reverts this mutation check; note the result in the SUMMARY)
    - `npm test` (full suite, 20 existing files + this one) exits 0
  </acceptance_criteria>
  <done>The token/neutral/weight contract is CI-enforced; 07-04 and 07-05 extend this file rather than re-deriving it.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS tokens ↔ TS constants | Two representations of the brand palette; drift would ship a different color on /og than on the page |
| Vendored shadcn source | Edited in place; future `shadcn add` could regress — the gate test catches it |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-04 | Tampering / Availability | Image and color sources | mitigate | Gate test forbids `remotePatterns` in next.config.ts and pins brand hex equality — no runtime third-party color/image source |
| T-07-12 | Tampering | Vendored ui/* edited in place | mitigate | Gate asserts `font-medium` = 0 and `md:text-sm` = 0 so a re-vendor cannot silently reintroduce them |
| T-07-17 | Information disclosure | brand.ts ships to the client bundle | mitigate | Gate asserts brand.ts contains no `process.env`; the module is literal-only |
| T-07-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `npm run build`, `npm test`, `npm run typecheck`, `npm run lint` all exit 0
- `tests/design-system-gate.test.ts` green; mutation check (terracotta revert) fails the gate
- `/og` renders with `#b25429` (visible in plan 07-10's Post Inspector re-scrape)
</verification>

<success_criteria>
- Heading token, warm neutrals, safe-area utility, and AA accent exist once and are pinned by a test
- No achromatic gray, no `.dark` block, no `font-medium`, no desktop 14px inputs remain in vendored code
- /og reads colors from brand.ts
</success_criteria>

<output>
Create `.planning/phases/07-editorial-polish-launch/07-02-SUMMARY.md` when done
</output>
