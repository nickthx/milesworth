---
phase: 07-editorial-polish-launch
plan: 10
type: execute
wave: 6
depends_on: ["07-09"]
files_modified: []
autonomous: false
requirements: [PLAT-02, PLAT-05]
user_setup:
  - service: vercel
    why: "Custom domain + production env vars (only if D7-01 = a)"
    env_vars:
      - name: NEXT_PUBLIC_SITE_URL
        source: "Vercel Dashboard → Project → Settings → Environment Variables (Production) — the https origin of the custom domain"
      - name: NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY
        source: "Clerk Dashboard → production instance → API keys (pk_live_…)"
      - name: CLERK_SECRET_KEY
        source: "Clerk Dashboard → production instance → API keys (sk_live_…) — entered in Vercel only, never in chat or git"
    dashboard_config:
      - task: "Add the custom domain and set it as the production domain; DNS per Vercel's instructions"
        location: "Vercel Dashboard → Project → Settings → Domains"
      - task: "Create a production Clerk instance for the domain; enable Email code; social connections per D7-02; consent-to-legal ON with the /privacy URL (06-01 settings)"
        location: "Clerk Dashboard → Create production instance"
must_haves:
  truths:
    - "Production serves robots.txt (disallow /account) and sitemap.xml, / has no noindex, /account has noindex, the branded icon and the OG image resolve, and the host in og:url matches the live origin"
    - "The Lighthouse gate passes with SEO as an error on the live origin"
    - "LinkedIn's Post Inspector shows the fresh OG card for the launch share link and the phone opens it inside the LinkedIn app with no Vercel/Clerk development artifacts (badge absent if D7-01 = a)"
  artifacts:
    - path: ".planning/phases/07-editorial-polish-launch/07-10-SUMMARY.md"
      provides: "curl probe transcript, final Lighthouse table, Post Inspector result, launch URL"
      contains: "robots.txt"
  key_links:
    - from: "SITE_URL (env NEXT_PUBLIC_SITE_URL or constant)"
      to: "og:url, /og footer host, robots sitemap URL"
      via: "src/lib/site.ts single source (T-05-10)"
      pattern: "SITE_URL"
---

<objective>
Ship the launch: cut over to the custom domain and Clerk production instance if D7-01 chose it, deploy the 07-09 flip, prove every launch surface with curl probes and a final Lighthouse run (SEO as error), then have Nick re-scrape the share link in LinkedIn's Post Inspector and open it on the phone one last time.

Purpose: The phase goal is judged on the device that matters. This plan is the last check before the LinkedIn post: live host, live metadata, live card.
Output: launch URL verified end-to-end, probe transcript and scores in the SUMMARY.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-editorial-polish-launch/07-01-SUMMARY.md
@.planning/phases/07-editorial-polish-launch/07-07-SUMMARY.md
@.planning/phases/07-editorial-polish-launch/07-09-SUMMARY.md
@.planning/phases/07-editorial-polish-launch/07-RESEARCH.md
@.planning/phases/06-accounts-legal/06-07-PLAN.md
@src/lib/site.ts

<interfaces>
<!-- Live-surface facts. -->

HOST = custom domain origin if D7-01 = a (e.g. https://milesworth.com), else https://milesworth.vercel.app.
SITE_URL resolution: `process.env.NEXT_PUBLIC_SITE_URL ?? "https://milesworth.vercel.app"`; `new URL(SITE_URL)` throws at boot on a
malformed value (site.ts) — so a bad env var shows up as a failed deployment, not a wrong card.
Vercel CLI 57 is installed and linked (project points-unlocked / team_lQXwUaJBkdtDP5Aq7Bv443mZ). Useful: `vercel ls --prod`,
`vercel inspect <deployment-url>` (its meta lines include the git commit SHA the build came from), `vercel env ls production`, `vercel --prod` (redeploy from the current main if the env change needs a rebuild).
Deploy-by-push (06-07 Task 1 step 2 precedent): origin/main is production; 07-08 and 07-09 commit but do not push, so this plan pushes the flip:
`git status --porcelain` empty → `git merge-base --is-ancestor origin/main HEAD` exits 0 → `git push origin HEAD:main` (never force-push) → poll every 15s up to 4 min.
Phase 6 precedent for production probes and the human walkthrough: 06-07-PLAN.md Tasks 1–3.
/og cache proof (05/06 precedent): `curl -sI "HOST/og?ur=90000&mr=50000"` → 308 to the `&d=` PNG; second request X-Vercel-Cache: HIT.
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Domain + Clerk production cut-over (ONLY if D7-01 = a; otherwise reply "skip")</name>
  <files>(none — external dashboards; secrets entered in Vercel, never in chat)</files>
  <read_first>.planning/phases/07-editorial-polish-launch/07-01-SUMMARY.md (D7-01 and the domain), .planning/phases/06-accounts-legal/06-01-SUMMARY.md (the Clerk settings to replicate: consent-to-legal ON with /privacy URL, self-serve delete OFF, no Terms URL), .planning/phases/07-editorial-polish-launch/07-RESEARCH.md (Open Question 1 cut-over sequence)</read_first>
  <action>If 07-01-SUMMARY says `D7-01: b`, do nothing: record "Task 1 skipped per D7-01: b" in the SUMMARY and continue to Task 2 without pausing. If `D7-01: a`, pause and present the steps below; these have no CLI path Claude can complete (domain purchase, DNS at the registrar, Clerk instance creation, and secret entry must be done by Nick).</action>
  <what-built>Nothing yet on the new host. The code is host-agnostic through SITE_URL (site.ts); the OG route, robots.ts, sitemap.ts, and metadataBase all follow NEXT_PUBLIC_SITE_URL.</what-built>
  <how-to-verify>
    1. Buy the domain from D7-01. In Vercel → Project → Settings → Domains add it, set it as the production domain, and complete the DNS records Vercel shows; wait until Vercel reports "Valid Configuration".
    2. Clerk Dashboard → create a **production** instance for the domain. Replicate 06-01: Email code enabled; social connections per D7-02; consent-to-legal ON pointing at `https://<domain>/privacy`; self-serve delete OFF. Follow Clerk's DNS steps for the production instance (CNAME records) until Clerk shows them verified.
    3. Vercel → Settings → Environment Variables (Production only): set `NEXT_PUBLIC_SITE_URL` = `https://<domain>` (no trailing slash), `NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY` = the production `pk_live_…`, `CLERK_SECRET_KEY` = the production `sk_live_…`. Do not paste any key in this chat.
    4. Reply "domain ready" — the executor redeploys and verifies. (Note: `vercel env pull` locally will now fetch development-env values only; production keys stay in Vercel.)
  </how-to-verify>
  <acceptance_criteria>
    - Reply is "skip" (D7-01 = b) or "domain ready" (D7-01 = a) with the domain named
    - When "domain ready": `vercel env ls production` lists NEXT_PUBLIC_SITE_URL, NEXT_PUBLIC_CLERK_PUBLISHABLE_KEY, CLERK_SECRET_KEY (names only — the executor never prints values)
  </acceptance_criteria>
  <verify>
    <human-check>Reply is "skip" (D7-01 = b, recorded in the SUMMARY) or "domain ready" with `vercel env ls production` listing the three variable names.</human-check>
  </verify>
  <resume-signal>Type "skip" or "domain ready"</resume-signal>
  <done>The final host and Clerk instance are configured (or explicitly kept as-is per D7-01) before any live probe runs.</done>
</task>

<task type="auto">
  <name>Task 2: Deploy the flip, probe every launch surface on HOST, run the final Lighthouse gate with SEO as an error</name>
  <files>(none — production verification; results go in the SUMMARY)</files>
  <read_first>.planning/phases/07-editorial-polish-launch/07-09-SUMMARY.md, .planning/phases/06-accounts-legal/06-07-PLAN.md (Task 1 step 2 deploy-by-push sequence; Task 2 probe style), config/lighthouserc.cjs, src/lib/site.ts</read_first>
  <action>
    Deploy the flip (06-07 Task 1 step 2 sequence): confirm `git status --porcelain` is empty and `git merge-base --is-ancestor origin/main HEAD` exits 0; then `git push origin HEAD:main`. If HEAD is not a descendant of origin/main, STOP and report — never force-push. Never print env values (T-07-08). Set HOST per D7-01. Poll `curl -s -o /dev/null -w '%{http_code}' HOST/robots.txt` every 15s up to 4 minutes until it returns 200 and the body contains `Disallow: /account` (the 07-09 flip marker — proof the new build is live, not the old one).

    Assert the deployed SHA: `vercel ls --prod` → take the newest READY deployment URL → `vercel inspect <that-url>` and confirm the commit SHA it reports equals `git rev-parse HEAD`; record both SHAs in the SUMMARY. If they differ, the push did not trigger a build (check the Vercel Git integration) — stop and report. If D7-01 = a and the env vars changed after the last build, run `vercel --prod` to rebuild with them, then re-run the SHA check on the new deployment.

    Probes (record each command + the relevant header/line in the SUMMARY; never print env values):
    - `curl -s HOST/robots.txt` → 200, body contains `Disallow: /account` and `Sitemap: HOST/sitemap.xml`
    - `curl -s HOST/sitemap.xml` → 200, contains `<loc>HOST/</loc>`, `HOST/methodology`, `HOST/privacy`, and not `/account`
    - `curl -s HOST/ | grep -o '<meta name="robots"[^>]*>'` → empty (no noindex on /); `curl -s HOST/methodology | grep -c noindex` → 0
    - `curl -s HOST/account | grep -o 'content="noindex[^"]*"'` → `content="noindex, nofollow"` (or equivalent) — the signed-out prompt page still carries the route metadata
    - `curl -s HOST/ | grep -o 'rel="icon"[^>]*'` → one PNG link, then `curl -sI HOST<that href>` → 200 `content-type: image/png`; same for the `apple-touch-icon` href
    - `curl -s "HOST/?ur=90000&mr=50000" | grep -o '<meta property="og:url"[^>]*>'` → content starts with HOST (host matches — T-05-10)
    - `curl -sI "HOST/og?ur=90000&mr=50000"` → 308 to the `&d=` URL; follow it twice → 200 `image/png`, second `x-vercel-cache: HIT`, no `set-cookie`
    - `curl -s HOST/nowhere -o /dev/null -w "%{http_code}"` → 404 and the body contains `Page not found`
    - `curl -s HOST/ | grep -c "Photos via "` ≥ 1; `grep -c "viewport-fit=cover"` ≥ 1

    Final gate: `LHCI_BASE_URL=HOST npm run lighthouse` (Windows: set the env var for the command) — must exit 0 with `categories:seo` now an error. Record the four-URL median table next to the 07-07 baseline. PSI spot-check for `HOST/` recorded too.
  </action>
  <verify>
    <automated>test -z "$(git status --porcelain)" && test "$(git rev-parse HEAD)" = "$(git rev-parse origin/main)" && node -e "const h=process.env.HOST||'https://milesworth.vercel.app';console.log('HOST '+h)" && curl -sf "${HOST:-https://milesworth.vercel.app}/robots.txt" | grep -q "Disallow: /account" && curl -sf "${HOST:-https://milesworth.vercel.app}/sitemap.xml" | grep -q "/methodology" && ! curl -s "${HOST:-https://milesworth.vercel.app}/methodology" | grep -q "noindex" && curl -s "${HOST:-https://milesworth.vercel.app}/account" | grep -q "noindex" && ! curl -s "${HOST:-https://milesworth.vercel.app}/sitemap.xml" | grep -q "/account" && LHCI_BASE_URL="${HOST:-https://milesworth.vercel.app}" npm run lighthouse</automated>
  </verify>
  <acceptance_criteria>
    - `git push origin HEAD:main` succeeded as a fast-forward (or the task stopped and reported the non-FF state); `git rev-parse origin/main` equals `git rev-parse HEAD`; `HOST/robots.txt` reached 200 with `Disallow: /account` within 4 minutes of the push
    - `vercel inspect` on the newest READY production deployment reports the same commit SHA as `git rev-parse HEAD` (both SHAs recorded in the SUMMARY); no env value appears in any task output
    - All probes above pass with the exact expectations; the transcript is in the SUMMARY with values redacted only where a secret could appear (none expected)
    - `og:url` and the `Sitemap:` line both start with HOST
    - `npm run lighthouse` exits 0 on HOST with SEO asserted as an error; the final median table is recorded
    - If D7-01 = a: `curl -s HOST/ | grep -c "pk_test_"` = 0 (no development publishable key in the HTML)
  </acceptance_criteria>
  <done>Every launch surface is proven live on the final host; the Lighthouse gate passes in full.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: LinkedIn Post Inspector re-scrape and final phone open of the launch link</name>
  <files>(none)</files>
  <read_first>.planning/phases/07-editorial-polish-launch/07-10-SUMMARY.md draft (HOST, probe results), .planning/phases/05-credibility-layer/05-05-PLAN.md (Post Inspector precedent, if present)</read_first>
  <action>Pause execution and present the three steps below. Record the Post Inspector result (title, image OK, URL on HOST) and the launch URL in 07-10-SUMMARY.md; if Nick reports a discrepancy, record it under `## Launch findings` for gap closure rather than improvising a fix here.</action>
  <what-built>Production on HOST with the launch flip live: indexable site with /account private, robots/sitemap, branded icon, verified dataset at N, editorial imagery, WebView hardening, and a green Lighthouse gate including SEO. Cache on LinkedIn's side still holds the pre-launch card for any URL it has seen before — the Post Inspector forces a fresh scrape.</what-built>
  <how-to-verify>
    1. Open https://www.linkedin.com/post-inspector/ and inspect `HOST/?ur=90000&mr=50000`. Confirm: title/description from the share content, the OG image renders the cream card with the terracotta delta (new hex, slightly deeper), and the reported URL is on HOST. Then inspect `HOST/` (baseline card) the same way.
    2. On your iPhone in the LinkedIn app, DM yourself `HOST/?ur=90000&mr=50000`; confirm the unfurl matches step 1; tap it and confirm the page opens in the in-app browser with the branded favicon in the browser chrome (if shown) and — if D7-01 = a — that "Sign in to save" opens a Clerk modal with NO "Development mode" badge.
    3. Optional: paste the link into a draft LinkedIn post (do not publish) and confirm the preview card.
    Reply "launch verified" or describe what differs.
  </how-to-verify>
  <acceptance_criteria>
    - Reply contains "launch verified" (or an issue list recorded under `## Launch findings` in the SUMMARY for gap closure)
    - SUMMARY records the Post Inspector result (title, image OK, URL on HOST) and the launch URL
  </acceptance_criteria>
  <verify>
    <human-check>Nick replied "launch verified" (or listed a discrepancy now recorded in the SUMMARY); the Post Inspector result and launch URL are in the SUMMARY.</human-check>
  </verify>
  <resume-signal>Type "launch verified" or describe the discrepancy</resume-signal>
  <done>The launch share link unfurls freshly on LinkedIn and opens correctly on the phone — all three phase success criteria hold on production.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local repo → origin/main → Vercel production | The push that makes the launch flip public |
| Nick → Vercel/Clerk dashboards | Production secrets and DNS; never transit chat or git |
| Public internet / crawlers → HOST | Site is now indexable |
| LinkedIn scraper → HOST/og | Cached social card |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-08 | Information disclosure | Clerk sk_live / env values | mitigate | Entered only in Vercel by Nick; executor prints env *names* only (`vercel env ls`); no env values printed during the push, `vercel inspect`, or probes; acceptance forbids `pk_test_` in live HTML after cut-over |
| T-07-13 | Spoofing / open-redirect class | NEXT_PUBLIC_SITE_URL | mitigate | Must be the exact https origin; `new URL()` in site.ts fails the build on garbage; probe asserts og:url and Sitemap start with HOST |
| T-07-06 | Information disclosure | /account indexable | mitigate | Live probe asserts noindex on /account, none on /, and no /account in sitemap.xml |
| T-07-07 | Denial of service | /og under the final Lighthouse + inspector | mitigate | 4 HTML URLs only; /og probed with 3 requests; CDN HIT proven |
| T-07-27 | Repudiation (stale card) | LinkedIn cache | mitigate | Post Inspector forced re-scrape recorded with the result |
| T-07-SC | Tampering | package installs | accept | This plan installs nothing |
</threat_model>

<verification>
- `git push origin HEAD:main` fast-forwarded; `vercel inspect` SHA = `git rev-parse HEAD`
- Probe transcript green on HOST; `npm run lighthouse` green with SEO as error
- Post Inspector shows the fresh card; phone open verified by Nick
</verification>

<success_criteria>
- The launch link works end-to-end on the final host inside LinkedIn's browser and on the social card
- All three phase success criteria are demonstrably true on production
</success_criteria>

<output>
Create `.planning/phases/07-editorial-polish-launch/07-10-SUMMARY.md` when done (launch URL, probe transcript, final Lighthouse table, Post Inspector result)
</output>
