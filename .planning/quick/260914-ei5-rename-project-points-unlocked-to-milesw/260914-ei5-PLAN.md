---
phase: quick-260914-ei5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/layout.tsx
  - src/app/methodology/page.tsx
  - src/app/page.tsx
  - src/components/site-footer.tsx
  - src/lib/share-content.ts
  - src/lib/site.ts
  - tests/share-content.test.ts
  - package.json
  - package-lock.json
  - CLAUDE.md
  - PROJECT-BRIEF.md
  - .planning/**/*.md
autonomous: false
requirements: [QUICK-260914-ei5]

must_haves:
  truths:
    - "Every user-visible surface (title, OG siteName/title/alt, share card, footer, methodology title) says Milesworth, never Points Unlocked"
    - "The fallback site origin is https://milesworth.vercel.app and metadataBase resolves to it when NEXT_PUBLIC_SITE_URL is unset"
    - "package.json name is milesworth and lint/typecheck/test/build are all green"
    - "No .planning doc, CLAUDE.md, or PROJECT-BRIEF.md still contains 'Points Unlocked' or 'points-unlocked' (except this quick task's own folder/plan)"
    - "git remote origin points at github.com/nickthx/milesworth and git ls-remote succeeds"
    - "Vercel project is named milesworth, still linked to nickthx/milesworth, and https://milesworth.vercel.app serves HTTP 200 containing 'Milesworth'"
  artifacts:
    - path: "src/lib/site.ts"
      provides: "SITE_URL fallback https://milesworth.vercel.app"
      contains: "https://milesworth.vercel.app"
    - path: "src/lib/share-content.ts"
      provides: "Baseline share content branded Milesworth"
      contains: "Milesworth"
    - path: "package.json"
      provides: "Package name milesworth"
      contains: "\"name\": \"milesworth\""
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/lib/site.ts"
      via: "metadataBase: new URL(SITE_URL)"
      pattern: "new URL\\(SITE_URL\\)"
    - from: "tests/share-content.test.ts"
      to: "src/lib/share-content.ts"
      via: "asserts baseline title/eyebrow/imageAlt equal Milesworth strings"
      pattern: "Milesworth"
---

<objective>
Rename the project from "Points Unlocked" (slug `points-unlocked`) to "Milesworth" (slug `milesworth`) everywhere it is authored: app code and metadata, tests, package manifests, root CLAUDE.md and PROJECT-BRIEF.md, all `.planning` docs, the GitHub repo, and the Vercel project.

Purpose: The product is launching under the Milesworth name (future custom domain milesworth.app). Every surface a recruiter, crawler, or collaborator sees must carry one name, and the deploy target must be `milesworth.vercel.app` before Phase 6/7 wire Clerk and the share cards against a fixed origin.

Output: Two commits (app/code rename; planning-docs rename + remote switch), a renamed GitHub repo, a renamed Vercel project, and a live `https://milesworth.vercel.app`.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@CLAUDE.md
@src/lib/site.ts
@src/lib/share-content.ts
@src/app/layout.tsx

<interfaces>
<!-- Exact occurrences already inventoried by the orchestrator. Do not re-scan broadly. -->

From src/lib/site.ts (the only origin constant in the app; layout.tsx metadataBase and the /og footer read it):
```typescript
export const SITE_URL =
  process.env.NEXT_PUBLIC_SITE_URL ?? "https://points-unlocked.vercel.app";
export const SITE_HOST = new URL(SITE_URL).host;
```

From src/lib/share-content.ts `baselineContent()` (lines 77, 79, 84): `title: "Points Unlocked"`, `eyebrow: "Points Unlocked"`, `imageAlt: "Points Unlocked — what are your points actually worth?"`.

From src/app/layout.tsx `metadata` (lines 30, 43, 44, 51, 57): `title`, `openGraph.siteName`, `openGraph.title`, `openGraph.images[0].alt`, `twitter.title` — all "Points Unlocked".

Other code occurrences: src/app/methodology/page.tsx line 23 ("Methodology — Points Unlocked"), src/app/page.tsx line 51 (siteName), src/components/site-footer.tsx line 16, tests/share-content.test.ts (asserts on the name).

Manifests: package.json line 2 `"name": "points-unlocked"`; package-lock.json lines 2 and 8 `"name": "points-unlocked"` (the only two occurrences of that exact string in the lock file).

Scripts available in package.json: `lint` (eslint), `typecheck` (tsc --noEmit), `test` (vitest run), `build` (next build).

Do NOT edit: `.vercel/project.json` (gitignored, owned by `vercel link`), `node_modules`, `.next`, `.git`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename app code, tests, manifests, root CLAUDE.md and PROJECT-BRIEF.md; flip site origin fallback</name>
  <files>src/app/layout.tsx, src/app/methodology/page.tsx, src/app/page.tsx, src/components/site-footer.tsx, src/lib/share-content.ts, src/lib/site.ts, tests/share-content.test.ts, package.json, package-lock.json, CLAUDE.md, PROJECT-BRIEF.md</files>
  <action>
Run one targeted replacement pass (Git Bash `sed -i`) over exactly the eleven files listed, in this order of substitutions: `Points Unlocked` → `Milesworth`, `PointsUnlocked` → `Milesworth`, `Points-Unlocked` → `Milesworth`, `points-unlocked` → `milesworth`. The slug substitution is what turns the `src/lib/site.ts` fallback into `https://milesworth.vercel.app` (and the doc comment host examples on lines 12 and 17 into `milesworth.vercel.app`); it also turns `package.json` line 2 and `package-lock.json` lines 2 and 8 into `"name": "milesworth"`. For `package-lock.json` restrict the sed to the exact string `"name": "points-unlocked"` so nothing else in the lock file is touched; do not run `npm install` (no dependency change, avoid lock churn). Do NOT touch `.vercel/project.json` — Vercel owns it and Task 3 renames the project in the dashboard. Leave the Task-1 text in CLAUDE.md/PROJECT-BRIEF.md otherwise intact; only the name/slug strings change. After the sed, run `git diff --stat` and confirm only those eleven files changed, then confirm zero remaining occurrences with `grep -rn "Points Unlocked\|points-unlocked\|PointsUnlocked" src tests package.json package-lock.json CLAUDE.md PROJECT-BRIEF.md` (expect no output). Then run the four gates in sequence: `npm run lint`, `npm run typecheck`, `npm test`, `npm run build` — all must exit 0 (the `tests/share-content.test.ts` assertions were renamed in the same pass, so the suite must still pass unchanged in count). Commit with message `chore: rename Points Unlocked to Milesworth in app code, tests, and manifests`. Mention in the SUMMARY that the local folder `C:\Users\geoca\points-unlocked` was intentionally NOT renamed (renaming it mid-session breaks the session; optional manual step for the user later).
  </action>
  <verify>
    <automated>grep -rn "Points Unlocked\|points-unlocked\|PointsUnlocked" src tests package.json package-lock.json CLAUDE.md PROJECT-BRIEF.md | wc -l | grep -qx 0 && grep -q 'https://milesworth.vercel.app' src/lib/site.ts && grep -q '"name": "milesworth"' package.json && npm run lint && npm run typecheck && npm test && npm run build</automated>
  </verify>
  <done>Zero "Points Unlocked"/"points-unlocked" occurrences in src, tests, package manifests, CLAUDE.md, PROJECT-BRIEF.md; `SITE_URL` falls back to `https://milesworth.vercel.app`; lint, typecheck, test, and build all green; one commit on main.</done>
</task>

<task type="auto">
  <name>Task 2: Bulk-rename .planning docs, rename the GitHub repo, and switch the git remote</name>
  <files>.planning/**/*.md (43 files incl. PROJECT.md, ROADMAP.md, STATE.md, REQUIREMENTS.md, research, phase PLAN/SUMMARY/CONTEXT/VALIDATION files), .git/config (remote URL)</files>
  <action>
Bulk replace across every `*.md` under `.planning/` EXCEPT this quick task's own directory (`.planning/quick/260914-ei5-rename-project-points-unlocked-to-milesw/` — its folder name and plan text intentionally describe the old name): use `find .planning -name '*.md' -not -path '.planning/quick/260914-ei5*' -exec sed -i 's/Points Unlocked/Milesworth/g; s/PointsUnlocked/Milesworth/g; s/Points-Unlocked/Milesworth/g; s/points-unlocked/milesworth/g' {} +`. This single pass also rewrites every `https://points-unlocked.vercel.app/...` URL to `https://milesworth.vercel.app/...` (including the Phase 6 plan 06-01 Task 3 privacy-policy URL `https://milesworth.vercel.app/privacy`, the Clerk app name "Milesworth" in its step 1, and the "Vercel -> milesworth -> Settings" dashboard paths). Do NOT touch `node_modules`, `.git`, `.next`, or `package-lock.json` in this pass. Verify with `grep -rln "Points Unlocked\|points-unlocked\|PointsUnlocked" .planning --include='*.md' | grep -v '^.planning/quick/260914-ei5'` (expect empty). Then rename the GitHub repo: `gh repo rename milesworth --repo nickthx/points-unlocked --yes` (gh is authenticated as nickthx; GitHub auto-redirects the old URL), then `git remote set-url origin https://github.com/nickthx/milesworth.git`, then `git ls-remote origin HEAD` must succeed and `git remote get-url origin` must print the new URL. Commit the planning-doc changes with message `docs: rename Points Unlocked to Milesworth across planning docs`. Finally attempt `git push origin main` so Vercel builds the renamed code (Vercel's Git integration follows the repo rename automatically because it links by repo ID). If the push fails for credential/TTY reasons (Phase 6 recorded a TTY push checkpoint on this machine), do not retry in a loop — record it and fold "push main" into Task 3's human instructions.
  </action>
  <verify>
    <automated>test -z "$(grep -rln 'Points Unlocked\|points-unlocked\|PointsUnlocked' .planning --include='*.md' | grep -v '^.planning/quick/260914-ei5')" && git remote get-url origin | grep -q 'github.com/nickthx/milesworth.git' && git ls-remote origin HEAD</automated>
  </verify>
  <done>All .planning markdown (other than this quick task's folder) uses Milesworth/milesworth, including all vercel.app URLs; GitHub repo is nickthx/milesworth; origin remote points at it and `git ls-remote` works; planning changes committed and main pushed (or push deferred to Task 3 with the reason recorded).</done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 3: Rename the Vercel project to milesworth and confirm the live host</name>
  <files>(none — Vercel dashboard only; do not edit .vercel/project.json)</files>
  <action>
Vercel CLI is not installed and there is no VERCEL_TOKEN, so the project rename cannot be automated. Before pausing, print the instructions below verbatim. After resume, verify the live host as described in <verify>. Record in the SUMMARY: the new default domain `milesworth.vercel.app`, that `points-unlocked.vercel.app` no longer resolves, and the downstream notes at the bottom of this plan.
  </action>
  <what-built>Code (Task 1) and planning docs (Task 2) are renamed and pushed; the GitHub repo is now nickthx/milesworth. The Vercel project still carries the old name and the old default domain until renamed in the dashboard.</what-built>
  <how-to-verify>
    1. Open https://vercel.com → the `points-unlocked` project → Settings → General → Project Name. Change it to `milesworth` and save. Vercel's default domain becomes `milesworth.vercel.app`; `points-unlocked.vercel.app` stops resolving.
    2. On the same Settings → Git screen, confirm the Connected Git Repository shows `nickthx/milesworth` (Vercel links by repo ID, so it should follow the GitHub rename). If it still shows the old name or is disconnected, reconnect it to `nickthx/milesworth`.
    3. If Task 2 reported that `git push origin main` was deferred, run `git push origin main` from a terminal now. Otherwise the push already triggered a Production deployment.
    4. Wait for the Production deployment of the latest main commit to finish (Deployments tab → Ready).
    5. Open https://milesworth.vercel.app in a browser; the page title and footer should read "Milesworth".
  </how-to-verify>
  <resume-signal>Type "vercel-renamed" when steps 1–5 are done</resume-signal>
  <verify>
    <automated>test "$(curl -s -o /dev/null -w '%{http_code}' https://milesworth.vercel.app)" = "200" && curl -s https://milesworth.vercel.app | grep -c "Milesworth" | grep -qv '^0$'</automated>
  </verify>
  <done>Vercel project is named milesworth, linked to nickthx/milesworth, and `https://milesworth.vercel.app` returns HTTP 200 with "Milesworth" in the HTML.</done>
</task>

</tasks>

<downstream_notes>
## Downstream notes (record in SUMMARY; do not act on here)

- **Clerk production instance is now possible.** Phase 6 plan 06-01 (assumption A1, threat T-06-12) assumed v1 must ship on a Clerk DEVELOPMENT instance because `*.vercel.app` cannot host a production instance. Once `milesworth.app` is purchased and attached to the Vercel project, a Clerk PRODUCTION instance becomes possible; the cut-over belongs in the Phase 7 launch checklist. Until then 06-01 Task 3 should use the privacy URL `https://milesworth.vercel.app/privacy` (already rewritten by Task 2).
- **Origin flip when the custom domain attaches.** `src/lib/site.ts` falls back to `https://milesworth.vercel.app`. When `milesworth.app` is live, set `NEXT_PUBLIC_SITE_URL=https://milesworth.app` on Vercel (Production) — or change the fallback — so `metadataBase`, the `/og` card footer, and share links all advertise the custom domain. Do not hard-code `milesworth.app` before the domain is purchased and attached.
- **Optional local folder rename.** The working directory `C:\Users\geoca\points-unlocked` was left as-is (renaming mid-session breaks the session). The user may later close the session and `Rename-Item C:\Users\geoca\points-unlocked C:\Users\geoca\milesworth`; nothing in the repo depends on the folder name (Vercel link is via `.vercel/project.json`, which is gitignored and re-creatable with `vercel link`).
</downstream_notes>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| local repo → GitHub / Vercel | Repo rename and remote switch via authenticated `gh`; project rename via dashboard |
| bulk text replace → tracked files | sed passes touch many files; wrong scope could corrupt lock file or vendored code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-Q-01 | Tampering | sed bulk replace | mitigate | Task 1 sed is restricted to an explicit eleven-file list; `package-lock.json` is limited to the exact `"name": "points-unlocked"` string; Task 2 is scoped to `.planning/**/*.md` and excludes node_modules/.git/.next; `git diff --stat` reviewed before each commit |
| T-Q-02 | Denial of Service | Vercel default domain | accept | `points-unlocked.vercel.app` stops resolving after the rename; no external links to it are published yet (site is noindex, pre-launch) |
| T-Q-03 | Information Disclosure | env files / keys | mitigate | No `.env*` file, `.vercel/project.json`, or key value is edited, printed, or committed by any task |
| T-Q-SC | Tampering | npm/pip/cargo installs | accept | No package installs in this plan; `npm install` is explicitly not run |
</threat_model>

<verification>
- `grep -rn "Points Unlocked\|points-unlocked\|PointsUnlocked" src tests package.json package-lock.json CLAUDE.md PROJECT-BRIEF.md` returns nothing
- `grep -rln "Points Unlocked\|points-unlocked" .planning --include='*.md' | grep -v '^.planning/quick/260914-ei5'` returns nothing
- `npm run lint && npm run typecheck && npm test && npm run build` all exit 0
- `git remote get-url origin` prints `https://github.com/nickthx/milesworth.git` and `git ls-remote origin HEAD` succeeds
- `curl -s -o /dev/null -w '%{http_code}' https://milesworth.vercel.app` prints 200 and the HTML contains "Milesworth"
</verification>

<success_criteria>
- Two commits on main: app/code rename and planning-doc rename
- App title, OG/twitter metadata, share card, footer, and methodology title say Milesworth
- `SITE_URL` fallback is `https://milesworth.vercel.app`; `package.json` name is `milesworth`
- GitHub repo is `nickthx/milesworth`; origin remote updated
- Vercel project renamed to `milesworth`, Git link confirmed, `https://milesworth.vercel.app` live with HTTP 200
- SUMMARY records the three downstream notes (Clerk production instance, NEXT_PUBLIC_SITE_URL flip, optional folder rename)
</success_criteria>

<output>
Create `.planning/quick/260914-ei5-rename-project-points-unlocked-to-milesw/260914-ei5-SUMMARY.md` when done
</output>
